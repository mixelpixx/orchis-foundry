// AUTO-GENERATED by scripts/build-web.mjs — do not edit.
// Source of truth: web/src/*.jsx. Rebuild with `cd build && npm run build`.
"use strict";

// ===== tweaks-panel.jsx =====
// tweaks-panel.jsx
// Reusable Tweaks shell + form-control helpers.
//
// Owns the host protocol (listens for __activate_edit_mode / __deactivate_edit_mode,
// posts __edit_mode_available / __edit_mode_set_keys / __edit_mode_dismissed) so
// individual prototypes don't re-roll it. Ships a consistent set of controls so you
// don't hand-draw <input type="range">, segmented radios, steppers, etc.
//
// Usage (in an HTML file that loads React + Babel):
//
//   const TWEAK_DEFAULTS = /*EDITMODE-BEGIN*/{
//     "primaryColor": "#D97757",
//     "palette": ["#D97757", "#29261b", "#f6f4ef"],
//     "fontSize": 16,
//     "density": "regular",
//     "dark": false
//   }/*EDITMODE-END*/;
//
//   function App() {
//     const [t, setTweak] = useTweaks(TWEAK_DEFAULTS);
//     return (
//       <div style={{ fontSize: t.fontSize, color: t.primaryColor }}>
//         Hello
//         <TweaksPanel>
//           <TweakSection label="Typography" />
//           <TweakSlider label="Font size" value={t.fontSize} min={10} max={32} unit="px"
//                        onChange={(v) => setTweak('fontSize', v)} />
//           <TweakRadio  label="Density" value={t.density}
//                        options={['compact', 'regular', 'comfy']}
//                        onChange={(v) => setTweak('density', v)} />
//           <TweakSection label="Theme" />
//           <TweakColor  label="Primary" value={t.primaryColor}
//                        options={['#D97757', '#2A6FDB', '#1F8A5B', '#7A5AE0']}
//                        onChange={(v) => setTweak('primaryColor', v)} />
//           <TweakColor  label="Palette" value={t.palette}
//                        options={[['#D97757', '#29261b', '#f6f4ef'],
//                                  ['#475569', '#0f172a', '#f1f5f9']]}
//                        onChange={(v) => setTweak('palette', v)} />
//           <TweakToggle label="Dark mode" value={t.dark}
//                        onChange={(v) => setTweak('dark', v)} />
//         </TweaksPanel>
//       </div>
//     );
//   }
//
// ─────────────────────────────────────────────────────────────────────────────

const __TWEAKS_STYLE = `
  .twk-panel{position:fixed;right:16px;bottom:16px;z-index:2147483646;width:280px;
    max-height:calc(100vh - 32px);display:flex;flex-direction:column;
    transform:scale(var(--dc-inv-zoom,1));transform-origin:bottom right;
    background:rgba(250,249,247,.78);color:#29261b;
    -webkit-backdrop-filter:blur(24px) saturate(160%);backdrop-filter:blur(24px) saturate(160%);
    border:.5px solid rgba(255,255,255,.6);border-radius:14px;
    box-shadow:0 1px 0 rgba(255,255,255,.5) inset,0 12px 40px rgba(0,0,0,.18);
    font:11.5px/1.4 ui-sans-serif,system-ui,-apple-system,sans-serif;overflow:hidden}
  .twk-hd{display:flex;align-items:center;justify-content:space-between;
    padding:10px 8px 10px 14px;cursor:move;user-select:none}
  .twk-hd b{font-size:12px;font-weight:600;letter-spacing:.01em}
  .twk-x{appearance:none;border:0;background:transparent;color:rgba(41,38,27,.55);
    width:22px;height:22px;border-radius:6px;cursor:default;font-size:13px;line-height:1}
  .twk-x:hover{background:rgba(0,0,0,.06);color:#29261b}
  .twk-body{padding:2px 14px 14px;display:flex;flex-direction:column;gap:10px;
    overflow-y:auto;overflow-x:hidden;min-height:0;
    scrollbar-width:thin;scrollbar-color:rgba(0,0,0,.15) transparent}
  .twk-body::-webkit-scrollbar{width:8px}
  .twk-body::-webkit-scrollbar-track{background:transparent;margin:2px}
  .twk-body::-webkit-scrollbar-thumb{background:rgba(0,0,0,.15);border-radius:4px;
    border:2px solid transparent;background-clip:content-box}
  .twk-body::-webkit-scrollbar-thumb:hover{background:rgba(0,0,0,.25);
    border:2px solid transparent;background-clip:content-box}
  .twk-row{display:flex;flex-direction:column;gap:5px}
  .twk-row-h{flex-direction:row;align-items:center;justify-content:space-between;gap:10px}
  .twk-lbl{display:flex;justify-content:space-between;align-items:baseline;
    color:rgba(41,38,27,.72)}
  .twk-lbl>span:first-child{font-weight:500}
  .twk-val{color:rgba(41,38,27,.5);font-variant-numeric:tabular-nums}

  .twk-sect{font-size:10px;font-weight:600;letter-spacing:.06em;text-transform:uppercase;
    color:rgba(41,38,27,.45);padding:10px 0 0}
  .twk-sect:first-child{padding-top:0}

  .twk-field{appearance:none;box-sizing:border-box;width:100%;min-width:0;height:26px;padding:0 8px;
    border:.5px solid rgba(0,0,0,.1);border-radius:7px;
    background:rgba(255,255,255,.6);color:inherit;font:inherit;outline:none}
  .twk-field:focus{border-color:rgba(0,0,0,.25);background:rgba(255,255,255,.85)}
  select.twk-field{padding-right:22px;
    background-image:url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='10' height='6' viewBox='0 0 10 6'><path fill='rgba(0,0,0,.5)' d='M0 0h10L5 6z'/></svg>");
    background-repeat:no-repeat;background-position:right 8px center}

  .twk-slider{appearance:none;-webkit-appearance:none;width:100%;height:4px;margin:6px 0;
    border-radius:999px;background:rgba(0,0,0,.12);outline:none}
  .twk-slider::-webkit-slider-thumb{-webkit-appearance:none;appearance:none;
    width:14px;height:14px;border-radius:50%;background:#fff;
    border:.5px solid rgba(0,0,0,.12);box-shadow:0 1px 3px rgba(0,0,0,.2);cursor:default}
  .twk-slider::-moz-range-thumb{width:14px;height:14px;border-radius:50%;
    background:#fff;border:.5px solid rgba(0,0,0,.12);box-shadow:0 1px 3px rgba(0,0,0,.2);cursor:default}

  .twk-seg{position:relative;display:flex;padding:2px;border-radius:8px;
    background:rgba(0,0,0,.06);user-select:none}
  .twk-seg-thumb{position:absolute;top:2px;bottom:2px;border-radius:6px;
    background:rgba(255,255,255,.9);box-shadow:0 1px 2px rgba(0,0,0,.12);
    transition:left .15s cubic-bezier(.3,.7,.4,1),width .15s}
  .twk-seg.dragging .twk-seg-thumb{transition:none}
  .twk-seg button{appearance:none;position:relative;z-index:1;flex:1;border:0;
    background:transparent;color:inherit;font:inherit;font-weight:500;min-height:22px;
    border-radius:6px;cursor:default;padding:4px 6px;line-height:1.2;
    overflow-wrap:anywhere}

  .twk-toggle{position:relative;width:32px;height:18px;border:0;border-radius:999px;
    background:rgba(0,0,0,.15);transition:background .15s;cursor:default;padding:0}
  .twk-toggle[data-on="1"]{background:#34c759}
  .twk-toggle i{position:absolute;top:2px;left:2px;width:14px;height:14px;border-radius:50%;
    background:#fff;box-shadow:0 1px 2px rgba(0,0,0,.25);transition:transform .15s}
  .twk-toggle[data-on="1"] i{transform:translateX(14px)}

  .twk-num{display:flex;align-items:center;box-sizing:border-box;min-width:0;height:26px;padding:0 0 0 8px;
    border:.5px solid rgba(0,0,0,.1);border-radius:7px;background:rgba(255,255,255,.6)}
  .twk-num-lbl{font-weight:500;color:rgba(41,38,27,.6);cursor:ew-resize;
    user-select:none;padding-right:8px}
  .twk-num input{flex:1;min-width:0;height:100%;border:0;background:transparent;
    font:inherit;font-variant-numeric:tabular-nums;text-align:right;padding:0 8px 0 0;
    outline:none;color:inherit;-moz-appearance:textfield}
  .twk-num input::-webkit-inner-spin-button,.twk-num input::-webkit-outer-spin-button{
    -webkit-appearance:none;margin:0}
  .twk-num-unit{padding-right:8px;color:rgba(41,38,27,.45)}

  .twk-btn{appearance:none;height:26px;padding:0 12px;border:0;border-radius:7px;
    background:rgba(0,0,0,.78);color:#fff;font:inherit;font-weight:500;cursor:default}
  .twk-btn:hover{background:rgba(0,0,0,.88)}
  .twk-btn.secondary{background:rgba(0,0,0,.06);color:inherit}
  .twk-btn.secondary:hover{background:rgba(0,0,0,.1)}

  .twk-swatch{appearance:none;-webkit-appearance:none;width:56px;height:22px;
    border:.5px solid rgba(0,0,0,.1);border-radius:6px;padding:0;cursor:default;
    background:transparent;flex-shrink:0}
  .twk-swatch::-webkit-color-swatch-wrapper{padding:0}
  .twk-swatch::-webkit-color-swatch{border:0;border-radius:5.5px}
  .twk-swatch::-moz-color-swatch{border:0;border-radius:5.5px}

  .twk-chips{display:flex;gap:6px}
  .twk-chip{position:relative;appearance:none;flex:1;min-width:0;height:46px;
    padding:0;border:0;border-radius:6px;overflow:hidden;cursor:default;
    box-shadow:0 0 0 .5px rgba(0,0,0,.12),0 1px 2px rgba(0,0,0,.06);
    transition:transform .12s cubic-bezier(.3,.7,.4,1),box-shadow .12s}
  .twk-chip:hover{transform:translateY(-1px);
    box-shadow:0 0 0 .5px rgba(0,0,0,.18),0 4px 10px rgba(0,0,0,.12)}
  .twk-chip[data-on="1"]{box-shadow:0 0 0 1.5px rgba(0,0,0,.85),
    0 2px 6px rgba(0,0,0,.15)}
  .twk-chip>span{position:absolute;top:0;bottom:0;right:0;width:34%;
    display:flex;flex-direction:column;box-shadow:-1px 0 0 rgba(0,0,0,.1)}
  .twk-chip>span>i{flex:1;box-shadow:0 -1px 0 rgba(0,0,0,.1)}
  .twk-chip>span>i:first-child{box-shadow:none}
  .twk-chip svg{position:absolute;top:6px;left:6px;width:13px;height:13px;
    filter:drop-shadow(0 1px 1px rgba(0,0,0,.3))}
`;

// ── useTweaks ───────────────────────────────────────────────────────────────
// Single source of truth for tweak values. setTweak persists via the host
// (__edit_mode_set_keys → host rewrites the EDITMODE block on disk).
function useTweaks(defaults) {
  const [values, setValues] = React.useState(defaults);
  // Accepts either setTweak('key', value) or setTweak({ key: value, ... }) so a
  // useState-style call doesn't write a "[object Object]" key into the persisted
  // JSON block.
  const setTweak = React.useCallback((keyOrEdits, val) => {
    const edits = typeof keyOrEdits === 'object' && keyOrEdits !== null ? keyOrEdits : {
      [keyOrEdits]: val
    };
    setValues(prev => ({
      ...prev,
      ...edits
    }));
    window.parent.postMessage({
      type: '__edit_mode_set_keys',
      edits
    }, '*');
    // Same-window signal so in-page listeners (deck-stage rail thumbnails)
    // can react — the parent message only reaches the host, not peers.
    window.dispatchEvent(new CustomEvent('tweakchange', {
      detail: edits
    }));
  }, []);
  return [values, setTweak];
}

// ── TweaksPanel ─────────────────────────────────────────────────────────────
// Floating shell. Registers the protocol listener BEFORE announcing
// availability — if the announce ran first, the host's activate could land
// before our handler exists and the toolbar toggle would silently no-op.
// The close button posts __edit_mode_dismissed so the host's toolbar toggle
// flips off in lockstep; the host echoes __deactivate_edit_mode back which
// is what actually hides the panel.
function TweaksPanel({
  title = 'Tweaks',
  noDeckControls = false,
  children
}) {
  const [open, setOpen] = React.useState(false);
  const dragRef = React.useRef(null);
  // Auto-inject a rail toggle when a <deck-stage> is on the page. The
  // toggle drives the deck's per-viewer _railVisible via window message;
  // state is mirrored from the same localStorage key the deck reads so
  // the control reflects reality across reloads. The mechanism is the
  // message — authors who want custom placement can post it directly
  // and pass noDeckControls to suppress this one.
  const hasDeckStage = React.useMemo(() => typeof document !== 'undefined' && !!document.querySelector('deck-stage'), []);
  // deck-stage enables its rail in connectedCallback, but this panel can
  // mount before that element has upgraded. The initial read catches the
  // common case; the listener covers mounting first. (Older deck-stage.js
  // copies still wait for the host's __omelette_rail_enabled postMessage —
  // same listener handles those.)
  const [railEnabled, setRailEnabled] = React.useState(() => hasDeckStage && !!document.querySelector('deck-stage')?._railEnabled);
  React.useEffect(() => {
    if (!hasDeckStage || railEnabled) return undefined;
    const onMsg = e => {
      if (e.data && e.data.type === '__omelette_rail_enabled') setRailEnabled(true);
    };
    window.addEventListener('message', onMsg);
    return () => window.removeEventListener('message', onMsg);
  }, [hasDeckStage, railEnabled]);
  const [railVisible, setRailVisible] = React.useState(() => {
    try {
      return localStorage.getItem('deck-stage.railVisible') !== '0';
    } catch (e) {
      return true;
    }
  });
  const toggleRail = on => {
    setRailVisible(on);
    window.postMessage({
      type: '__deck_rail_visible',
      on
    }, '*');
  };
  const offsetRef = React.useRef({
    x: 16,
    y: 16
  });
  const PAD = 16;
  const clampToViewport = React.useCallback(() => {
    const panel = dragRef.current;
    if (!panel) return;
    const w = panel.offsetWidth,
      h = panel.offsetHeight;
    const maxRight = Math.max(PAD, window.innerWidth - w - PAD);
    const maxBottom = Math.max(PAD, window.innerHeight - h - PAD);
    offsetRef.current = {
      x: Math.min(maxRight, Math.max(PAD, offsetRef.current.x)),
      y: Math.min(maxBottom, Math.max(PAD, offsetRef.current.y))
    };
    panel.style.right = offsetRef.current.x + 'px';
    panel.style.bottom = offsetRef.current.y + 'px';
  }, []);
  React.useEffect(() => {
    if (!open) return;
    clampToViewport();
    if (typeof ResizeObserver === 'undefined') {
      window.addEventListener('resize', clampToViewport);
      return () => window.removeEventListener('resize', clampToViewport);
    }
    const ro = new ResizeObserver(clampToViewport);
    ro.observe(document.documentElement);
    return () => ro.disconnect();
  }, [open, clampToViewport]);
  React.useEffect(() => {
    const onMsg = e => {
      const t = e?.data?.type;
      if (t === '__activate_edit_mode') setOpen(true);else if (t === '__deactivate_edit_mode') setOpen(false);
    };
    window.addEventListener('message', onMsg);
    window.parent.postMessage({
      type: '__edit_mode_available'
    }, '*');
    return () => window.removeEventListener('message', onMsg);
  }, []);
  const dismiss = () => {
    setOpen(false);
    window.parent.postMessage({
      type: '__edit_mode_dismissed'
    }, '*');
  };
  const onDragStart = e => {
    const panel = dragRef.current;
    if (!panel) return;
    const r = panel.getBoundingClientRect();
    const sx = e.clientX,
      sy = e.clientY;
    const startRight = window.innerWidth - r.right;
    const startBottom = window.innerHeight - r.bottom;
    const move = ev => {
      offsetRef.current = {
        x: startRight - (ev.clientX - sx),
        y: startBottom - (ev.clientY - sy)
      };
      clampToViewport();
    };
    const up = () => {
      window.removeEventListener('mousemove', move);
      window.removeEventListener('mouseup', up);
    };
    window.addEventListener('mousemove', move);
    window.addEventListener('mouseup', up);
  };
  if (!open) return null;
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("style", null, __TWEAKS_STYLE), /*#__PURE__*/React.createElement("div", {
    ref: dragRef,
    className: "twk-panel",
    "data-noncommentable": "",
    style: {
      right: offsetRef.current.x,
      bottom: offsetRef.current.y
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "twk-hd",
    onMouseDown: onDragStart
  }, /*#__PURE__*/React.createElement("b", null, title), /*#__PURE__*/React.createElement("button", {
    className: "twk-x",
    "aria-label": "Close tweaks",
    onMouseDown: e => e.stopPropagation(),
    onClick: dismiss
  }, "\u2715")), /*#__PURE__*/React.createElement("div", {
    className: "twk-body"
  }, children, hasDeckStage && railEnabled && !noDeckControls && /*#__PURE__*/React.createElement(TweakSection, {
    label: "Deck"
  }, /*#__PURE__*/React.createElement(TweakToggle, {
    label: "Thumbnail rail",
    value: railVisible,
    onChange: toggleRail
  })))));
}

// ── Layout helpers ──────────────────────────────────────────────────────────

function TweakSection({
  label,
  children
}) {
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "twk-sect"
  }, label), children);
}
function TweakRow({
  label,
  value,
  children,
  inline = false
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: inline ? 'twk-row twk-row-h' : 'twk-row'
  }, /*#__PURE__*/React.createElement("div", {
    className: "twk-lbl"
  }, /*#__PURE__*/React.createElement("span", null, label), value != null && /*#__PURE__*/React.createElement("span", {
    className: "twk-val"
  }, value)), children);
}

// ── Controls ────────────────────────────────────────────────────────────────

function TweakSlider({
  label,
  value,
  min = 0,
  max = 100,
  step = 1,
  unit = '',
  onChange
}) {
  return /*#__PURE__*/React.createElement(TweakRow, {
    label: label,
    value: `${value}${unit}`
  }, /*#__PURE__*/React.createElement("input", {
    type: "range",
    className: "twk-slider",
    min: min,
    max: max,
    step: step,
    value: value,
    onChange: e => onChange(Number(e.target.value))
  }));
}
function TweakToggle({
  label,
  value,
  onChange
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: "twk-row twk-row-h"
  }, /*#__PURE__*/React.createElement("div", {
    className: "twk-lbl"
  }, /*#__PURE__*/React.createElement("span", null, label)), /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "twk-toggle",
    "data-on": value ? '1' : '0',
    role: "switch",
    "aria-checked": !!value,
    onClick: () => onChange(!value)
  }, /*#__PURE__*/React.createElement("i", null)));
}
function TweakRadio({
  label,
  value,
  options,
  onChange
}) {
  const trackRef = React.useRef(null);
  const [dragging, setDragging] = React.useState(false);
  // The active value is read by pointer-move handlers attached for the lifetime
  // of a drag — ref it so a stale closure doesn't fire onChange for every move.
  const valueRef = React.useRef(value);
  valueRef.current = value;

  // Segments wrap mid-word once per-segment width runs out. The track is
  // ~248px (280 panel − 28 body pad − 4 seg pad), each button loses 12px
  // to its own padding, and 11.5px system-ui averages ~6.3px/char — so 2
  // options fit ~16 chars each, 3 fit ~10. Past that (or >3 options), fall
  // back to a dropdown rather than wrap.
  const labelLen = o => String(typeof o === 'object' ? o.label : o).length;
  const maxLen = options.reduce((m, o) => Math.max(m, labelLen(o)), 0);
  const fitsAsSegments = maxLen <= ({
    2: 16,
    3: 10
  }[options.length] ?? 0);
  if (!fitsAsSegments) {
    // <select> emits strings — map back to the original option value so the
    // fallback stays type-preserving (numbers, booleans) like the segment path.
    const resolve = s => {
      const m = options.find(o => String(typeof o === 'object' ? o.value : o) === s);
      return m === undefined ? s : typeof m === 'object' ? m.value : m;
    };
    return /*#__PURE__*/React.createElement(TweakSelect, {
      label: label,
      value: value,
      options: options,
      onChange: s => onChange(resolve(s))
    });
  }
  const opts = options.map(o => typeof o === 'object' ? o : {
    value: o,
    label: o
  });
  const idx = Math.max(0, opts.findIndex(o => o.value === value));
  const n = opts.length;
  const segAt = clientX => {
    const r = trackRef.current.getBoundingClientRect();
    const inner = r.width - 4;
    const i = Math.floor((clientX - r.left - 2) / inner * n);
    return opts[Math.max(0, Math.min(n - 1, i))].value;
  };
  const onPointerDown = e => {
    setDragging(true);
    const v0 = segAt(e.clientX);
    if (v0 !== valueRef.current) onChange(v0);
    const move = ev => {
      if (!trackRef.current) return;
      const v = segAt(ev.clientX);
      if (v !== valueRef.current) onChange(v);
    };
    const up = () => {
      setDragging(false);
      window.removeEventListener('pointermove', move);
      window.removeEventListener('pointerup', up);
    };
    window.addEventListener('pointermove', move);
    window.addEventListener('pointerup', up);
  };
  return /*#__PURE__*/React.createElement(TweakRow, {
    label: label
  }, /*#__PURE__*/React.createElement("div", {
    ref: trackRef,
    role: "radiogroup",
    onPointerDown: onPointerDown,
    className: dragging ? 'twk-seg dragging' : 'twk-seg'
  }, /*#__PURE__*/React.createElement("div", {
    className: "twk-seg-thumb",
    style: {
      left: `calc(2px + ${idx} * (100% - 4px) / ${n})`,
      width: `calc((100% - 4px) / ${n})`
    }
  }), opts.map(o => /*#__PURE__*/React.createElement("button", {
    key: o.value,
    type: "button",
    role: "radio",
    "aria-checked": o.value === value
  }, o.label))));
}
function TweakSelect({
  label,
  value,
  options,
  onChange
}) {
  return /*#__PURE__*/React.createElement(TweakRow, {
    label: label
  }, /*#__PURE__*/React.createElement("select", {
    className: "twk-field",
    value: value,
    onChange: e => onChange(e.target.value)
  }, options.map(o => {
    const v = typeof o === 'object' ? o.value : o;
    const l = typeof o === 'object' ? o.label : o;
    return /*#__PURE__*/React.createElement("option", {
      key: v,
      value: v
    }, l);
  })));
}
function TweakText({
  label,
  value,
  placeholder,
  onChange
}) {
  return /*#__PURE__*/React.createElement(TweakRow, {
    label: label
  }, /*#__PURE__*/React.createElement("input", {
    className: "twk-field",
    type: "text",
    value: value,
    placeholder: placeholder,
    onChange: e => onChange(e.target.value)
  }));
}
function TweakNumber({
  label,
  value,
  min,
  max,
  step = 1,
  unit = '',
  onChange
}) {
  const clamp = n => {
    if (min != null && n < min) return min;
    if (max != null && n > max) return max;
    return n;
  };
  const startRef = React.useRef({
    x: 0,
    val: 0
  });
  const onScrubStart = e => {
    e.preventDefault();
    startRef.current = {
      x: e.clientX,
      val: value
    };
    const decimals = (String(step).split('.')[1] || '').length;
    const move = ev => {
      const dx = ev.clientX - startRef.current.x;
      const raw = startRef.current.val + dx * step;
      const snapped = Math.round(raw / step) * step;
      onChange(clamp(Number(snapped.toFixed(decimals))));
    };
    const up = () => {
      window.removeEventListener('pointermove', move);
      window.removeEventListener('pointerup', up);
    };
    window.addEventListener('pointermove', move);
    window.addEventListener('pointerup', up);
  };
  return /*#__PURE__*/React.createElement("div", {
    className: "twk-num"
  }, /*#__PURE__*/React.createElement("span", {
    className: "twk-num-lbl",
    onPointerDown: onScrubStart
  }, label), /*#__PURE__*/React.createElement("input", {
    type: "number",
    value: value,
    min: min,
    max: max,
    step: step,
    onChange: e => onChange(clamp(Number(e.target.value)))
  }), unit && /*#__PURE__*/React.createElement("span", {
    className: "twk-num-unit"
  }, unit));
}

// Relative-luminance contrast pick — checkmarks drawn over a swatch need to
// read on both #111 and #fafafa without per-option configuration. Hex input
// only (#rgb / #rrggbb); named or rgb()/hsl() colors fall through to "light".
function __twkIsLight(hex) {
  const h = String(hex).replace('#', '');
  const x = h.length === 3 ? h.replace(/./g, c => c + c) : h.padEnd(6, '0');
  const n = parseInt(x.slice(0, 6), 16);
  if (Number.isNaN(n)) return true;
  const r = n >> 16 & 255,
    g = n >> 8 & 255,
    b = n & 255;
  return r * 299 + g * 587 + b * 114 > 148000;
}
const __TwkCheck = ({
  light
}) => /*#__PURE__*/React.createElement("svg", {
  viewBox: "0 0 14 14",
  "aria-hidden": "true"
}, /*#__PURE__*/React.createElement("path", {
  d: "M3 7.2 5.8 10 11 4.2",
  fill: "none",
  strokeWidth: "2.2",
  strokeLinecap: "round",
  strokeLinejoin: "round",
  stroke: light ? 'rgba(0,0,0,.78)' : '#fff'
}));

// TweakColor — curated color/palette picker. Each option is either a single
// hex string or an array of 1-5 hex strings; the card adapts — a lone color
// renders solid, a palette renders colors[0] as the hero (left ~2/3) with the
// rest stacked in a sharp column on the right. onChange emits the
// option in the shape it was passed (string stays string, array stays array).
// Without options it falls back to the native color input for back-compat.
function TweakColor({
  label,
  value,
  options,
  onChange
}) {
  if (!options || !options.length) {
    return /*#__PURE__*/React.createElement("div", {
      className: "twk-row twk-row-h"
    }, /*#__PURE__*/React.createElement("div", {
      className: "twk-lbl"
    }, /*#__PURE__*/React.createElement("span", null, label)), /*#__PURE__*/React.createElement("input", {
      type: "color",
      className: "twk-swatch",
      value: value,
      onChange: e => onChange(e.target.value)
    }));
  }
  // Native <input type=color> emits lowercase hex per the HTML spec, so
  // compare case-insensitively. String() guards JSON.stringify(undefined),
  // which returns the primitive undefined (no .toLowerCase).
  const key = o => String(JSON.stringify(o)).toLowerCase();
  const cur = key(value);
  return /*#__PURE__*/React.createElement(TweakRow, {
    label: label
  }, /*#__PURE__*/React.createElement("div", {
    className: "twk-chips",
    role: "radiogroup"
  }, options.map((o, i) => {
    const colors = Array.isArray(o) ? o : [o];
    const [hero, ...rest] = colors;
    const sup = rest.slice(0, 4);
    const on = key(o) === cur;
    return /*#__PURE__*/React.createElement("button", {
      key: i,
      type: "button",
      className: "twk-chip",
      role: "radio",
      "aria-checked": on,
      "data-on": on ? '1' : '0',
      "aria-label": colors.join(', '),
      title: colors.join(' · '),
      style: {
        background: hero
      },
      onClick: () => onChange(o)
    }, sup.length > 0 && /*#__PURE__*/React.createElement("span", null, sup.map((c, j) => /*#__PURE__*/React.createElement("i", {
      key: j,
      style: {
        background: c
      }
    }))), on && /*#__PURE__*/React.createElement(__TwkCheck, {
      light: __twkIsLight(hero)
    }));
  })));
}
function TweakButton({
  label,
  onClick,
  secondary = false
}) {
  return /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: secondary ? 'twk-btn secondary' : 'twk-btn',
    onClick: onClick
  }, label);
}
Object.assign(window, {
  useTweaks,
  TweaksPanel,
  TweakSection,
  TweakRow,
  TweakSlider,
  TweakToggle,
  TweakRadio,
  TweakSelect,
  TweakText,
  TweakNumber,
  TweakColor,
  TweakButton
});

// ===== src/icons.jsx =====
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
// Hand-crafted minimal line icons. 1.5px stroke, 16px nominal.
const Icon = ({
  d,
  size = 16,
  stroke = 1.5,
  fill = "none",
  style,
  ...rest
}) => /*#__PURE__*/React.createElement("svg", _extends({
  width: size,
  height: size,
  viewBox: "0 0 16 16",
  fill: fill,
  stroke: "currentColor",
  strokeWidth: stroke,
  strokeLinecap: "round",
  strokeLinejoin: "round",
  style: style
}, rest), typeof d === "string" ? /*#__PURE__*/React.createElement("path", {
  d: d
}) : d);
const Icons = {
  Home: p => /*#__PURE__*/React.createElement(Icon, _extends({}, p, {
    d: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M2.5 7l5.5-4.5L13.5 7v6.5h-3v-4h-4v4h-3z"
    }))
  })),
  Repo: p => /*#__PURE__*/React.createElement(Icon, _extends({}, p, {
    d: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M3 2.5h8.5a1.5 1.5 0 0 1 1.5 1.5v9.5"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M3 2.5v9a1 1 0 0 0 1 1h9"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M5.5 5.5h4"
    }))
  })),
  PR: p => /*#__PURE__*/React.createElement(Icon, _extends({}, p, {
    d: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("circle", {
      cx: "4",
      cy: "4",
      r: "1.5"
    }), /*#__PURE__*/React.createElement("circle", {
      cx: "4",
      cy: "12",
      r: "1.5"
    }), /*#__PURE__*/React.createElement("circle", {
      cx: "12",
      cy: "12",
      r: "1.5"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M4 5.5v5"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M10.5 12H8a3.5 3.5 0 0 1-3.5-3.5V5.5"
    }))
  })),
  Issue: p => /*#__PURE__*/React.createElement(Icon, _extends({}, p, {
    d: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("circle", {
      cx: "8",
      cy: "8",
      r: "5.5"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M8 5.5v3M8 10.4v.1"
    }))
  })),
  Search: p => /*#__PURE__*/React.createElement(Icon, _extends({}, p, {
    d: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("circle", {
      cx: "7",
      cy: "7",
      r: "4.5"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M10.5 10.5l3 3"
    }))
  })),
  Settings: p => /*#__PURE__*/React.createElement(Icon, _extends({}, p, {
    d: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("circle", {
      cx: "8",
      cy: "8",
      r: "2"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M8 1.5v1.8M8 12.7v1.8M14.5 8h-1.8M3.3 8H1.5M12.6 3.4l-1.3 1.3M4.7 11.3l-1.3 1.3M12.6 12.6l-1.3-1.3M4.7 4.7L3.4 3.4"
    }))
  })),
  Key: p => /*#__PURE__*/React.createElement(Icon, _extends({}, p, {
    d: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("circle", {
      cx: "5.5",
      cy: "8",
      r: "3"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M8 8h6M11 8v2M13 8v3"
    }))
  })),
  Webhook: p => /*#__PURE__*/React.createElement(Icon, _extends({}, p, {
    d: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("circle", {
      cx: "5",
      cy: "5",
      r: "2"
    }), /*#__PURE__*/React.createElement("circle", {
      cx: "11",
      cy: "11",
      r: "2"
    }), /*#__PURE__*/React.createElement("circle", {
      cx: "11",
      cy: "5",
      r: "2"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M6.5 6.5L9.5 9.5"
    }))
  })),
  Plus: p => /*#__PURE__*/React.createElement(Icon, _extends({}, p, {
    d: "M8 3v10M3 8h10"
  })),
  Check: p => /*#__PURE__*/React.createElement(Icon, _extends({}, p, {
    d: "M3 8.5l3 3 7-7"
  })),
  X: p => /*#__PURE__*/React.createElement(Icon, _extends({}, p, {
    d: "M3.5 3.5l9 9M12.5 3.5l-9 9"
  })),
  Chevron: p => /*#__PURE__*/React.createElement(Icon, _extends({}, p, {
    d: "M6 4l4 4-4 4"
  })),
  ChevronDown: p => /*#__PURE__*/React.createElement(Icon, _extends({}, p, {
    d: "M4 6l4 4 4-4"
  })),
  Folder: p => /*#__PURE__*/React.createElement(Icon, _extends({}, p, {
    d: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M2 4.5a1 1 0 0 1 1-1h3l1.5 1.5H13a1 1 0 0 1 1 1V12a1 1 0 0 1-1 1H3a1 1 0 0 1-1-1z"
    }))
  })),
  File: p => /*#__PURE__*/React.createElement(Icon, _extends({}, p, {
    d: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M3.5 2h6L13 5.5V13a1 1 0 0 1-1 1H3.5a1 1 0 0 1-1-1V3a1 1 0 0 1 1-1z"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M9 2v3.5h4"
    }))
  })),
  Branch: p => /*#__PURE__*/React.createElement(Icon, _extends({}, p, {
    d: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("circle", {
      cx: "4",
      cy: "3.5",
      r: "1.5"
    }), /*#__PURE__*/React.createElement("circle", {
      cx: "4",
      cy: "12.5",
      r: "1.5"
    }), /*#__PURE__*/React.createElement("circle", {
      cx: "12",
      cy: "3.5",
      r: "1.5"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M4 5v6M12 5v2a3 3 0 0 1-3 3H4"
    }))
  })),
  Commit: p => /*#__PURE__*/React.createElement(Icon, _extends({}, p, {
    d: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("circle", {
      cx: "8",
      cy: "8",
      r: "2.5"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M1.5 8H5.5M10.5 8H14.5"
    }))
  })),
  Star: p => /*#__PURE__*/React.createElement(Icon, _extends({}, p, {
    d: "M8 2l1.8 3.8 4.2.6-3 3 .8 4.2L8 11.6l-3.8 2L5 9.4l-3-3 4.2-.6z"
  })),
  Eye: p => /*#__PURE__*/React.createElement(Icon, _extends({}, p, {
    d: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M1.5 8s2.5-4.5 6.5-4.5S14.5 8 14.5 8 12 12.5 8 12.5 1.5 8 1.5 8z"
    }), /*#__PURE__*/React.createElement("circle", {
      cx: "8",
      cy: "8",
      r: "1.7"
    }))
  })),
  Fork: p => /*#__PURE__*/React.createElement(Icon, _extends({}, p, {
    d: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("circle", {
      cx: "4",
      cy: "3",
      r: "1.5"
    }), /*#__PURE__*/React.createElement("circle", {
      cx: "12",
      cy: "3",
      r: "1.5"
    }), /*#__PURE__*/React.createElement("circle", {
      cx: "8",
      cy: "13",
      r: "1.5"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M4 4.5v2a2 2 0 0 0 2 2h4a2 2 0 0 0 2-2v-2M8 8.5v3"
    }))
  })),
  Logo: p => /*#__PURE__*/React.createElement(Icon, _extends({}, p, {
    d: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M3 8a5 5 0 0 1 10 0"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M3 8a5 5 0 0 0 10 0"
    }), /*#__PURE__*/React.createElement("circle", {
      cx: "8",
      cy: "8",
      r: "1.5",
      fill: "currentColor",
      stroke: "none"
    }))
  })),
  Bolt: p => /*#__PURE__*/React.createElement(Icon, _extends({}, p, {
    d: "M9 1.5L3 9h4l-1 5.5L13 6H9z"
  })),
  Sun: p => /*#__PURE__*/React.createElement(Icon, _extends({}, p, {
    d: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("circle", {
      cx: "8",
      cy: "8",
      r: "3"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M8 1v1.5M8 13.5V15M1 8h1.5M13.5 8H15M3 3l1 1M12 12l1 1M3 13l1-1M12 4l1-1"
    }))
  })),
  Moon: p => /*#__PURE__*/React.createElement(Icon, _extends({}, p, {
    d: "M13 9a5 5 0 0 1-6.5-6 5.5 5.5 0 1 0 6.5 6z"
  })),
  Copy: p => /*#__PURE__*/React.createElement(Icon, _extends({}, p, {
    d: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("rect", {
      x: "5",
      y: "5",
      width: "9",
      height: "9",
      rx: "1.5"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M11 5V3.5A1.5 1.5 0 0 0 9.5 2H3.5A1.5 1.5 0 0 0 2 3.5v6A1.5 1.5 0 0 0 3.5 11H5"
    }))
  })),
  Split: p => /*#__PURE__*/React.createElement(Icon, _extends({}, p, {
    d: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("rect", {
      x: "1.5",
      y: "2.5",
      width: "13",
      height: "11",
      rx: "1.5"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M8 2.5v11"
    }))
  })),
  Close: p => /*#__PURE__*/React.createElement(Icon, _extends({}, p, {
    d: "M4 4l8 8M12 4l-8 8"
  })),
  Code: p => /*#__PURE__*/React.createElement(Icon, _extends({}, p, {
    d: "M5 5L2 8l3 3M11 5l3 3-3 3M9.5 3l-3 10"
  })),
  Book: p => /*#__PURE__*/React.createElement(Icon, _extends({}, p, {
    d: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M2.5 3.5A1 1 0 0 1 3.5 2.5H8v11H3.5a1 1 0 0 1-1-1z"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M13.5 3.5a1 1 0 0 0-1-1H8v11h4.5a1 1 0 0 0 1-1z"
    }))
  })),
  Activity: p => /*#__PURE__*/React.createElement(Icon, _extends({}, p, {
    d: "M1.5 8h2.5l2-5 4 10 2-5h2.5"
  })),
  Bell: p => /*#__PURE__*/React.createElement(Icon, _extends({}, p, {
    d: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M3.5 11.5L4 10V7a4 4 0 0 1 8 0v3l.5 1.5z"
    }), /*#__PURE__*/React.createElement("path", {
      d: "M6.5 13a1.5 1.5 0 0 0 3 0"
    }))
  })),
  Filter: p => /*#__PURE__*/React.createElement(Icon, _extends({}, p, {
    d: "M2 3.5h12l-4.5 6V14L6.5 12V9.5z"
  })),
  Diff: p => /*#__PURE__*/React.createElement(Icon, _extends({}, p, {
    d: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M4 2v10M2 4h4M11 4v8M9 12h4"
    }))
  })),
  Pin: p => /*#__PURE__*/React.createElement(Icon, _extends({}, p, {
    d: "M10 2l4 4-1.5 1.5-1-1L8 10.5l-2 .5-1 3 3-1 .5-2L11.5 7l-1-1z"
  })),
  Tag: p => /*#__PURE__*/React.createElement(Icon, _extends({}, p, {
    d: /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("path", {
      d: "M2 2.5h5L14 9.5a1.5 1.5 0 0 1 0 2L11.5 14a1.5 1.5 0 0 1-2 0L2.5 7z"
    }), /*#__PURE__*/React.createElement("circle", {
      cx: "5",
      cy: "5",
      r: "0.8",
      fill: "currentColor"
    }))
  }))
};
window.Icon = Icon;
window.Icons = Icons;

// ===== src/data.jsx =====
// Mock data — kept small and realistic.

const USERS = {
  me: {
    id: "me",
    name: "Avery Park",
    handle: "avery",
    color: "oklch(60% 0.14 200)",
    initials: "AP"
  },
  jana: {
    id: "jana",
    name: "Jana Okafor",
    handle: "jana",
    color: "oklch(60% 0.14 30)",
    initials: "JO"
  },
  sam: {
    id: "sam",
    name: "Sam Riedl",
    handle: "sam",
    color: "oklch(60% 0.14 280)",
    initials: "SR"
  },
  noor: {
    id: "noor",
    name: "Noor Vance",
    handle: "noor",
    color: "oklch(60% 0.14 140)",
    initials: "NV"
  },
  lin: {
    id: "lin",
    name: "Lin Park-Soto",
    handle: "lin",
    color: "oklch(60% 0.14 70)",
    initials: "LP"
  },
  bot: {
    id: "bot",
    name: "ci-bot",
    handle: "ci-bot",
    color: "oklch(55% 0.04 250)",
    initials: "CI"
  }
};
const ORGS = [{
  id: "personal",
  name: "avery",
  kind: "user"
}, {
  id: "kelp",
  name: "kelp",
  kind: "org"
}, {
  id: "open-strata",
  name: "open-strata",
  kind: "org"
}];
const REPOS = [{
  id: "kelp/atlas",
  name: "atlas",
  org: "kelp",
  description: "Edge-deployed analytics router. Receives, batches, ships.",
  language: "Rust",
  languageColor: "oklch(60% 0.14 30)",
  stars: 1284,
  forks: 96,
  watchers: 24,
  visibility: "private",
  defaultBranch: "main",
  updated: "12 min ago",
  pinned: true
}, {
  id: "kelp/atlas-ui",
  name: "atlas-ui",
  org: "kelp",
  description: "Web console for atlas. React + Vite.",
  language: "TypeScript",
  languageColor: "oklch(62% 0.12 240)",
  stars: 312,
  forks: 18,
  watchers: 9,
  visibility: "private",
  defaultBranch: "main",
  updated: "2 h ago",
  pinned: true
}, {
  id: "kelp/ingest-spec",
  name: "ingest-spec",
  org: "kelp",
  description: "Versioned wire spec for atlas ingest.",
  language: "Markdown",
  languageColor: "oklch(60% 0.06 250)",
  stars: 41,
  forks: 4,
  watchers: 3,
  visibility: "private",
  defaultBranch: "main",
  updated: "yesterday",
  pinned: false
}, {
  id: "open-strata/cordon",
  name: "cordon",
  org: "open-strata",
  description: "Tiny structured-log library with zero deps.",
  language: "Go",
  languageColor: "oklch(62% 0.12 200)",
  stars: 8421,
  forks: 412,
  watchers: 88,
  visibility: "public",
  defaultBranch: "main",
  updated: "3 d ago",
  pinned: true
}, {
  id: "avery/dotfiles",
  name: "dotfiles",
  org: "avery",
  description: "Configs, scripts, and the ever-growing zshrc.",
  language: "Shell",
  languageColor: "oklch(70% 0.10 110)",
  stars: 12,
  forks: 0,
  watchers: 1,
  visibility: "public",
  defaultBranch: "main",
  updated: "1 w ago",
  pinned: false
}];

// File tree for kelp/atlas
const FILE_TREE = [{
  type: "dir",
  name: ".github",
  path: ".github",
  children: [{
    type: "dir",
    name: "workflows",
    path: ".github/workflows",
    children: [{
      type: "file",
      name: "ci.yml",
      path: ".github/workflows/ci.yml",
      lang: "yaml"
    }, {
      type: "file",
      name: "release.yml",
      path: ".github/workflows/release.yml",
      lang: "yaml"
    }]
  }]
}, {
  type: "dir",
  name: "crates",
  path: "crates",
  expanded: true,
  children: [{
    type: "dir",
    name: "atlas-core",
    path: "crates/atlas-core",
    expanded: true,
    children: [{
      type: "dir",
      name: "src",
      path: "crates/atlas-core/src",
      expanded: true,
      children: [{
        type: "file",
        name: "lib.rs",
        path: "crates/atlas-core/src/lib.rs",
        lang: "rust"
      }, {
        type: "file",
        name: "router.rs",
        path: "crates/atlas-core/src/router.rs",
        lang: "rust",
        active: true
      }, {
        type: "file",
        name: "batch.rs",
        path: "crates/atlas-core/src/batch.rs",
        lang: "rust"
      }, {
        type: "file",
        name: "ship.rs",
        path: "crates/atlas-core/src/ship.rs",
        lang: "rust"
      }]
    }, {
      type: "file",
      name: "Cargo.toml",
      path: "crates/atlas-core/Cargo.toml",
      lang: "toml"
    }]
  }, {
    type: "dir",
    name: "atlas-cli",
    path: "crates/atlas-cli",
    children: [{
      type: "file",
      name: "main.rs",
      path: "crates/atlas-cli/src/main.rs",
      lang: "rust"
    }]
  }]
}, {
  type: "dir",
  name: "docs",
  path: "docs",
  children: [{
    type: "file",
    name: "architecture.md",
    path: "docs/architecture.md",
    lang: "markdown"
  }, {
    type: "file",
    name: "deploy.md",
    path: "docs/deploy.md",
    lang: "markdown"
  }]
}, {
  type: "file",
  name: ".gitignore",
  path: ".gitignore",
  lang: "text"
}, {
  type: "file",
  name: "Cargo.toml",
  path: "Cargo.toml",
  lang: "toml"
}, {
  type: "file",
  name: "Cargo.lock",
  path: "Cargo.lock",
  lang: "toml"
}, {
  type: "file",
  name: "LICENSE",
  path: "LICENSE",
  lang: "text"
}, {
  type: "file",
  name: "README.md",
  path: "README.md",
  lang: "markdown"
}];

// Code sample — router.rs
const ROUTER_RS = `use std::sync::Arc;
use tokio::sync::mpsc;
use tracing::{debug, info, warn};

use crate::batch::{Batch, BatchSink};
use crate::ship::Shipper;

/// Routes incoming events to the right batch sink based on the
/// declared destination in the event envelope.
///
/// Routing is intentionally simple: a static map keyed by the
/// destination id, populated at startup. Dynamic reconfig happens
/// out-of-band by swapping the whole router.
pub struct Router {
    sinks: Arc<dashmap::DashMap<String, BatchSink>>,
    fallback: Option<BatchSink>,
}

impl Router {
    pub fn new(fallback: Option<BatchSink>) -> Self {
        Self {
            sinks: Arc::new(dashmap::DashMap::new()),
            fallback,
        }
    }

    pub fn register(&self, dest: impl Into<String>, sink: BatchSink) {
        let dest = dest.into();
        info!(destination = %dest, "registering sink");
        self.sinks.insert(dest, sink);
    }

    pub async fn route(&self, env: Envelope) -> Result<(), RouteError> {
        let dest = env.destination();
        match self.sinks.get(dest) {
            Some(sink) => sink.send(env).await.map_err(Into::into),
            None => match &self.fallback {
                Some(fb) => {
                    warn!(destination = %dest, "no sink, using fallback");
                    fb.send(env).await.map_err(Into::into)
                }
                None => Err(RouteError::UnknownDestination(dest.to_string())),
            },
        }
    }
}
`;
const README_MD = `# atlas

> Edge-deployed analytics router. Receives, batches, ships.

Atlas is the routing brain behind the kelp analytics pipeline.
It runs at the edge, accepts events over HTTP or gRPC, batches
them by destination, and ships to downstream sinks with backpressure.

## Quickstart

\`\`\`sh
cargo install atlas-cli
atlas serve --config ./atlas.toml
\`\`\`

## Why another router?

- **Tiny** — single binary, ~12MB stripped.
- **Backpressure-aware** — no silent drops, no unbounded queues.
- **Observable by default** — every hop emits a structured span.

See [docs/architecture.md](docs/architecture.md) for the long version.
`;

// Pull requests
const PRS = [{
  id: 842,
  title: "router: switch fallback sink to bounded channel",
  repo: "kelp/atlas",
  author: USERS.jana,
  branch: "jana/bounded-fallback",
  base: "main",
  status: "open",
  reviewers: [USERS.me, USERS.sam],
  checks: {
    passed: 6,
    failed: 0,
    pending: 1
  },
  additions: 142,
  deletions: 88,
  commits: 4,
  files: 6,
  updated: "8 min ago",
  description: "Replaces the unbounded `tokio::mpsc::unbounded_channel` in the fallback path with a bounded one (size: 4096). Backpressure now reaches the caller instead of silently growing memory.",
  labels: [{
    name: "backend",
    color: "info"
  }, {
    name: "ready for review",
    color: "accent"
  }],
  comments: 7
}, {
  id: 839,
  title: "ui: tweak diff colors for high-contrast mode",
  repo: "kelp/atlas-ui",
  author: USERS.sam,
  branch: "sam/diff-contrast",
  base: "main",
  status: "open",
  reviewers: [USERS.me],
  checks: {
    passed: 4,
    failed: 0,
    pending: 0
  },
  additions: 24,
  deletions: 12,
  commits: 2,
  files: 3,
  updated: "1 h ago",
  description: "Bumps the diff highlight delta so green/red are legible against the high-contrast theme.",
  labels: [{
    name: "ui",
    color: "purple"
  }],
  comments: 2
}, {
  id: 837,
  title: "ingest-spec: bump version to 0.6, add `trace_id` field",
  repo: "kelp/ingest-spec",
  author: USERS.noor,
  branch: "noor/spec-0.6",
  base: "main",
  status: "review",
  reviewers: [USERS.me, USERS.jana, USERS.lin],
  checks: {
    passed: 2,
    failed: 0,
    pending: 0
  },
  additions: 312,
  deletions: 41,
  commits: 9,
  files: 12,
  updated: "yesterday",
  description: "Spec bump: adds optional `trace_id` to the envelope. Backwards compatible.",
  labels: [{
    name: "spec",
    color: "info"
  }, {
    name: "breaking?",
    color: "warn"
  }],
  comments: 14
}];

// Diff for PR #842
const PR_DIFF_FILES = [{
  path: "crates/atlas-core/src/router.rs",
  additions: 18,
  deletions: 22,
  hunks: [{
    header: "@@ -12,7 +12,7 @@ pub struct Router {",
    lines: [{
      type: "ctx",
      num: ["12", "12"],
      text: "/// out-of-band by swapping the whole router."
    }, {
      type: "ctx",
      num: ["13", "13"],
      text: "pub struct Router {"
    }, {
      type: "ctx",
      num: ["14", "14"],
      text: "    sinks: Arc<dashmap::DashMap<String, BatchSink>>,"
    }, {
      type: "del",
      num: ["15", " "],
      text: "    fallback: Option<mpsc::UnboundedSender<Envelope>>,"
    }, {
      type: "add",
      num: [" ", "15"],
      text: "    fallback: Option<BatchSink>,"
    }, {
      type: "ctx",
      num: ["16", "16"],
      text: "}"
    }, {
      type: "ctx",
      num: ["17", "17"],
      text: ""
    }]
  }, {
    header: "@@ -34,9 +34,13 @@ impl Router {",
    lines: [{
      type: "ctx",
      num: ["34", "34"],
      text: "    pub async fn route(&self, env: Envelope) -> Result<(), RouteError> {"
    }, {
      type: "ctx",
      num: ["35", "35"],
      text: "        let dest = env.destination();"
    }, {
      type: "ctx",
      num: ["36", "36"],
      text: "        match self.sinks.get(dest) {"
    }, {
      type: "del",
      num: ["37", " "],
      text: "            Some(sink) => sink.send(env).await.map_err(Into::into),"
    }, {
      type: "add",
      num: [" ", "37"],
      text: "            Some(sink) => sink.send(env).await.map_err(Into::into),"
    }, {
      type: "ctx",
      num: ["38", "38"],
      text: "            None => match &self.fallback {"
    }, {
      type: "del",
      num: ["39", " "],
      text: "                Some(fb) => fb.send(env).map_err(|_| RouteError::FallbackClosed),"
    }, {
      type: "add",
      num: [" ", "39"],
      text: "                Some(fb) => {"
    }, {
      type: "add",
      num: [" ", "40"],
      text: "                    warn!(destination = %dest, \"no sink, using fallback\");"
    }, {
      type: "add",
      num: [" ", "41"],
      text: "                    fb.send(env).await.map_err(Into::into)"
    }, {
      type: "add",
      num: [" ", "42"],
      text: "                }"
    }, {
      type: "ctx",
      num: ["40", "43"],
      text: "                None => Err(RouteError::UnknownDestination(dest.to_string())),"
    }, {
      type: "ctx",
      num: ["41", "44"],
      text: "            },"
    }, {
      type: "ctx",
      num: ["42", "45"],
      text: "        }"
    }]
  }],
  comments: [{
    line: 41,
    side: "right",
    author: USERS.me,
    when: "3 min ago",
    body: "Could we surface this as a metric too? `atlas_fallback_total{destination=...}` would help us catch misconfigured pipelines.",
    suggestion: null
  }]
}, {
  path: "crates/atlas-core/src/batch.rs",
  additions: 64,
  deletions: 34,
  hunks: [{
    header: "@@ -1,8 +1,12 @@",
    lines: [{
      type: "del",
      num: ["1", " "],
      text: "use tokio::sync::mpsc::{unbounded_channel, UnboundedSender};"
    }, {
      type: "add",
      num: [" ", "1"],
      text: "use tokio::sync::mpsc::{channel, Sender};"
    }, {
      type: "ctx",
      num: ["2", "2"],
      text: "use tracing::debug;"
    }, {
      type: "ctx",
      num: ["3", "3"],
      text: ""
    }, {
      type: "add",
      num: [" ", "4"],
      text: "/// Default backpressure depth — events block once this many"
    }, {
      type: "add",
      num: [" ", "5"],
      text: "/// envelopes are queued for a single destination."
    }, {
      type: "add",
      num: [" ", "6"],
      text: "const DEFAULT_DEPTH: usize = 4096;"
    }]
  }],
  comments: []
}, {
  path: "crates/atlas-core/src/ship.rs",
  additions: 12,
  deletions: 8,
  hunks: [],
  comments: []
}];

// Activity feed
const ACTIVITY = [{
  id: 1,
  kind: "pr_review_requested",
  actor: USERS.jana,
  target: "kelp/atlas#842",
  title: "router: switch fallback sink to bounded channel",
  when: "8 min ago"
}, {
  id: 2,
  kind: "pr_approved",
  actor: USERS.lin,
  target: "kelp/atlas-ui#839",
  title: "ui: tweak diff colors for high-contrast mode",
  when: "1 h ago"
}, {
  id: 3,
  kind: "commit",
  actor: USERS.me,
  target: "kelp/atlas / main",
  title: "docs: clarify shipping retries",
  when: "3 h ago"
}, {
  id: 4,
  kind: "issue_assigned",
  actor: USERS.noor,
  target: "kelp/atlas#911",
  title: "Memory growth under fallback-only routing",
  when: "yesterday"
}, {
  id: 5,
  kind: "deploy",
  actor: USERS.bot,
  target: "kelp/atlas / production",
  title: "v0.14.2 shipped",
  when: "yesterday"
}];

// Personal access tokens
const TOKENS = [{
  id: "tok_1",
  name: "macbook — atlas dev",
  scopes: ["repo:read", "repo:write", "actions:read"],
  created: "2 weeks ago",
  lastUsed: "8 min ago",
  expires: "in 87 days"
}, {
  id: "tok_2",
  name: "ci pipeline",
  scopes: ["repo:read", "packages:write"],
  created: "3 months ago",
  lastUsed: "2 h ago",
  expires: "in 14 days",
  expiringSoon: true
}, {
  id: "tok_3",
  name: "old laptop",
  scopes: ["repo:read"],
  created: "1 year ago",
  lastUsed: "4 months ago",
  expires: "expired",
  expired: true
}];
const SSH_KEYS = [{
  id: "ssh_1",
  name: "macbook-air",
  fingerprint: "SHA256:k9d2…aZqL",
  added: "2 weeks ago",
  lastUsed: "today"
}, {
  id: "ssh_2",
  name: "linux-tower",
  fingerprint: "SHA256:p3xN…7gFq",
  added: "8 months ago",
  lastUsed: "3 d ago"
}];
const WEBHOOKS = [{
  id: "wh_1",
  url: "https://hooks.kelp.dev/ci",
  events: ["push", "pull_request"],
  repo: "kelp/atlas",
  lastDelivery: "8 min ago",
  status: "ok"
}, {
  id: "wh_2",
  url: "https://internal.kelp.dev/deploy",
  events: ["release"],
  repo: "kelp/atlas",
  lastDelivery: "yesterday",
  status: "ok"
}, {
  id: "wh_3",
  url: "https://hooks.kelp.dev/ci",
  events: ["push"],
  repo: "kelp/atlas-ui",
  lastDelivery: "1 h ago",
  status: "warn"
}];
Object.assign(window, {
  USERS,
  ORGS,
  REPOS,
  FILE_TREE,
  ROUTER_RS,
  README_MD,
  PRS,
  PR_DIFF_FILES,
  ACTIVITY,
  TOKENS,
  SSH_KEYS,
  WEBHOOKS
});

// --- Real API wiring -------------------------------------------------------
// Small fetch helper + loaders that replace mock data in place. When the
// backend is unreachable (e.g. opening the prototype from disk), the mock
// constants above remain as the fallback.
const OrchisAPI = {
  async get(path) {
    const r = await fetch(path, {
      credentials: "same-origin"
    });
    if (!r.ok) throw new Error(path + " -> " + r.status);
    return r.json();
  },
  async post(path, body) {
    const r = await fetch(path, {
      method: "POST",
      credentials: "same-origin",
      headers: {
        "Content-Type": "application/json"
      },
      body: JSON.stringify(body || {})
    });
    if (!r.ok) throw new Error(path + " -> " + r.status);
    if (r.status === 204 || r.headers.get("content-length") === "0") return true;
    return r.json();
  },
  async patch(path, body) {
    const r = await fetch(path, {
      method: "PATCH",
      credentials: "same-origin",
      headers: {
        "Content-Type": "application/json"
      },
      body: JSON.stringify(body || {})
    });
    if (!r.ok) throw new Error(path + " -> " + r.status);
    return r.json();
  },
  async put(path, body) {
    const r = await fetch(path, {
      method: "PUT",
      credentials: "same-origin",
      headers: {
        "Content-Type": "application/json"
      },
      body: JSON.stringify(body || {})
    });
    if (!r.ok) throw new Error(path + " -> " + r.status);
    return r.json();
  },
  async del(path) {
    const r = await fetch(path, {
      method: "DELETE",
      credentials: "same-origin"
    });
    if (!r.ok && r.status !== 204) throw new Error(path + " -> " + r.status);
    return true;
  }
};

// Replace REPOS contents in place with the live repo list.
async function loadRepos() {
  const repos = await OrchisAPI.get("/v1/repos");
  REPOS.length = 0;
  (repos || []).forEach(r => REPOS.push(r));
  return REPOS;
}
window.OrchisAPI = OrchisAPI;
window.loadRepos = loadRepos;

// ===== src/palette.jsx =====
// Command palette — the 3-step shortcut for everything.
function Palette({
  open,
  onClose,
  onNavigate
}) {
  const [q, setQ] = React.useState("");
  const [idx, setIdx] = React.useState(0);
  const inputRef = React.useRef(null);
  React.useEffect(() => {
    if (open) {
      setTimeout(() => inputRef.current?.focus(), 10);
      setQ("");
      setIdx(0);
    }
  }, [open]);

  // Live search results from the server (repos / pulls / files).
  const [hits, setHits] = React.useState({
    repos: [],
    pulls: [],
    files: []
  });

  // Static actions — quick actions + navigation. These need local navigation
  // callbacks, so they're built client-side (the server supplies only data).
  const staticActions = React.useMemo(() => {
    const acts = [];
    // Quick actions (the killers — 1 step to PAT/SSH/webhook)
    acts.push({
      id: "act-pat",
      group: "Quick action",
      icon: /*#__PURE__*/React.createElement(Icons.Key, null),
      label: "Create personal access token",
      hint: "PAT",
      kbd: "⌘T",
      run: () => onNavigate({
        view: "settings",
        tab: "tokens",
        newToken: true
      })
    });
    acts.push({
      id: "act-ssh",
      group: "Quick action",
      icon: /*#__PURE__*/React.createElement(Icons.Key, null),
      label: "Add SSH key",
      hint: "SSH",
      run: () => onNavigate({
        view: "settings",
        tab: "ssh",
        newKey: true
      })
    });
    acts.push({
      id: "act-wh",
      group: "Quick action",
      icon: /*#__PURE__*/React.createElement(Icons.Webhook, null),
      label: "Add webhook",
      run: () => onNavigate({
        view: "settings",
        tab: "webhooks",
        newHook: true
      })
    });
    acts.push({
      id: "act-new-repo",
      group: "Quick action",
      icon: /*#__PURE__*/React.createElement(Icons.Plus, null),
      label: "New repository",
      kbd: "⌘N",
      run: () => onNavigate({
        view: "home",
        newRepo: true
      })
    });

    // Navigation
    acts.push({
      id: "nav-home",
      group: "Go to",
      icon: /*#__PURE__*/React.createElement(Icons.Home, null),
      label: "Home",
      run: () => onNavigate({
        view: "home"
      })
    });
    acts.push({
      id: "nav-prs",
      group: "Go to",
      icon: /*#__PURE__*/React.createElement(Icons.PR, null),
      label: "My pull requests",
      run: () => onNavigate({
        view: "prs"
      })
    });
    acts.push({
      id: "nav-dev",
      group: "Go to",
      icon: /*#__PURE__*/React.createElement(Icons.Settings, null),
      label: "Developer settings",
      run: () => onNavigate({
        view: "settings"
      })
    });
    acts.push({
      id: "nav-tokens",
      group: "Go to",
      icon: /*#__PURE__*/React.createElement(Icons.Key, null),
      label: "Tokens",
      run: () => onNavigate({
        view: "settings",
        tab: "tokens"
      })
    });
    acts.push({
      id: "nav-ssh",
      group: "Go to",
      icon: /*#__PURE__*/React.createElement(Icons.Key, null),
      label: "SSH keys",
      run: () => onNavigate({
        view: "settings",
        tab: "ssh"
      })
    });
    acts.push({
      id: "nav-wh",
      group: "Go to",
      icon: /*#__PURE__*/React.createElement(Icons.Webhook, null),
      label: "Webhooks",
      run: () => onNavigate({
        view: "settings",
        tab: "webhooks"
      })
    });
    return acts;
  }, [onNavigate]);

  // Fetch live hits (debounced) whenever the query changes while open.
  React.useEffect(() => {
    if (!open) return;
    let cancelled = false;
    const t = setTimeout(async () => {
      try {
        const res = await window.OrchisAPI.get("/v1/search/palette?q=" + encodeURIComponent(q));
        if (!cancelled) setHits({
          repos: res.repos || [],
          pulls: res.pulls || [],
          files: res.files || []
        });
      } catch (_) {
        if (!cancelled) setHits({
          repos: [],
          pulls: [],
          files: []
        });
      }
    }, q.trim() ? 140 : 0);
    return () => {
      cancelled = true;
      clearTimeout(t);
    };
  }, [q, open]);

  // Server hits mapped into action rows (run = navigate to the route).
  const serverActions = React.useMemo(() => {
    const acts = [];
    (hits.repos || []).forEach(r => {
      acts.push({
        id: "repo-" + r.id,
        group: "Repositories",
        icon: /*#__PURE__*/React.createElement(Icons.Repo, null),
        label: r.label,
        sub: r.sub,
        run: () => onNavigate(r.route)
      });
    });
    (hits.pulls || []).forEach(p => {
      acts.push({
        id: "pr-" + p.id,
        group: "Pull requests",
        icon: /*#__PURE__*/React.createElement(Icons.PR, null),
        label: p.label,
        sub: p.sub,
        run: () => onNavigate(p.route)
      });
    });
    (hits.files || []).forEach(f => {
      acts.push({
        id: "file-" + f.id,
        group: "Files",
        icon: /*#__PURE__*/React.createElement(Icons.File, null),
        label: f.label,
        sub: f.sub,
        run: () => onNavigate(f.route)
      });
    });
    return acts;
  }, [hits, onNavigate]);

  // Filter the static actions client-side; server actions are already filtered.
  const filtered = React.useMemo(() => {
    let statics;
    if (!q.trim()) {
      statics = staticActions;
    } else {
      const ql = q.toLowerCase();
      statics = staticActions.map(a => {
        const hay = (a.label + " " + (a.sub || "") + " " + (a.hint || "") + " " + a.group).toLowerCase();
        if (!hay.includes(ql)) return null;
        const score = a.label.toLowerCase().startsWith(ql) ? 0 : a.label.toLowerCase().includes(ql) ? 1 : 2;
        return {
          ...a,
          _score: score
        };
      }).filter(Boolean).sort((a, b) => a._score - b._score);
    }
    // When there's a query, always offer a "search code" escape hatch that
    // opens the full code-search view pre-filled with the query.
    const codeAction = q.trim() ? [{
      id: "act-code-search",
      group: "Quick action",
      icon: /*#__PURE__*/React.createElement(Icons.Search, null),
      label: "Search code for “" + q.trim() + "”",
      kbd: "↵",
      run: () => onNavigate({
        view: "search",
        q: q.trim()
      })
    }] : [];
    return [...codeAction, ...statics, ...serverActions].slice(0, 40);
  }, [q, staticActions, serverActions, onNavigate]);

  // Group rendering
  const groups = React.useMemo(() => {
    const out = [];
    const seen = new Map();
    filtered.forEach(a => {
      if (!seen.has(a.group)) {
        seen.set(a.group, []);
        out.push({
          name: a.group,
          items: seen.get(a.group)
        });
      }
      seen.get(a.group).push(a);
    });
    return out;
  }, [filtered]);

  // Keyboard nav
  React.useEffect(() => {
    if (!open) return;
    const handler = e => {
      if (e.key === "Escape") {
        e.preventDefault();
        onClose();
      } else if (e.key === "ArrowDown") {
        e.preventDefault();
        setIdx(i => Math.min(i + 1, filtered.length - 1));
      } else if (e.key === "ArrowUp") {
        e.preventDefault();
        setIdx(i => Math.max(i - 1, 0));
      } else if (e.key === "Enter") {
        e.preventDefault();
        const a = filtered[idx];
        if (a) {
          a.run();
          onClose();
        }
      }
    };
    window.addEventListener("keydown", handler);
    return () => window.removeEventListener("keydown", handler);
  }, [open, filtered, idx, onClose]);
  if (!open) return null;
  let runningIdx = -1;
  return /*#__PURE__*/React.createElement("div", {
    style: palStyles.scrim,
    onClick: onClose
  }, /*#__PURE__*/React.createElement("div", {
    style: palStyles.modal,
    onClick: e => e.stopPropagation(),
    className: "fade-in"
  }, /*#__PURE__*/React.createElement("div", {
    style: palStyles.inputRow
  }, /*#__PURE__*/React.createElement(Icons.Search, {
    size: 16,
    style: {
      color: "var(--fg-2)"
    }
  }), /*#__PURE__*/React.createElement("input", {
    ref: inputRef,
    value: q,
    onChange: e => {
      setQ(e.target.value);
      setIdx(0);
    },
    placeholder: "Type to do anything \u2014 create a token, open a repo, find a file\u2026",
    style: palStyles.input
  }), /*#__PURE__*/React.createElement("span", {
    className: "kbd"
  }, "esc")), /*#__PURE__*/React.createElement("div", {
    style: palStyles.results
  }, groups.length === 0 ? /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 24,
      textAlign: "center",
      color: "var(--fg-3)",
      fontSize: 13
    }
  }, "No matches. Try \u201Ctoken\u201D, a repo name, or \u201Cwebhook\u201D.") : groups.map(g => /*#__PURE__*/React.createElement("div", {
    key: g.name
  }, /*#__PURE__*/React.createElement("div", {
    style: palStyles.group
  }, g.name), g.items.map(a => {
    runningIdx++;
    const active = runningIdx === idx;
    return /*#__PURE__*/React.createElement("button", {
      key: a.id,
      onMouseEnter: () => setIdx(filtered.findIndex(x => x.id === a.id)),
      onClick: () => {
        a.run();
        onClose();
      },
      style: {
        ...palStyles.row,
        ...(active ? palStyles.rowActive : {})
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        color: active ? "var(--accent)" : "var(--fg-2)",
        display: "inline-flex"
      }
    }, a.icon), /*#__PURE__*/React.createElement("span", {
      style: {
        flex: 1,
        textAlign: "left",
        overflow: "hidden",
        textOverflow: "ellipsis",
        whiteSpace: "nowrap"
      }
    }, a.label, a.sub ? /*#__PURE__*/React.createElement("span", {
      className: "subtle",
      style: {
        marginLeft: 8,
        fontFamily: "var(--font-mono)",
        fontSize: 11
      }
    }, a.sub) : null), a.kbd ? /*#__PURE__*/React.createElement("span", {
      className: "kbd"
    }, a.kbd) : null);
  })))), /*#__PURE__*/React.createElement("div", {
    style: palStyles.footer
  }, /*#__PURE__*/React.createElement("span", {
    className: "row",
    style: {
      gap: 6
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "kbd"
  }, "\u2191"), /*#__PURE__*/React.createElement("span", {
    className: "kbd"
  }, "\u2193"), /*#__PURE__*/React.createElement("span", {
    className: "subtle"
  }, "navigate")), /*#__PURE__*/React.createElement("span", {
    className: "row",
    style: {
      gap: 6
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "kbd"
  }, "\u21B5"), /*#__PURE__*/React.createElement("span", {
    className: "subtle"
  }, "select")), /*#__PURE__*/React.createElement("div", {
    className: "spacer"
  }), /*#__PURE__*/React.createElement("span", {
    className: "subtle",
    style: {
      fontSize: 11
    }
  }, "Anything reachable in \u2264 3 steps."))));
}
const palStyles = {
  scrim: {
    position: "fixed",
    inset: 0,
    background: "color-mix(in oklab, var(--bg) 60%, rgba(0,0,0,0.4))",
    backdropFilter: "blur(2px)",
    zIndex: 1000,
    display: "flex",
    alignItems: "flex-start",
    justifyContent: "center",
    paddingTop: "12vh"
  },
  modal: {
    width: "min(640px, 92vw)",
    background: "var(--bg-1)",
    border: "1px solid var(--line-strong)",
    borderRadius: 12,
    boxShadow: "var(--shadow-lg)",
    overflow: "hidden",
    display: "flex",
    flexDirection: "column",
    maxHeight: "70vh"
  },
  inputRow: {
    display: "flex",
    alignItems: "center",
    gap: 10,
    padding: "12px 14px",
    borderBottom: "1px solid var(--line)"
  },
  input: {
    flex: 1,
    border: "none",
    outline: "none",
    background: "transparent",
    color: "var(--fg)",
    font: "inherit",
    fontSize: 15
  },
  results: {
    overflowY: "auto",
    padding: "6px 6px 8px",
    flex: 1
  },
  group: {
    fontSize: 10.5,
    fontWeight: 500,
    color: "var(--fg-3)",
    textTransform: "uppercase",
    letterSpacing: "0.07em",
    padding: "10px 12px 4px"
  },
  row: {
    display: "flex",
    alignItems: "center",
    gap: 10,
    width: "100%",
    height: 34,
    padding: "0 10px",
    borderRadius: 6,
    border: "none",
    background: "transparent",
    color: "var(--fg)",
    fontSize: 13,
    cursor: "pointer",
    font: "inherit"
  },
  rowActive: {
    background: "var(--bg-2)"
  },
  footer: {
    display: "flex",
    alignItems: "center",
    gap: 14,
    padding: "8px 14px",
    borderTop: "1px solid var(--line)",
    fontSize: 11,
    color: "var(--fg-2)",
    background: "var(--bg)"
  }
};
window.Palette = Palette;

// ===== src/sidebar.jsx =====
// Sidebar — slim, vertical, with quick switch + pinned repos.
function Sidebar({
  route,
  setRoute,
  openPalette,
  openTokens,
  theme,
  setTheme
}) {
  const navItems = [{
    id: "home",
    label: "Home",
    icon: /*#__PURE__*/React.createElement(Icons.Home, null)
  }, {
    id: "prs",
    label: "Pull requests",
    icon: /*#__PURE__*/React.createElement(Icons.PR, null),
    badge: 3
  }, {
    id: "search",
    label: "Search",
    icon: /*#__PURE__*/React.createElement(Icons.Search, null)
  }, {
    id: "settings",
    label: "Developer",
    icon: /*#__PURE__*/React.createElement(Icons.Key, null)
  }];
  const [pinnedState, setPinnedState] = React.useState(null);
  React.useEffect(() => {
    const load = () => {
      if (window.OrchisAPI) window.OrchisAPI.get("/v1/me/pinned").then(setPinnedState).catch(() => setPinnedState([]));
    };
    load();
    window.addEventListener("orchis:repos-changed", load);
    return () => window.removeEventListener("orchis:repos-changed", load);
  }, []);
  const pinned = pinnedState != null ? pinnedState : window.OrchisAPI ? [] : REPOS.filter(r => r.pinned);
  return /*#__PURE__*/React.createElement("aside", {
    style: sbStyles.aside
  }, /*#__PURE__*/React.createElement("div", {
    style: sbStyles.brand
  }, /*#__PURE__*/React.createElement("div", {
    style: sbStyles.brandMark
  }, /*#__PURE__*/React.createElement(Icons.Logo, {
    size: 18
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      lineHeight: 1.1
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontWeight: 600,
      fontSize: 14
    }
  }, "Orchis"), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 10.5,
      color: "var(--fg-3)"
    }
  }, "avery / kelp")), /*#__PURE__*/React.createElement("div", {
    className: "spacer"
  }), /*#__PURE__*/React.createElement(NotificationsBell, {
    setRoute: setRoute
  }), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost icon",
    onClick: () => setTheme(theme === "dark" ? "light" : "dark"),
    title: "Toggle theme"
  }, theme === "dark" ? /*#__PURE__*/React.createElement(Icons.Sun, null) : /*#__PURE__*/React.createElement(Icons.Moon, null))), /*#__PURE__*/React.createElement("div", {
    style: sbStyles.searchWrap
  }, /*#__PURE__*/React.createElement("button", {
    style: sbStyles.searchBtn,
    onClick: openPalette
  }, /*#__PURE__*/React.createElement(Icons.Search, {
    size: 14
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--fg-3)",
      flex: 1,
      textAlign: "left"
    }
  }, "Jump to anything\u2026"), /*#__PURE__*/React.createElement("span", {
    className: "kbd"
  }, "\u2318K"))), /*#__PURE__*/React.createElement("nav", {
    style: sbStyles.nav
  }, navItems.map(item => /*#__PURE__*/React.createElement("button", {
    key: item.id,
    onClick: () => setRoute({
      view: item.id
    }),
    style: {
      ...sbStyles.navItem,
      ...(route.view === item.id ? sbStyles.navItemActive : {})
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 18,
      display: "inline-flex",
      color: route.view === item.id ? "var(--accent)" : "var(--fg-2)"
    }
  }, item.icon), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      textAlign: "left"
    }
  }, item.label), item.badge ? /*#__PURE__*/React.createElement("span", {
    className: "chip accent",
    style: {
      height: 18,
      fontSize: 10.5
    }
  }, item.badge) : null, item.kbd ? /*#__PURE__*/React.createElement("span", {
    className: "kbd"
  }, item.kbd) : null))), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "16px 12px 6px"
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "section-title"
  }, "Pinned")), /*#__PURE__*/React.createElement("div", {
    style: sbStyles.pinned
  }, pinned.map(r => /*#__PURE__*/React.createElement("button", {
    key: r.id,
    onClick: () => setRoute({
      view: "repo",
      repo: r.id
    }),
    style: {
      ...sbStyles.navItem,
      ...(route.view === "repo" && route.repo === r.id ? sbStyles.navItemActive : {}),
      paddingLeft: 12
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 8,
      height: 8,
      borderRadius: 2,
      background: r.languageColor,
      flexShrink: 0
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      textAlign: "left",
      fontFamily: "var(--font-mono)",
      fontSize: 12.5,
      overflow: "hidden",
      textOverflow: "ellipsis",
      whiteSpace: "nowrap"
    }
  }, r.org, "/", r.name), r.visibility === "private" ? /*#__PURE__*/React.createElement("span", {
    className: "subtle",
    style: {
      fontSize: 10
    }
  }, "private") : null))), /*#__PURE__*/React.createElement("div", {
    className: "spacer"
  }), /*#__PURE__*/React.createElement("div", {
    style: sbStyles.footer
  }, /*#__PURE__*/React.createElement("button", {
    style: sbStyles.user,
    onClick: () => setRoute({
      view: "settings"
    })
  }, /*#__PURE__*/React.createElement("span", {
    className: "avatar",
    style: {
      background: USERS.me.color
    }
  }, USERS.me.initials), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      lineHeight: 1.15,
      textAlign: "left",
      flex: 1,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12.5,
      fontWeight: 500
    }
  }, USERS.me.name), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 10.5,
      color: "var(--fg-3)"
    }
  }, "@", USERS.me.handle)), /*#__PURE__*/React.createElement(Icons.Chevron, {
    size: 12,
    style: {
      color: "var(--fg-3)"
    }
  }))));
}

// NotificationsBell — global inbox affordance. Shows an unread badge, opens a
// dropdown of inbox items, and refreshes live via the SSE inbox:me topic.
// Unread is tracked client-side (localStorage seen-keys) — no backend change.
function NotificationsBell({
  setRoute
}) {
  const [items, setItems] = React.useState([]);
  const [open, setOpen] = React.useState(false);
  const [seen, setSeen] = React.useState(() => {
    try {
      return new Set(JSON.parse(localStorage.getItem("orchis-inbox-seen") || "[]"));
    } catch (e) {
      return new Set();
    }
  });
  const itemKey = it => [it.title, it.repo || "", it.detail || ""].join("|");
  const persistSeen = s => {
    try {
      localStorage.setItem("orchis-inbox-seen", JSON.stringify([...s]));
    } catch (e) {}
  };
  const load = React.useCallback(() => {
    if (window.OrchisAPI) window.OrchisAPI.get("/v1/me/inbox").then(x => setItems(x || [])).catch(() => setItems([]));
  }, []);
  React.useEffect(() => {
    load();
  }, [load]);
  React.useEffect(() => {
    if (!window.EventSource) return;
    const es = new EventSource("/v1/stream?topics=inbox:me");
    es.addEventListener("inbox.new", load);
    return () => es.close();
  }, [load]);
  const unread = items.filter(it => !seen.has(itemKey(it))).length;
  const toggle = () => {
    const next = !open;
    setOpen(next);
    if (next && items.length) {
      const s = new Set(seen);
      items.forEach(it => s.add(itemKey(it)));
      setSeen(s);
      persistSeen(s);
    }
  };
  const go = it => {
    setOpen(false);
    if (it.route) setRoute(it.route);
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: "relative"
    }
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost icon",
    onClick: toggle,
    title: "Notifications",
    style: {
      position: "relative"
    }
  }, /*#__PURE__*/React.createElement(Icons.Bell, null), unread > 0 ? /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      top: 3,
      right: 3,
      minWidth: 14,
      height: 14,
      padding: "0 3px",
      borderRadius: 999,
      background: "var(--accent)",
      color: "var(--accent-fg)",
      fontSize: 9,
      fontWeight: 600,
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      lineHeight: 1
    }
  }, unread > 9 ? "9+" : unread) : null), open ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    onClick: () => setOpen(false),
    style: {
      position: "fixed",
      inset: 0,
      zIndex: 90
    }
  }), /*#__PURE__*/React.createElement("div", {
    className: "card fade-in",
    style: {
      position: "absolute",
      top: "calc(100% + 6px)",
      right: 0,
      width: 320,
      maxHeight: 420,
      overflowY: "auto",
      zIndex: 91,
      boxShadow: "var(--shadow-lg)",
      padding: 6
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      padding: "6px 8px 8px"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12.5,
      fontWeight: 600
    }
  }, "Notifications"), /*#__PURE__*/React.createElement("span", {
    className: "spacer"
  }), /*#__PURE__*/React.createElement("span", {
    className: "subtle",
    style: {
      fontSize: 11
    }
  }, items.length, " item", items.length === 1 ? "" : "s")), items.length === 0 ? /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "16px 10px",
      color: "var(--fg-3)",
      fontSize: 12.5,
      textAlign: "center"
    }
  }, "You're all caught up.") : items.map((it, i) => /*#__PURE__*/React.createElement("button", {
    key: i,
    onClick: () => go(it),
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 2,
      width: "100%",
      textAlign: "left",
      padding: "9px 10px",
      borderRadius: 6,
      background: "transparent",
      border: "none",
      cursor: "pointer",
      font: "inherit",
      color: "inherit"
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 6
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 6,
      height: 6,
      borderRadius: 999,
      background: it.kind === "warn" ? "var(--warn)" : "var(--accent)",
      flexShrink: 0
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12.5,
      fontWeight: 500
    }
  }, it.title), /*#__PURE__*/React.createElement("span", {
    className: "spacer"
  }), /*#__PURE__*/React.createElement("span", {
    className: "mono subtle",
    style: {
      fontSize: 10.5
    }
  }, it.repo)), /*#__PURE__*/React.createElement("div", {
    className: "subtle",
    style: {
      fontSize: 11.5,
      paddingLeft: 12,
      overflow: "hidden",
      textOverflow: "ellipsis",
      whiteSpace: "nowrap"
    }
  }, it.detail))))) : null);
}
const sbStyles = {
  aside: {
    width: 240,
    flexShrink: 0,
    height: "100%",
    display: "flex",
    flexDirection: "column",
    borderRight: "1px solid var(--line)",
    background: "var(--bg-1)"
  },
  brand: {
    display: "flex",
    alignItems: "center",
    gap: 10,
    padding: "12px 12px 10px",
    borderBottom: "1px solid var(--line)"
  },
  brandMark: {
    width: 28,
    height: 28,
    borderRadius: 7,
    background: "var(--accent)",
    color: "var(--accent-fg)",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    flexShrink: 0
  },
  searchWrap: {
    padding: "10px 10px 6px"
  },
  searchBtn: {
    display: "flex",
    alignItems: "center",
    gap: 8,
    width: "100%",
    height: 30,
    padding: "0 10px",
    background: "var(--bg-2)",
    border: "1px solid var(--line)",
    borderRadius: 6,
    color: "var(--fg-2)",
    cursor: "pointer",
    font: "inherit",
    fontSize: 12.5
  },
  nav: {
    display: "flex",
    flexDirection: "column",
    gap: 2,
    padding: "4px 8px"
  },
  navItem: {
    display: "flex",
    alignItems: "center",
    gap: 10,
    height: 30,
    padding: "0 10px",
    borderRadius: 6,
    border: "1px solid transparent",
    background: "transparent",
    cursor: "pointer",
    color: "var(--fg-1)",
    fontSize: 13,
    font: "inherit",
    fontWeight: 450
  },
  navItemActive: {
    background: "var(--bg-2)",
    color: "var(--fg)",
    borderColor: "var(--line)"
  },
  pinned: {
    display: "flex",
    flexDirection: "column",
    gap: 1,
    padding: "0 8px",
    overflowY: "auto"
  },
  footer: {
    borderTop: "1px solid var(--line)",
    padding: 8
  },
  user: {
    display: "flex",
    alignItems: "center",
    gap: 10,
    width: "100%",
    padding: "6px 8px",
    background: "transparent",
    border: "1px solid transparent",
    borderRadius: 8,
    cursor: "pointer",
    font: "inherit"
  }
};
window.Sidebar = Sidebar;

// ===== src/views/dashboard.jsx =====
// Dashboard / home view.
function inboxIcon(name) {
  if (name === "pr") return /*#__PURE__*/React.createElement(Icons.PR, null);
  if (name === "key") return /*#__PURE__*/React.createElement(Icons.Key, null);
  if (name === "alert") return /*#__PURE__*/React.createElement(Icons.Issue, null);
  return /*#__PURE__*/React.createElement(Icons.Issue, null);
}
function DashboardView({
  setRoute,
  openPalette,
  route
}) {
  const today = new Date().toLocaleDateString(undefined, {
    weekday: "long",
    month: "long",
    day: "numeric"
  });
  const firstName = (USERS.me.name || USERS.me.handle || "there").split(" ")[0];
  const [showNewRepo, setShowNewRepo] = React.useState(!!(route && route.newRepo));
  const [inbox, setInbox] = React.useState(null);
  const [activity, setActivity] = React.useState(null);
  const loadInbox = React.useCallback(() => {
    if (window.OrchisAPI) window.OrchisAPI.get("/v1/me/inbox").then(setInbox).catch(() => setInbox([]));
  }, []);
  React.useEffect(() => {
    if (window.OrchisAPI) {
      loadInbox();
      window.OrchisAPI.get("/v1/me/activity").then(setActivity).catch(() => setActivity([]));
    }
  }, [loadInbox]);

  // Realtime: refresh the inbox live when something needs your attention
  // (e.g. a review request). Subscribes to your own inbox topic via SSE.
  React.useEffect(() => {
    if (!window.EventSource) return;
    const es = new EventSource("/v1/stream?topics=inbox:me");
    es.addEventListener("inbox.new", loadInbox);
    return () => es.close();
  }, [loadInbox]);
  const inboxItems = inbox != null ? inbox : [];
  const activityItems = activity != null ? activity : window.OrchisAPI ? [] : ACTIVITY;
  return /*#__PURE__*/React.createElement("div", {
    style: dashStyles.scroll
  }, /*#__PURE__*/React.createElement("div", {
    style: dashStyles.page,
    className: "fade-in"
  }, /*#__PURE__*/React.createElement("header", {
    style: dashStyles.header
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 11.5,
      color: "var(--fg-3)",
      textTransform: "uppercase",
      letterSpacing: "0.06em"
    }
  }, today), /*#__PURE__*/React.createElement("h1", {
    style: dashStyles.h1
  }, greeting(), ", ", firstName, "."), /*#__PURE__*/React.createElement("div", {
    style: {
      color: "var(--fg-2)",
      fontSize: 14
    }
  }, "Welcome to Orchis Foundry.")), /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn",
    onClick: openPalette
  }, /*#__PURE__*/React.createElement(Icons.Search, {
    size: 14
  }), " Quick action ", /*#__PURE__*/React.createElement("span", {
    className: "kbd",
    style: {
      marginLeft: 4
    }
  }, "\u2318K")), /*#__PURE__*/React.createElement("button", {
    className: "btn primary",
    onClick: () => setShowNewRepo(true)
  }, /*#__PURE__*/React.createElement(Icons.Plus, {
    size: 14
  }), " New repository"))), showNewRepo ? /*#__PURE__*/React.createElement(NewRepoModal, {
    onClose: () => setShowNewRepo(false),
    setRoute: setRoute
  }) : null, /*#__PURE__*/React.createElement("section", {
    style: dashStyles.section
  }, /*#__PURE__*/React.createElement("div", {
    style: dashStyles.sectionHead
  }, /*#__PURE__*/React.createElement("h2", {
    style: dashStyles.h2
  }, "Inbox"), /*#__PURE__*/React.createElement("span", {
    className: "muted",
    style: {
      fontSize: 12
    }
  }, "Filtered to what actually needs you.")), inboxItems.length === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      padding: "20px 18px",
      color: "var(--fg-3)",
      fontSize: 13
    }
  }, "Nothing needs your attention right now.") : /*#__PURE__*/React.createElement("div", {
    style: dashStyles.inbox
  }, inboxItems.map((it, i) => /*#__PURE__*/React.createElement(InboxCard, {
    key: i,
    icon: inboxIcon(it.icon),
    kind: it.kind,
    title: it.title,
    repo: it.repo,
    detail: it.detail,
    meta: it.meta || [],
    cta: it.cta,
    onClick: () => it.route && setRoute(it.route)
  })))), /*#__PURE__*/React.createElement("div", {
    style: dashStyles.cols
  }, /*#__PURE__*/React.createElement("section", {
    style: dashStyles.section
  }, /*#__PURE__*/React.createElement("div", {
    style: dashStyles.sectionHead
  }, /*#__PURE__*/React.createElement("h2", {
    style: dashStyles.h2
  }, "Pinned"), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm"
  }, "Manage")), /*#__PURE__*/React.createElement("div", {
    style: dashStyles.repoGrid
  }, REPOS.filter(r => r.pinned).map(r => /*#__PURE__*/React.createElement("button", {
    key: r.id,
    className: "card",
    style: dashStyles.repoCard,
    onClick: () => setRoute({
      view: "repo",
      repo: r.id
    })
  }, /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 8
    }
  }, /*#__PURE__*/React.createElement(Icons.Repo, {
    size: 14,
    style: {
      color: "var(--fg-2)"
    }
  }), /*#__PURE__*/React.createElement("span", {
    className: "mono",
    style: {
      fontSize: 13,
      fontWeight: 500
    }
  }, r.org, "/", r.name), r.visibility === "private" ? /*#__PURE__*/React.createElement("span", {
    className: "chip"
  }, "private") : null), /*#__PURE__*/React.createElement("p", {
    style: {
      color: "var(--fg-2)",
      fontSize: 13,
      margin: "8px 0 12px",
      lineHeight: 1.4
    }
  }, r.description), /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 14,
      color: "var(--fg-2)",
      fontSize: 11.5
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "row",
    style: {
      gap: 5
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 8,
      height: 8,
      borderRadius: 2,
      background: r.languageColor
    }
  }), " ", r.language), /*#__PURE__*/React.createElement("span", {
    className: "row",
    style: {
      gap: 4
    }
  }, /*#__PURE__*/React.createElement(Icons.Star, {
    size: 12
  }), " ", r.stars.toLocaleString()), /*#__PURE__*/React.createElement("span", {
    className: "row",
    style: {
      gap: 4
    }
  }, /*#__PURE__*/React.createElement(Icons.Fork, {
    size: 12
  }), " ", r.forks), /*#__PURE__*/React.createElement("span", {
    className: "spacer"
  }), /*#__PURE__*/React.createElement("span", {
    className: "subtle"
  }, r.updated)))))), /*#__PURE__*/React.createElement("section", {
    style: dashStyles.section
  }, /*#__PURE__*/React.createElement("div", {
    style: dashStyles.sectionHead
  }, /*#__PURE__*/React.createElement("h2", {
    style: dashStyles.h2
  }, "Activity"), /*#__PURE__*/React.createElement("span", {
    className: "muted",
    style: {
      fontSize: 12
    }
  }, "Across all your repos")), activityItems.length === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      padding: "20px 18px",
      color: "var(--fg-3)",
      fontSize: 13
    }
  }, "No activity yet.") : /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      padding: 4
    }
  }, activityItems.map(a => /*#__PURE__*/React.createElement("div", {
    key: a.id,
    style: dashStyles.activityRow
  }, /*#__PURE__*/React.createElement("span", {
    className: "avatar",
    style: {
      background: a.actor.color,
      width: 20,
      height: 20,
      fontSize: 9
    }
  }, a.actor.initials), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 13,
      lineHeight: 1.4,
      overflow: "hidden",
      textOverflow: "ellipsis",
      whiteSpace: "nowrap"
    }
  }, /*#__PURE__*/React.createElement("strong", {
    style: {
      fontWeight: 500
    }
  }, a.actor.name), " ", /*#__PURE__*/React.createElement("span", {
    className: "muted"
  }, activityVerb(a.kind)), " ", /*#__PURE__*/React.createElement("span", {
    className: "mono",
    style: {
      fontSize: 11.5
    }
  }, a.target)), /*#__PURE__*/React.createElement("div", {
    className: "subtle",
    style: {
      fontSize: 11.5,
      overflow: "hidden",
      textOverflow: "ellipsis",
      whiteSpace: "nowrap"
    }
  }, a.title)), /*#__PURE__*/React.createElement("span", {
    className: "subtle",
    style: {
      fontSize: 11
    }
  }, a.when))))))));
}
function activityVerb(k) {
  switch (k) {
    case "repo_created":
      return "created";
    case "pr_opened":
      return "opened";
    case "pr_merged":
      return "merged";
    case "pr_reviewed":
      return "reviewed";
    case "pr_commented":
      return "commented on";
    case "issue_opened":
      return "opened issue";
    case "issue_closed":
      return "closed issue";
    case "scan_flagged":
      return "flagged";
    // legacy mock kinds
    case "pr_review_requested":
      return "requested your review on";
    case "pr_approved":
      return "approved";
    case "commit":
      return "pushed to";
    case "issue_assigned":
      return "assigned you to";
    case "deploy":
      return "deployed";
    default:
      return "updated";
  }
}
function InboxCard({
  icon,
  kind = "info",
  title,
  repo,
  detail,
  meta = [],
  cta,
  onClick
}) {
  const tone = {
    accent: {
      bg: "var(--accent-soft)",
      fg: "var(--accent)",
      border: "var(--accent-line)"
    },
    warn: {
      bg: "color-mix(in oklab, var(--warn) 14%, var(--bg))",
      fg: "var(--warn)",
      border: "color-mix(in oklab, var(--warn) 35%, var(--bg))"
    },
    info: {
      bg: "color-mix(in oklab, var(--info) 14%, var(--bg))",
      fg: "var(--info)",
      border: "color-mix(in oklab, var(--info) 35%, var(--bg))"
    }
  }[kind];
  return /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: dashStyles.inboxCard
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      ...dashStyles.inboxIcon,
      background: tone.bg,
      color: tone.fg,
      borderColor: tone.border
    }
  }, icon), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 8,
      marginBottom: 4
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontWeight: 500,
      fontSize: 13
    }
  }, title), /*#__PURE__*/React.createElement("span", {
    className: "mono subtle",
    style: {
      fontSize: 11.5
    }
  }, repo)), /*#__PURE__*/React.createElement("div", {
    style: {
      color: "var(--fg-1)",
      fontSize: 13,
      marginBottom: 6,
      lineHeight: 1.4
    }
  }, detail), /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 12,
      color: "var(--fg-2)",
      fontSize: 11.5,
      flexWrap: "wrap"
    }
  }, meta.map((m, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    className: "row",
    style: {
      gap: 5
    }
  }, m.avatar ? /*#__PURE__*/React.createElement("span", {
    className: "avatar",
    style: {
      background: m.avatar.color,
      width: 16,
      height: 16,
      fontSize: 8
    }
  }, m.avatar.initials) : null, /*#__PURE__*/React.createElement("span", {
    className: m.mono ? "mono" : ""
  }, m.label))))), /*#__PURE__*/React.createElement("button", {
    className: "btn",
    onClick: onClick
  }, cta, " ", /*#__PURE__*/React.createElement(Icons.Chevron, {
    size: 12
  })));
}
const dashStyles = {
  scroll: {
    overflowY: "auto",
    height: "100%"
  },
  page: {
    maxWidth: 1180,
    margin: "0 auto",
    padding: "36px 36px 80px"
  },
  header: {
    display: "flex",
    alignItems: "flex-end",
    justifyContent: "space-between",
    marginBottom: 28,
    gap: 16,
    flexWrap: "wrap"
  },
  h1: {
    fontSize: 30,
    fontWeight: 500,
    letterSpacing: "-0.02em",
    margin: "6px 0 2px"
  },
  section: {
    marginBottom: 28
  },
  sectionHead: {
    display: "flex",
    alignItems: "baseline",
    justifyContent: "space-between",
    marginBottom: 10,
    gap: 12
  },
  h2: {
    fontSize: 13,
    fontWeight: 500,
    color: "var(--fg-3)",
    textTransform: "uppercase",
    letterSpacing: "0.07em",
    margin: 0
  },
  inbox: {
    display: "flex",
    flexDirection: "column",
    gap: 8
  },
  inboxCard: {
    display: "flex",
    alignItems: "center",
    gap: 14,
    padding: "14px 16px"
  },
  inboxIcon: {
    width: 32,
    height: 32,
    borderRadius: 8,
    border: "1px solid",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    flexShrink: 0
  },
  cols: {
    display: "grid",
    gridTemplateColumns: "1fr 1fr",
    gap: 24
  },
  repoGrid: {
    display: "grid",
    gridTemplateColumns: "1fr",
    gap: 8
  },
  repoCard: {
    padding: "14px 16px",
    textAlign: "left",
    cursor: "pointer",
    transition: "border-color 100ms, background 100ms",
    font: "inherit",
    color: "inherit"
  },
  activityRow: {
    display: "flex",
    alignItems: "center",
    gap: 10,
    padding: "10px 12px",
    borderBottom: "1px solid var(--line)",
    fontSize: 13
  }
};
function greeting() {
  const h = new Date().getHours();
  if (h < 12) return "Good morning";
  if (h < 18) return "Good afternoon";
  return "Good evening";
}

// New repository modal — POSTs /v1/repos and navigates to the new repo.
function NewRepoModal({
  onClose,
  setRoute
}) {
  const [name, setName] = React.useState("");
  const [description, setDescription] = React.useState("");
  const [visibility, setVisibility] = React.useState("private");
  const [busy, setBusy] = React.useState(false);
  const [err, setErr] = React.useState("");
  const create = async () => {
    setErr("");
    setBusy(true);
    try {
      const repo = await window.OrchisAPI.post("/v1/repos", {
        name: name.trim(),
        description,
        visibility
      });
      await window.loadRepos().catch(() => {});
      onClose();
      setRoute({
        view: "repo",
        repo: repo.id
      });
    } catch (e) {
      setErr("Could not create repository. The name may already be taken, or it contains invalid characters.");
      setBusy(false);
    }
  };
  const valid = /^[A-Za-z0-9._-]{1,100}$/.test(name.trim());
  return /*#__PURE__*/React.createElement("div", {
    onClick: onClose,
    style: {
      position: "fixed",
      inset: 0,
      background: "color-mix(in oklab, var(--bg) 40%, transparent)",
      backdropFilter: "blur(2px)",
      zIndex: 200,
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      padding: 24
    }
  }, /*#__PURE__*/React.createElement("div", {
    onClick: e => e.stopPropagation(),
    className: "card fade-in",
    style: {
      width: 520,
      maxWidth: "92vw",
      padding: 22
    }
  }, /*#__PURE__*/React.createElement("h2", {
    style: {
      fontSize: 18,
      fontWeight: 500,
      margin: "0 0 4px"
    }
  }, "New repository"), /*#__PURE__*/React.createElement("p", {
    className: "muted",
    style: {
      fontSize: 13,
      margin: "0 0 18px"
    }
  }, "Creates an empty repository under ", /*#__PURE__*/React.createElement("span", {
    className: "mono"
  }, USERS.me.handle, "/"), ". Push to it over HTTPS or SSH."), err ? /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "10px 12px",
      border: "1px solid var(--danger)",
      borderRadius: 8,
      color: "var(--danger)",
      fontSize: 13,
      marginBottom: 14
    }
  }, err) : null, /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: 14
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "section-title",
    style: {
      marginBottom: 6
    }
  }, "Name"), /*#__PURE__*/React.createElement("input", {
    className: "input",
    autoFocus: true,
    value: name,
    onChange: e => setName(e.target.value),
    placeholder: "kicad-mcp"
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: 14
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "section-title",
    style: {
      marginBottom: 6
    }
  }, "Description ", /*#__PURE__*/React.createElement("span", {
    className: "muted",
    style: {
      textTransform: "none",
      letterSpacing: 0
    }
  }, "(optional)")), /*#__PURE__*/React.createElement("input", {
    className: "input",
    value: description,
    onChange: e => setDescription(e.target.value),
    placeholder: "What's in this repo?"
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: 18
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "section-title",
    style: {
      marginBottom: 6
    }
  }, "Visibility"), /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 8
    }
  }, ["private", "internal", "public"].map(v => /*#__PURE__*/React.createElement("label", {
    key: v,
    className: "row",
    style: {
      gap: 8,
      padding: "8px 12px",
      border: "1px solid var(--line)",
      borderRadius: 6,
      cursor: "pointer",
      flex: 1,
      background: visibility === v ? "var(--accent-soft)" : "var(--bg-1)",
      borderColor: visibility === v ? "var(--accent-line)" : "var(--line)"
    }
  }, /*#__PURE__*/React.createElement("input", {
    type: "radio",
    name: "vis",
    checked: visibility === v,
    onChange: () => setVisibility(v)
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      textTransform: "capitalize",
      fontSize: 13
    }
  }, v))))), /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn",
    onClick: onClose
  }, "Cancel"), /*#__PURE__*/React.createElement("span", {
    className: "spacer"
  }), /*#__PURE__*/React.createElement("button", {
    className: "btn primary",
    disabled: !valid || busy,
    onClick: create
  }, busy ? "Creating…" : "Create repository"))));
}
window.DashboardView = DashboardView;

// ===== src/views/repo.jsx =====
// Repo view — IDE-like: file tree (left) + main panel (code/readme) with optional split.
function RepoView({
  repoId,
  file,
  setRoute,
  openSplit,
  splitOpen,
  splitContent,
  closeSplit
}) {
  const repo = REPOS.find(r => r.id === repoId) || REPOS[0] || {
    id: repoId,
    name: repoId,
    org: "",
    description: "",
    languageColor: "var(--fg-3)",
    visibility: "private",
    stars: 0
  };
  const [active, setActive] = React.useState(file || "README.md");
  const [tab, setTab] = React.useState("code"); // code | readme | activity
  const [expanded, setExpanded] = React.useState(() => collectInitialExpanded(FILE_TREE));
  const [treeWidth, setTreeWidth] = React.useState(260);
  const [mainSplit, setMainSplit] = React.useState(0.55);

  // Live file tree for this repo (falls back to the mock when offline).
  const [tree, setTree] = React.useState(null);
  React.useEffect(() => {
    let cancelled = false;
    setTree(null);
    if (window.OrchisAPI && repoId) {
      window.OrchisAPI.get(`/v1/repos/${repoId}/tree`).then(t => {
        if (!cancelled) setTree(t || []);
      }).catch(() => {
        if (!cancelled) setTree(null);
      });
    }
    return () => {
      cancelled = true;
    };
  }, [repoId]);
  const treeNodes = tree || FILE_TREE;
  // Manage (settings + collaborators) is available to the owner or an admin
  // collaborator. repo.role comes from the API (owner|admin|write|read|"").
  const isOwner = repo.role === "owner" || repo.role === "admin" || !!(window.USERS && USERS.me && repo.org === USERS.me.handle);
  const [showSettings, setShowSettings] = React.useState(false);

  // Live star/pin state (seeded from the loaded repo, refreshed from the API).
  const [meta, setMeta] = React.useState({
    stars: repo.stars || 0,
    starred: !!repo.starred,
    pinned: !!repo.pinned
  });
  React.useEffect(() => {
    if (window.OrchisAPI && repoId) {
      window.OrchisAPI.get(`/v1/repos/${repoId}`).then(r => setMeta({
        stars: r.stars || 0,
        starred: !!r.starred,
        pinned: !!r.pinned
      })).catch(() => {});
    }
  }, [repoId]);
  const toggleStar = async () => {
    if (!window.OrchisAPI) return;
    const next = !meta.starred;
    setMeta(m => ({
      ...m,
      starred: next,
      stars: Math.max(0, m.stars + (next ? 1 : -1))
    }));
    try {
      await (next ? window.OrchisAPI.put(`/v1/me/stars/${repoId}`) : window.OrchisAPI.del(`/v1/me/stars/${repoId}`));
    } catch (e) {}
  };
  const togglePin = async () => {
    if (!window.OrchisAPI) return;
    const next = !meta.pinned;
    setMeta(m => ({
      ...m,
      pinned: next
    }));
    try {
      await (next ? window.OrchisAPI.put(`/v1/me/pinned/${repoId}`) : window.OrchisAPI.del(`/v1/me/pinned/${repoId}`));
      window.dispatchEvent(new CustomEvent("orchis:repos-changed"));
    } catch (e) {}
  };
  React.useEffect(() => {
    if (file) setActive(file);
  }, [file]);
  const toggle = path => setExpanded(e => ({
    ...e,
    [path]: !e[path]
  }));
  const onPickFile = node => {
    setActive(node.path);
    setTab("code");
  };
  const startTreeResize = e => {
    e.preventDefault();
    const startX = e.clientX;
    const startW = treeWidth;
    const move = ev => {
      const w = Math.max(180, Math.min(420, startW + (ev.clientX - startX)));
      setTreeWidth(w);
    };
    const up = () => {
      window.removeEventListener("mousemove", move);
      window.removeEventListener("mouseup", up);
    };
    window.addEventListener("mousemove", move);
    window.addEventListener("mouseup", up);
  };
  const startMainResize = e => {
    e.preventDefault();
    const containerEl = e.currentTarget.parentElement;
    const rect = containerEl.getBoundingClientRect();
    const move = ev => {
      const p = Math.max(0.25, Math.min(0.8, (ev.clientX - rect.left) / rect.width));
      setMainSplit(p);
    };
    const up = () => {
      window.removeEventListener("mousemove", move);
      window.removeEventListener("mouseup", up);
    };
    window.addEventListener("mousemove", move);
    window.addEventListener("mouseup", up);
  };
  return /*#__PURE__*/React.createElement("div", {
    style: repoStyles.shell
  }, /*#__PURE__*/React.createElement(RepoHeader, {
    repo: repo,
    setRoute: setRoute,
    openSplit: openSplit,
    isOwner: isOwner,
    onSettings: () => setShowSettings(true),
    meta: meta,
    toggleStar: toggleStar,
    togglePin: togglePin
  }), showSettings ? /*#__PURE__*/React.createElement(RepoSettingsModal, {
    repo: repo,
    onClose: () => setShowSettings(false),
    setRoute: setRoute
  }) : null, /*#__PURE__*/React.createElement("div", {
    style: repoStyles.body
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      ...repoStyles.tree,
      width: treeWidth
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: repoStyles.treeHead
  }, /*#__PURE__*/React.createElement("span", {
    className: "row",
    style: {
      gap: 6,
      color: "var(--fg-1)"
    }
  }, /*#__PURE__*/React.createElement(Icons.Branch, {
    size: 13
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: "var(--font-mono)",
      fontSize: 12
    }
  }, "main"), /*#__PURE__*/React.createElement(Icons.ChevronDown, {
    size: 11,
    style: {
      color: "var(--fg-3)"
    }
  })), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost icon sm",
    title: "Find in files"
  }, /*#__PURE__*/React.createElement(Icons.Search, {
    size: 12
  }))), /*#__PURE__*/React.createElement("div", {
    style: repoStyles.treeScroll
  }, treeNodes.length === 0 ? /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "16px 12px",
      color: "var(--fg-3)",
      fontSize: 12
    }
  }, "Empty repository. Push some code to get started.") : /*#__PURE__*/React.createElement(FileTree, {
    nodes: treeNodes,
    expanded: expanded,
    active: active,
    toggle: toggle,
    onPick: onPickFile,
    depth: 0
  }))), /*#__PURE__*/React.createElement("div", {
    style: repoStyles.resizer,
    onMouseDown: startTreeResize
  }), /*#__PURE__*/React.createElement("div", {
    style: repoStyles.mainArea
  }, splitOpen ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    style: {
      width: `calc(${mainSplit * 100}% - 3px)`,
      height: "100%",
      display: "flex",
      flexDirection: "column",
      overflow: "hidden"
    }
  }, /*#__PURE__*/React.createElement(RepoMainPanel, {
    repo: repo,
    repoId: repoId,
    active: active,
    tab: tab,
    setTab: setTab,
    setActive: onPickFile,
    setRoute: setRoute
  })), /*#__PURE__*/React.createElement("div", {
    style: repoStyles.mainResizer,
    onMouseDown: startMainResize
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      height: "100%",
      display: "flex",
      flexDirection: "column",
      overflow: "hidden",
      borderLeft: "1px solid var(--line)"
    }
  }, /*#__PURE__*/React.createElement(SplitPanel, {
    content: splitContent,
    close: closeSplit,
    setRoute: setRoute
  }))) : /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      height: "100%",
      display: "flex",
      flexDirection: "column",
      overflow: "hidden"
    }
  }, /*#__PURE__*/React.createElement(RepoMainPanel, {
    repo: repo,
    repoId: repoId,
    active: active,
    tab: tab,
    setTab: setTab,
    setActive: onPickFile
  })))));
}
function collectInitialExpanded(nodes, acc = {}) {
  nodes.forEach(n => {
    if (n.type === "dir" && n.expanded) {
      acc[n.path] = true;
      if (n.children) collectInitialExpanded(n.children, acc);
    }
  });
  return acc;
}
function FileTree({
  nodes,
  expanded,
  active,
  toggle,
  onPick,
  depth
}) {
  return nodes.map(n => {
    const isOpen = expanded[n.path];
    const isActive = n.type === "file" && active === n.path;
    return /*#__PURE__*/React.createElement(React.Fragment, {
      key: n.path
    }, /*#__PURE__*/React.createElement("button", {
      style: {
        ...repoStyles.treeRow,
        ...(isActive ? repoStyles.treeRowActive : {}),
        paddingLeft: 6 + depth * 14
      },
      onClick: () => n.type === "dir" ? toggle(n.path) : onPick(n)
    }, n.type === "dir" ? /*#__PURE__*/React.createElement(Icons.Chevron, {
      size: 11,
      style: {
        color: "var(--fg-3)",
        transform: isOpen ? "rotate(90deg)" : "none",
        transition: "transform 80ms"
      }
    }) : /*#__PURE__*/React.createElement("span", {
      style: {
        width: 11
      }
    }), n.type === "dir" ? /*#__PURE__*/React.createElement(Icons.Folder, {
      size: 13,
      style: {
        color: "var(--fg-2)"
      }
    }) : /*#__PURE__*/React.createElement("span", {
      style: {
        ...repoStyles.fileGlyph,
        background: langColor(n.lang)
      }
    }), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: "var(--font-mono)",
        fontSize: 12,
        color: isActive ? "var(--fg)" : "var(--fg-1)"
      }
    }, n.name)), n.type === "dir" && isOpen && n.children ? /*#__PURE__*/React.createElement(FileTree, {
      nodes: n.children,
      expanded: expanded,
      active: active,
      toggle: toggle,
      onPick: onPick,
      depth: depth + 1
    }) : null);
  });
}
function langColor(lang) {
  return {
    rust: "oklch(60% 0.14 30)",
    typescript: "oklch(62% 0.12 240)",
    yaml: "oklch(65% 0.10 90)",
    toml: "oklch(55% 0.08 30)",
    markdown: "oklch(60% 0.06 250)",
    go: "oklch(62% 0.12 200)",
    text: "var(--fg-3)"
  }[lang] || "var(--fg-3)";
}
function RepoHeader({
  repo,
  setRoute,
  openSplit,
  isOwner,
  onSettings,
  meta,
  toggleStar,
  togglePin
}) {
  const [copied, setCopied] = React.useState(false);
  const cloneURL = (typeof window !== "undefined" ? window.location.origin : "") + "/" + repo.org + "/" + repo.name + ".git";
  const copyClone = () => {
    navigator.clipboard?.writeText(cloneURL).then(() => {
      setCopied(true);
      setTimeout(() => setCopied(false), 1500);
    }).catch(() => {});
  };
  return /*#__PURE__*/React.createElement("div", {
    style: repoStyles.header
  }, /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 10,
      minWidth: 0,
      flex: 1
    }
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost icon sm",
    onClick: () => setRoute({
      view: "home"
    }),
    title: "Back"
  }, /*#__PURE__*/React.createElement(Icons.Chevron, {
    size: 12,
    style: {
      transform: "rotate(180deg)"
    }
  })), /*#__PURE__*/React.createElement("span", {
    style: {
      width: 10,
      height: 10,
      borderRadius: 3,
      background: repo.languageColor,
      flexShrink: 0
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: "var(--font-mono)",
      fontSize: 14,
      fontWeight: 500,
      overflow: "hidden",
      textOverflow: "ellipsis",
      whiteSpace: "nowrap"
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "muted"
  }, repo.org, "/"), repo.name), repo.visibility === "private" ? /*#__PURE__*/React.createElement("span", {
    className: "chip"
  }, "private") : /*#__PURE__*/React.createElement("span", {
    className: "chip"
  }, "public"), /*#__PURE__*/React.createElement("span", {
    className: "muted",
    style: {
      fontSize: 12.5,
      marginLeft: 8,
      overflow: "hidden",
      textOverflow: "ellipsis",
      whiteSpace: "nowrap"
    }
  }, repo.description)), /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 6,
      flexShrink: 0
    }
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn sm",
    onClick: () => openSplit({
      type: "prs",
      repo: repo.id
    }),
    title: "Open PRs in split"
  }, /*#__PURE__*/React.createElement(Icons.PR, {
    size: 12
  }), " PRs ", /*#__PURE__*/React.createElement("span", {
    className: "chip",
    style: {
      height: 16,
      fontSize: 10
    }
  }, "3")), /*#__PURE__*/React.createElement("button", {
    className: "btn sm",
    onClick: () => setRoute({
      view: "newpr",
      repo: repo.id
    }),
    title: "Open a pull request"
  }, /*#__PURE__*/React.createElement(Icons.Plus, {
    size: 12
  }), " PR"), /*#__PURE__*/React.createElement("button", {
    className: "btn sm",
    onClick: () => openSplit({
      type: "issues",
      repo: repo.id
    })
  }, /*#__PURE__*/React.createElement(Icons.Issue, {
    size: 12
  }), " Issues"), /*#__PURE__*/React.createElement("button", {
    className: "btn sm",
    onClick: () => openSplit({
      type: "actions",
      repo: repo.id
    })
  }, /*#__PURE__*/React.createElement(Icons.Bolt, {
    size: 12
  }), " Actions"), /*#__PURE__*/React.createElement("span", {
    style: {
      width: 1,
      height: 18,
      background: "var(--line)",
      margin: "0 4px"
    }
  }), /*#__PURE__*/React.createElement("button", {
    className: "btn sm",
    title: meta && meta.starred ? "Unstar" : "Star",
    onClick: toggleStar,
    style: meta && meta.starred ? {
      color: "var(--accent)",
      borderColor: "var(--accent-line)",
      background: "var(--accent-soft)"
    } : {}
  }, /*#__PURE__*/React.createElement(Icons.Star, {
    size: 12
  }), " ", ((meta ? meta.stars : repo.stars) || 0).toLocaleString()), /*#__PURE__*/React.createElement("button", {
    className: "btn sm",
    title: meta && meta.pinned ? "Unpin from sidebar" : "Pin to sidebar",
    onClick: togglePin,
    style: meta && meta.pinned ? {
      color: "var(--accent)",
      borderColor: "var(--accent-line)",
      background: "var(--accent-soft)"
    } : {}
  }, /*#__PURE__*/React.createElement(Icons.Pin, {
    size: 12
  }), " ", meta && meta.pinned ? "Pinned" : "Pin"), isOwner ? /*#__PURE__*/React.createElement("button", {
    className: "btn sm",
    title: "Repository settings",
    onClick: onSettings
  }, /*#__PURE__*/React.createElement(Icons.Settings, {
    size: 12
  })) : null, /*#__PURE__*/React.createElement("button", {
    className: "btn primary sm",
    onClick: copyClone,
    title: cloneURL
  }, /*#__PURE__*/React.createElement(Icons.Copy, {
    size: 12
  }), " ", copied ? "Copied!" : "Clone")));
}

// Owner-only repo settings: rename, description, visibility, default branch, delete.
function RepoSettingsModal({
  repo,
  onClose,
  setRoute
}) {
  const [name, setName] = React.useState(repo.name);
  const [description, setDescription] = React.useState(repo.description || "");
  const [visibility, setVisibility] = React.useState(repo.visibility || "private");
  const [defaultBranch, setDefaultBranch] = React.useState(repo.defaultBranch || "main");
  const [branches, setBranches] = React.useState([]);
  const [busy, setBusy] = React.useState(false);
  const [err, setErr] = React.useState("");
  const [confirmDelete, setConfirmDelete] = React.useState("");
  const [newBranch, setNewBranch] = React.useState("");
  const [branchBusy, setBranchBusy] = React.useState(false);
  const reloadBranches = React.useCallback(() => {
    if (window.OrchisAPI) window.OrchisAPI.get(`/v1/repos/${repo.id}/branches`).then(b => setBranches(b || [])).catch(() => {});
  }, [repo.id]);
  React.useEffect(() => {
    reloadBranches();
  }, [reloadBranches]);
  const createBranch = async () => {
    if (!newBranch.trim()) return;
    setErr("");
    setBranchBusy(true);
    try {
      await window.OrchisAPI.post(`/v1/repos/${repo.id}/branches`, {
        name: newBranch.trim(),
        from: defaultBranch
      });
      setNewBranch("");
      reloadBranches();
    } catch (e) {
      setErr("Could not create branch — the name may be invalid or already exist.");
    } finally {
      setBranchBusy(false);
    }
  };
  const deleteBranch = async b => {
    setErr("");
    try {
      await window.OrchisAPI.del(`/v1/repos/${repo.id}/branches/${b}`);
      reloadBranches();
    } catch (e) {
      setErr("Could not delete branch — you can't delete the default branch.");
    }
  };

  // Collaborators
  const [collabs, setCollabs] = React.useState([]);
  const [collabHandle, setCollabHandle] = React.useState("");
  const [collabRole, setCollabRole] = React.useState("write");
  const reloadCollabs = React.useCallback(() => {
    if (window.OrchisAPI) window.OrchisAPI.get(`/v1/repos/${repo.id}/collaborators`).then(c => setCollabs(c || [])).catch(() => {});
  }, [repo.id]);
  React.useEffect(() => {
    reloadCollabs();
  }, [reloadCollabs]);
  const addCollab = async () => {
    if (!collabHandle.trim()) return;
    setErr("");
    try {
      await window.OrchisAPI.put(`/v1/repos/${repo.id}/collaborators/${collabHandle.trim()}`, {
        role: collabRole
      });
      setCollabHandle("");
      reloadCollabs();
    } catch (e) {
      setErr("Could not add collaborator — check the handle exists.");
    }
  };
  const removeCollab = async h => {
    try {
      await window.OrchisAPI.del(`/v1/repos/${repo.id}/collaborators/${h}`);
      reloadCollabs();
    } catch (e) {}
  };
  const save = async () => {
    setErr("");
    setBusy(true);
    try {
      const updated = await window.OrchisAPI.patch(`/v1/repos/${repo.id}`, {
        name: name.trim(),
        description,
        visibility,
        defaultBranch
      });
      await window.loadRepos().catch(() => {});
      onClose();
      // Repo id is org/name; if renamed, navigate to the new id.
      if (updated && updated.id && updated.id !== repo.id) setRoute({
        view: "repo",
        repo: updated.id
      });
    } catch (e) {
      setErr("Could not save — the name may be taken, the default branch may not exist, or the value is invalid.");
      setBusy(false);
    }
  };
  const del = async () => {
    setBusy(true);
    try {
      await window.OrchisAPI.del(`/v1/repos/${repo.id}`);
      await window.loadRepos().catch(() => {});
      onClose();
      setRoute({
        view: "home"
      });
    } catch (e) {
      setErr("Delete failed.");
      setBusy(false);
    }
  };
  return /*#__PURE__*/React.createElement("div", {
    onClick: onClose,
    style: {
      position: "fixed",
      inset: 0,
      background: "color-mix(in oklab, var(--bg) 40%, transparent)",
      backdropFilter: "blur(2px)",
      zIndex: 200,
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      padding: 24
    }
  }, /*#__PURE__*/React.createElement("div", {
    onClick: e => e.stopPropagation(),
    className: "card fade-in",
    style: {
      width: 560,
      maxWidth: "92vw",
      maxHeight: "88vh",
      overflowY: "auto",
      padding: 22
    }
  }, /*#__PURE__*/React.createElement("h2", {
    style: {
      fontSize: 18,
      fontWeight: 500,
      margin: "0 0 4px"
    }
  }, "Repository settings"), /*#__PURE__*/React.createElement("p", {
    className: "muted",
    style: {
      fontSize: 13,
      margin: "0 0 18px"
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "mono"
  }, repo.org, "/", repo.name)), err ? /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "10px 12px",
      border: "1px solid var(--danger)",
      borderRadius: 8,
      color: "var(--danger)",
      fontSize: 13,
      marginBottom: 14
    }
  }, err) : null, /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: 14
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "section-title",
    style: {
      marginBottom: 6
    }
  }, "Name"), /*#__PURE__*/React.createElement("input", {
    className: "input",
    value: name,
    onChange: e => setName(e.target.value)
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: 14
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "section-title",
    style: {
      marginBottom: 6
    }
  }, "Description"), /*#__PURE__*/React.createElement("input", {
    className: "input",
    value: description,
    onChange: e => setDescription(e.target.value),
    placeholder: "What's in this repo?"
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: 14
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "section-title",
    style: {
      marginBottom: 6
    }
  }, "Default branch"), branches.length > 0 ? /*#__PURE__*/React.createElement("select", {
    className: "input",
    value: defaultBranch,
    onChange: e => setDefaultBranch(e.target.value)
  }, branches.map(b => /*#__PURE__*/React.createElement("option", {
    key: b.name,
    value: b.name
  }, b.name))) : /*#__PURE__*/React.createElement("input", {
    className: "input",
    value: defaultBranch,
    onChange: e => setDefaultBranch(e.target.value)
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: 18
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "section-title",
    style: {
      marginBottom: 6
    }
  }, "Visibility"), /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 8
    }
  }, ["private", "internal", "public"].map(v => /*#__PURE__*/React.createElement("label", {
    key: v,
    className: "row",
    style: {
      gap: 8,
      padding: "8px 12px",
      border: "1px solid var(--line)",
      borderRadius: 6,
      cursor: "pointer",
      flex: 1,
      background: visibility === v ? "var(--accent-soft)" : "var(--bg-1)",
      borderColor: visibility === v ? "var(--accent-line)" : "var(--line)"
    }
  }, /*#__PURE__*/React.createElement("input", {
    type: "radio",
    name: "vis",
    checked: visibility === v,
    onChange: () => setVisibility(v)
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      textTransform: "capitalize",
      fontSize: 13
    }
  }, v))))), /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 8,
      marginBottom: 24
    }
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn",
    onClick: onClose
  }, "Cancel"), /*#__PURE__*/React.createElement("span", {
    className: "spacer"
  }), /*#__PURE__*/React.createElement("button", {
    className: "btn primary",
    disabled: busy || !name.trim(),
    onClick: save
  }, busy ? "Saving…" : "Save changes")), /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: 24
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "section-title",
    style: {
      marginBottom: 8
    }
  }, "Branches"), /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      overflow: "hidden",
      marginBottom: 10
    }
  }, branches.length === 0 ? /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "12px 14px",
      color: "var(--fg-3)",
      fontSize: 12.5
    }
  }, "No branches yet \u2014 push some code.") : branches.map((b, i) => /*#__PURE__*/React.createElement("div", {
    key: b.name,
    className: "row",
    style: {
      gap: 10,
      padding: "9px 14px",
      borderBottom: i < branches.length - 1 ? "1px solid var(--line)" : "none"
    }
  }, /*#__PURE__*/React.createElement(Icons.Branch, {
    size: 13,
    style: {
      color: "var(--fg-2)"
    }
  }), /*#__PURE__*/React.createElement("span", {
    className: "mono",
    style: {
      fontSize: 12.5,
      flex: 1
    }
  }, b.name), b.name === (repo.defaultBranch || defaultBranch) ? /*#__PURE__*/React.createElement("span", {
    className: "chip",
    style: {
      height: 18,
      fontSize: 10
    }
  }, "default") : /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    style: {
      color: "var(--danger)"
    },
    onClick: () => deleteBranch(b.name)
  }, "Delete")))), /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("input", {
    className: "input",
    value: newBranch,
    onChange: e => setNewBranch(e.target.value),
    onKeyDown: e => {
      if (e.key === "Enter") createBranch();
    },
    placeholder: "new-branch (from " + defaultBranch + ")",
    style: {
      flex: 1
    }
  }), /*#__PURE__*/React.createElement("button", {
    className: "btn",
    disabled: !newBranch.trim() || branchBusy,
    onClick: createBranch
  }, branchBusy ? "Creating…" : "Create branch"))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: 24
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "section-title",
    style: {
      marginBottom: 8
    }
  }, "Collaborators"), /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      overflow: "hidden",
      marginBottom: 10
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 10,
      padding: "9px 14px",
      borderBottom: collabs.length ? "1px solid var(--line)" : "none"
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "avatar",
    style: {
      background: "var(--accent)",
      width: 22,
      height: 22,
      fontSize: 9,
      color: "var(--accent-fg)"
    }
  }, (repo.org || "?").slice(0, 2).toUpperCase()), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      fontSize: 13
    }
  }, repo.org, " ", /*#__PURE__*/React.createElement("span", {
    className: "subtle"
  }, "(owner)")), /*#__PURE__*/React.createElement("span", {
    className: "chip",
    style: {
      height: 18,
      fontSize: 10
    }
  }, "owner")), collabs.map((c, i) => /*#__PURE__*/React.createElement("div", {
    key: c.handle,
    className: "row",
    style: {
      gap: 10,
      padding: "9px 14px",
      borderBottom: i < collabs.length - 1 ? "1px solid var(--line)" : "none"
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "avatar",
    style: {
      background: c.color,
      width: 22,
      height: 22,
      fontSize: 9
    }
  }, c.initials), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      fontSize: 13
    }
  }, c.handle, " ", /*#__PURE__*/React.createElement("span", {
    className: "subtle"
  }, c.name)), /*#__PURE__*/React.createElement("span", {
    className: "chip",
    style: {
      height: 18,
      fontSize: 10
    }
  }, c.role), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    style: {
      color: "var(--danger)"
    },
    onClick: () => removeCollab(c.handle)
  }, "Remove")))), /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("input", {
    className: "input",
    value: collabHandle,
    onChange: e => setCollabHandle(e.target.value),
    placeholder: "user handle",
    style: {
      flex: 1
    },
    onKeyDown: e => {
      if (e.key === "Enter") addCollab();
    }
  }), /*#__PURE__*/React.createElement("select", {
    className: "input",
    value: collabRole,
    onChange: e => setCollabRole(e.target.value),
    style: {
      width: 110
    }
  }, /*#__PURE__*/React.createElement("option", {
    value: "read"
  }, "read"), /*#__PURE__*/React.createElement("option", {
    value: "write"
  }, "write"), /*#__PURE__*/React.createElement("option", {
    value: "admin"
  }, "admin")), /*#__PURE__*/React.createElement("button", {
    className: "btn",
    disabled: !collabHandle.trim(),
    onClick: addCollab
  }, "Add"))), /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid var(--danger)",
      borderRadius: 8,
      padding: 14
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontWeight: 500,
      color: "var(--danger)",
      fontSize: 13,
      marginBottom: 6
    }
  }, "Danger zone"), /*#__PURE__*/React.createElement("p", {
    className: "muted",
    style: {
      fontSize: 12.5,
      margin: "0 0 10px"
    }
  }, "Deleting a repository is permanent \u2014 code, PRs, and history are gone. Type ", /*#__PURE__*/React.createElement("span", {
    className: "mono"
  }, repo.name), " to confirm."), /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("input", {
    className: "input",
    value: confirmDelete,
    onChange: e => setConfirmDelete(e.target.value),
    placeholder: repo.name,
    style: {
      maxWidth: 240
    }
  }), /*#__PURE__*/React.createElement("button", {
    className: "btn",
    style: {
      borderColor: "var(--danger)",
      color: "var(--danger)"
    },
    disabled: busy || confirmDelete !== repo.name,
    onClick: del
  }, "Delete this repository")))));
}
function RepoMainPanel({
  repo,
  repoId,
  active,
  tab,
  setTab,
  setActive,
  setRoute
}) {
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    style: repoStyles.subtabs
  }, /*#__PURE__*/React.createElement(Subtab, {
    active: tab === "code",
    onClick: () => setTab("code"),
    icon: /*#__PURE__*/React.createElement(Icons.Code, {
      size: 12
    })
  }, "Code"), /*#__PURE__*/React.createElement(Subtab, {
    active: tab === "readme",
    onClick: () => setTab("readme"),
    icon: /*#__PURE__*/React.createElement(Icons.Book, {
      size: 12
    })
  }, "Readme"), /*#__PURE__*/React.createElement(Subtab, {
    active: tab === "commits",
    onClick: () => setTab("commits"),
    icon: /*#__PURE__*/React.createElement(Icons.Commit, {
      size: 12
    })
  }, "Commits"), /*#__PURE__*/React.createElement(Subtab, {
    active: tab === "activity",
    onClick: () => setTab("activity"),
    icon: /*#__PURE__*/React.createElement(Icons.Activity, {
      size: 12
    })
  }, "Activity"), /*#__PURE__*/React.createElement(Subtab, {
    active: tab === "releases",
    onClick: () => setTab("releases"),
    icon: /*#__PURE__*/React.createElement(Icons.Tag, {
      size: 12
    })
  }, "Releases")), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minHeight: 0,
      overflowY: "auto"
    }
  }, tab === "code" ? /*#__PURE__*/React.createElement(CodeView, {
    repoId: repoId,
    path: active
  }) : null, tab === "readme" ? /*#__PURE__*/React.createElement(ReadmeView, {
    repoId: repoId
  }) : null, tab === "commits" ? /*#__PURE__*/React.createElement(RepoCommitsList, {
    repoId: repoId,
    repo: repo,
    setRoute: setRoute
  }) : null, tab === "activity" ? /*#__PURE__*/React.createElement(RecentActivityView, {
    repoId: repoId
  }) : null, tab === "releases" ? /*#__PURE__*/React.createElement(ReleasesView, {
    repo: repo,
    repoId: repoId
  }) : null));
}

// RepoCommitsList — commit history for the repo's default branch; rows open the
// commit detail view.
function RepoCommitsList({
  repoId,
  repo,
  setRoute
}) {
  const [commits, setCommits] = React.useState(null);
  React.useEffect(() => {
    if (window.OrchisAPI && repoId) window.OrchisAPI.get(`/v1/repos/${repoId}/commits?limit=100`).then(setCommits).catch(() => setCommits([]));
  }, [repoId]);
  const list = commits || [];
  return /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "20px 24px",
      maxWidth: 900
    }
  }, commits == null ? /*#__PURE__*/React.createElement("div", {
    className: "muted",
    style: {
      fontSize: 12.5
    }
  }, "Loading\u2026") : null, commits != null && list.length === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      padding: "20px 18px",
      color: "var(--fg-3)",
      fontSize: 13
    }
  }, "No commits yet.") : null, list.length > 0 ? /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      overflow: "hidden"
    }
  }, list.map((c, i) => /*#__PURE__*/React.createElement("button", {
    key: c.sha,
    onClick: () => setRoute({
      view: "commit",
      repo: repoId,
      sha: c.sha
    }),
    style: {
      display: "flex",
      alignItems: "center",
      gap: 12,
      width: "100%",
      textAlign: "left",
      padding: "11px 14px",
      background: "transparent",
      border: "none",
      borderBottom: i < list.length - 1 ? "1px solid var(--line)" : "none",
      cursor: "pointer",
      font: "inherit",
      color: "inherit"
    }
  }, /*#__PURE__*/React.createElement(Icons.Commit, {
    size: 14,
    style: {
      color: "var(--fg-2)"
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 13.5,
      fontWeight: 450,
      overflow: "hidden",
      textOverflow: "ellipsis",
      whiteSpace: "nowrap"
    }
  }, c.message.split("\n")[0]), /*#__PURE__*/React.createElement("div", {
    className: "subtle",
    style: {
      fontSize: 11.5,
      marginTop: 2
    }
  }, c.author, " \xB7 ", relativeDate(c.date))), /*#__PURE__*/React.createElement("span", {
    className: "mono subtle",
    style: {
      fontSize: 11.5
    }
  }, c.short)))) : null);
}

// relativeDate renders an ISO date compactly (the API already sends UTC ISO).
function relativeDate(iso) {
  if (!iso) return "";
  const d = new Date(iso);
  if (isNaN(d)) return iso;
  return d.toLocaleDateString(undefined, {
    year: "numeric",
    month: "short",
    day: "numeric"
  });
}
function Subtab({
  active,
  onClick,
  icon,
  children
}) {
  return /*#__PURE__*/React.createElement("button", {
    onClick: onClick,
    style: {
      ...repoStyles.subtab,
      color: active ? "var(--fg)" : "var(--fg-2)",
      borderBottomColor: active ? "var(--accent)" : "transparent"
    }
  }, icon, children);
}
function CodeView({
  repoId,
  path
}) {
  const [blob, setBlob] = React.useState(null);
  const [err, setErr] = React.useState(false);
  React.useEffect(() => {
    let cancelled = false;
    setBlob(null);
    setErr(false);
    if (window.OrchisAPI && repoId && path && path !== "README.md") {
      window.OrchisAPI.get(`/v1/repos/${repoId}/blob?path=${encodeURIComponent(path)}`).then(b => {
        if (!cancelled) setBlob(b);
      }).catch(() => {
        if (!cancelled) setErr(true);
      });
    }
    return () => {
      cancelled = true;
    };
  }, [repoId, path]);
  if (path === "README.md") return /*#__PURE__*/React.createElement(ReadmeView, {
    repoId: repoId
  });

  // Offline prototype fallback: the original hardcoded router.rs sample.
  if (!window.OrchisAPI && path === "crates/atlas-core/src/router.rs") {
    return /*#__PURE__*/React.createElement("div", {
      style: repoStyles.code
    }, /*#__PURE__*/React.createElement("div", {
      style: repoStyles.codeBar
    }, /*#__PURE__*/React.createElement("span", {
      className: "mono",
      style: {
        color: "var(--fg-2)"
      }
    }, path), /*#__PURE__*/React.createElement("span", {
      className: "spacer"
    }), /*#__PURE__*/React.createElement("span", {
      className: "muted",
      style: {
        fontSize: 11.5
      }
    }, "56 lines \xB7 1.4 KB \xB7 Rust"), /*#__PURE__*/React.createElement("button", {
      className: "btn ghost sm icon"
    }, /*#__PURE__*/React.createElement(Icons.Copy, {
      size: 12
    }))), /*#__PURE__*/React.createElement(CodeBlock, {
      code: ROUTER_RS
    }));
  }
  if (blob) {
    return /*#__PURE__*/React.createElement("div", {
      style: repoStyles.code
    }, /*#__PURE__*/React.createElement("div", {
      style: repoStyles.codeBar
    }, /*#__PURE__*/React.createElement("span", {
      className: "mono",
      style: {
        color: "var(--fg-2)"
      }
    }, path), /*#__PURE__*/React.createElement("span", {
      className: "spacer"
    }), /*#__PURE__*/React.createElement("span", {
      className: "muted",
      style: {
        fontSize: 11.5
      }
    }, blob.lines, " lines \xB7 ", fmtBytes(blob.size), " \xB7 ", blob.lang), /*#__PURE__*/React.createElement("button", {
      className: "btn ghost sm icon"
    }, /*#__PURE__*/React.createElement(Icons.Copy, {
      size: 12
    }))), /*#__PURE__*/React.createElement(CodeBlock, {
      code: blob.content
    }));
  }
  return /*#__PURE__*/React.createElement("div", {
    style: repoStyles.code
  }, /*#__PURE__*/React.createElement("div", {
    style: repoStyles.codeBar
  }, /*#__PURE__*/React.createElement("span", {
    className: "mono",
    style: {
      color: "var(--fg-2)"
    }
  }, path), /*#__PURE__*/React.createElement("span", {
    className: "spacer"
  })), /*#__PURE__*/React.createElement("div", {
    className: "placeholder",
    style: {
      margin: 20,
      height: 220
    }
  }, err ? `[ could not load ${path} ]` : `[ loading ${path}… ]`));
}
function fmtBytes(n) {
  if (n == null) return "";
  if (n < 1024) return n + " B";
  if (n < 1024 * 1024) return (n / 1024).toFixed(1) + " KB";
  return (n / (1024 * 1024)).toFixed(1) + " MB";
}
function CodeBlock({
  code
}) {
  const lines = code.split("\n");
  return /*#__PURE__*/React.createElement("pre", {
    style: repoStyles.pre
  }, lines.map((ln, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: repoStyles.codeLine
  }, /*#__PURE__*/React.createElement("span", {
    style: repoStyles.lineNo
  }, i + 1), /*#__PURE__*/React.createElement("code", {
    style: {
      whiteSpace: "pre",
      fontFamily: "var(--font-mono)"
    }
  }, highlightRust(ln)))));
}

// Very lightweight Rust-ish syntax highlight.
function highlightRust(line) {
  const tokens = [];
  let i = 0;
  const push = (text, color) => tokens.push(/*#__PURE__*/React.createElement("span", {
    key: tokens.length,
    style: color ? {
      color
    } : null
  }, text));

  // Comments
  const commentIdx = line.indexOf("//");
  if (commentIdx !== -1) {
    const before = line.slice(0, commentIdx);
    const after = line.slice(commentIdx);
    return /*#__PURE__*/React.createElement(React.Fragment, null, highlightRust(before), /*#__PURE__*/React.createElement("span", {
      style: {
        color: "var(--fg-3)",
        fontStyle: "italic"
      }
    }, after));
  }
  const keywords = ["use", "pub", "fn", "impl", "struct", "let", "mut", "async", "await", "self", "Self", "match", "None", "Some", "Ok", "Err", "Result", "Option", "if", "else", "return", "true", "false", "in"];
  const regex = /([A-Za-z_][A-Za-z0-9_]*)|("(?:[^"\\]|\\.)*")|(\/\/.*$)|(\b\d+\b)|(\s+)|([^A-Za-z0-9_"\s]+)/g;
  let m;
  while ((m = regex.exec(line)) !== null) {
    const tok = m[0];
    if (m[1]) {
      if (keywords.includes(tok)) push(tok, "oklch(60% 0.14 280)");else if (/^[A-Z]/.test(tok)) push(tok, "oklch(64% 0.12 30)");else push(tok);
    } else if (m[2]) push(tok, "oklch(60% 0.12 130)");else if (m[4]) push(tok, "oklch(65% 0.13 200)");else if (m[6]) push(tok, "var(--fg-2)");else push(tok);
  }
  return tokens;
}
function ReadmeView({
  repoId
}) {
  const [md, setMd] = React.useState(null);
  React.useEffect(() => {
    let cancelled = false;
    if (window.OrchisAPI && repoId) {
      window.OrchisAPI.get(`/v1/repos/${repoId}/readme`).then(r => {
        if (!cancelled) setMd(r && r.content != null ? r.content : "");
      }).catch(() => {
        if (!cancelled) setMd(null);
      });
    }
    return () => {
      cancelled = true;
    };
  }, [repoId]);

  // md === null → not loaded / offline: use the mock. md === "" → no README.
  const content = md != null ? md : README_MD;
  if (content === "") {
    return /*#__PURE__*/React.createElement("div", {
      style: {
        ...repoStyles.readme,
        color: "var(--fg-3)"
      }
    }, "No README in this repository.");
  }
  return /*#__PURE__*/React.createElement("div", {
    style: repoStyles.readme
  }, parseMd(content));
}
function parseMd(md) {
  const out = [];
  const lines = md.split("\n");
  let i = 0;
  let key = 0;
  while (i < lines.length) {
    const ln = lines[i];
    if (ln.startsWith("# ")) {
      out.push(/*#__PURE__*/React.createElement("h1", {
        key: key++,
        style: repoStyles.mdH1
      }, ln.slice(2)));
      i++;
    } else if (ln.startsWith("## ")) {
      out.push(/*#__PURE__*/React.createElement("h2", {
        key: key++,
        style: repoStyles.mdH2
      }, ln.slice(3)));
      i++;
    } else if (ln.startsWith("> ")) {
      out.push(/*#__PURE__*/React.createElement("blockquote", {
        key: key++,
        style: repoStyles.mdQuote
      }, ln.slice(2)));
      i++;
    } else if (ln.startsWith("```")) {
      const start = i + 1;
      let end = start;
      while (end < lines.length && !lines[end].startsWith("```")) end++;
      out.push(/*#__PURE__*/React.createElement("pre", {
        key: key++,
        style: repoStyles.mdCode
      }, lines.slice(start, end).join("\n")));
      i = end + 1;
    } else if (ln.startsWith("- ")) {
      const items = [];
      while (i < lines.length && lines[i].startsWith("- ")) {
        items.push(lines[i].slice(2));
        i++;
      }
      out.push(/*#__PURE__*/React.createElement("ul", {
        key: key++,
        style: repoStyles.mdUl
      }, items.map((it, j) => /*#__PURE__*/React.createElement("li", {
        key: j,
        dangerouslySetInnerHTML: {
          __html: inlineMd(it)
        }
      }))));
    } else if (ln.trim() === "") {
      i++;
    } else {
      out.push(/*#__PURE__*/React.createElement("p", {
        key: key++,
        style: repoStyles.mdP,
        dangerouslySetInnerHTML: {
          __html: inlineMd(ln)
        }
      }));
      i++;
    }
  }
  return out;
}
function inlineMd(s) {
  return s.replace(/`([^`]+)`/g, "<code style=\"font-family: var(--font-mono); background: var(--bg-2); padding: 1px 5px; border-radius: 4px; font-size: 0.9em;\">$1</code>").replace(/\*\*([^*]+)\*\*/g, "<strong>$1</strong>").replace(/\[([^\]]+)\]\(([^)]+)\)/g, "<a href=\"#\" style=\"color: var(--accent); text-decoration: none;\">$1</a>");
}
function RecentActivityView({
  repoId
}) {
  const [activity, setActivity] = React.useState(null);
  React.useEffect(() => {
    if (window.OrchisAPI && repoId) {
      window.OrchisAPI.get(`/v1/repos/${repoId}/activity`).then(setActivity).catch(() => setActivity([]));
    }
  }, [repoId]);
  const items = activity != null ? activity : window.OrchisAPI ? [] : ACTIVITY.slice(0, 4);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "20px 24px"
    }
  }, items.length === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      padding: "20px 18px",
      color: "var(--fg-3)",
      fontSize: 13
    }
  }, "No activity yet.") : /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      padding: 4
    }
  }, items.map(a => /*#__PURE__*/React.createElement("div", {
    key: a.id,
    style: {
      display: "flex",
      alignItems: "center",
      gap: 10,
      padding: "10px 12px",
      borderBottom: "1px solid var(--line)",
      fontSize: 13
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "avatar",
    style: {
      background: a.actor.color,
      width: 20,
      height: 20,
      fontSize: 9
    }
  }, a.actor.initials), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1
    }
  }, /*#__PURE__*/React.createElement("strong", {
    style: {
      fontWeight: 500
    }
  }, a.actor.name), " ", /*#__PURE__*/React.createElement("span", {
    className: "muted"
  }, activityVerb(a.kind), " ", a.target), /*#__PURE__*/React.createElement("div", {
    className: "subtle",
    style: {
      fontSize: 11.5
    }
  }, a.title)), /*#__PURE__*/React.createElement("span", {
    className: "subtle",
    style: {
      fontSize: 11
    }
  }, a.when)))));
}

// Split panel — shows PRs, Issues, or Actions while you're in the code.
function SplitPanel({
  content,
  close,
  setRoute
}) {
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    style: repoStyles.splitHead
  }, /*#__PURE__*/React.createElement(Icons.Split, {
    size: 13,
    style: {
      color: "var(--fg-2)"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12.5,
      fontWeight: 500,
      textTransform: "capitalize"
    }
  }, content.type), /*#__PURE__*/React.createElement("span", {
    className: "mono subtle",
    style: {
      fontSize: 11
    }
  }, content.repo), /*#__PURE__*/React.createElement("span", {
    className: "spacer"
  }), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost icon sm",
    onClick: close,
    title: "Close split"
  }, /*#__PURE__*/React.createElement(Icons.Close, {
    size: 11
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      overflowY: "auto",
      padding: 12
    }
  }, content.type === "prs" ? /*#__PURE__*/React.createElement(PRsSplit, {
    repo: content.repo,
    setRoute: setRoute
  }) : content.type === "issues" ? /*#__PURE__*/React.createElement(IssuesSplit, {
    repo: content.repo
  }) : /*#__PURE__*/React.createElement(ActionsSplit, {
    repo: content.repo
  })));
}
function PRsSplit({
  repo,
  setRoute
}) {
  const [pulls, setPulls] = React.useState(null);
  React.useEffect(() => {
    if (window.OrchisAPI && repo) window.OrchisAPI.get(`/v1/repos/${repo}/pulls?state=open`).then(setPulls).catch(() => setPulls([]));
  }, [repo]);
  const prList = pulls != null ? pulls : window.OrchisAPI ? [] : PRS.filter(p => p.repo === repo);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 6
    }
  }, prList.length === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "subtle",
    style: {
      fontSize: 12,
      padding: 8
    }
  }, "No open pull requests.") : null, prList.map(p => /*#__PURE__*/React.createElement("button", {
    key: p.id,
    className: "card",
    style: repoStyles.prMini,
    onClick: () => setRoute({
      view: "pr",
      pr: p.id,
      repo
    })
  }, /*#__PURE__*/React.createElement(Icons.PR, {
    size: 14,
    style: {
      color: "var(--accent)"
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0,
      textAlign: "left"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 13,
      fontWeight: 450,
      overflow: "hidden",
      textOverflow: "ellipsis",
      whiteSpace: "nowrap"
    }
  }, p.title), /*#__PURE__*/React.createElement("div", {
    className: "subtle",
    style: {
      fontSize: 11,
      marginTop: 2
    }
  }, "#", p.id, " \xB7 by ", p.author.name, " \xB7 ", p.updated)), /*#__PURE__*/React.createElement("span", {
    className: "chip",
    style: {
      height: 18,
      fontSize: 10.5
    }
  }, "+", p.additions, " \u2212", p.deletions))));
}
function IssuesSplit({
  repo
}) {
  const [items, setItems] = React.useState(null);
  const [selected, setSelected] = React.useState(null); // issue number
  const [creating, setCreating] = React.useState(false);
  const [title, setTitle] = React.useState("");
  const [body, setBody] = React.useState("");
  const [busy, setBusy] = React.useState(false);
  const reload = React.useCallback(() => {
    if (window.OrchisAPI && repo) window.OrchisAPI.get(`/v1/repos/${repo}/issues`).then(setItems).catch(() => setItems([]));
  }, [repo]);
  React.useEffect(() => {
    reload();
  }, [reload]);
  const list = items != null ? items : [];
  const create = async () => {
    if (!title.trim()) return;
    setBusy(true);
    try {
      await window.OrchisAPI.post(`/v1/repos/${repo}/issues`, {
        title: title.trim(),
        body: body.trim()
      });
      setTitle("");
      setBody("");
      setCreating(false);
      reload();
    } catch (e) {} finally {
      setBusy(false);
    }
  };
  if (selected != null) {
    return /*#__PURE__*/React.createElement(IssueDetail, {
      repo: repo,
      num: selected,
      onBack: () => {
        setSelected(null);
        reload();
      }
    });
  }
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 6
    }
  }, window.OrchisAPI ? creating ? /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      padding: 12,
      display: "flex",
      flexDirection: "column",
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("input", {
    className: "input",
    value: title,
    onChange: e => setTitle(e.target.value),
    placeholder: "Issue title",
    autoFocus: true
  }), /*#__PURE__*/React.createElement("textarea", {
    value: body,
    onChange: e => setBody(e.target.value),
    placeholder: "Describe it\u2026 (optional)",
    style: {
      width: "100%",
      height: 70,
      padding: 8,
      border: "1px solid var(--line)",
      borderRadius: 6,
      fontFamily: "inherit",
      fontSize: 12.5,
      background: "var(--bg-1)",
      color: "var(--fg)",
      resize: "vertical"
    }
  }), /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn sm",
    onClick: () => {
      setCreating(false);
      setTitle("");
      setBody("");
    }
  }, "Cancel"), /*#__PURE__*/React.createElement("span", {
    className: "spacer"
  }), /*#__PURE__*/React.createElement("button", {
    className: "btn primary sm",
    onClick: create,
    disabled: !title.trim() || busy
  }, busy ? "Opening…" : "Open issue"))) : /*#__PURE__*/React.createElement("button", {
    className: "btn sm",
    style: {
      alignSelf: "flex-start",
      marginBottom: 2
    },
    onClick: () => setCreating(true)
  }, /*#__PURE__*/React.createElement(Icons.Plus, {
    size: 12
  }), " New issue") : null, list.length === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "subtle",
    style: {
      fontSize: 12,
      padding: 8
    }
  }, "No issues yet.") : null, list.map(it => /*#__PURE__*/React.createElement("button", {
    key: it.number,
    className: "card",
    style: repoStyles.prMini,
    onClick: () => setSelected(it.number)
  }, /*#__PURE__*/React.createElement(Icons.Issue, {
    size: 14,
    style: {
      color: it.closed ? "var(--purple)" : "var(--info)"
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0,
      textAlign: "left"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 13,
      fontWeight: 450,
      overflow: "hidden",
      textOverflow: "ellipsis",
      whiteSpace: "nowrap"
    }
  }, it.title), /*#__PURE__*/React.createElement("div", {
    className: "subtle",
    style: {
      fontSize: 11,
      marginTop: 2
    }
  }, "#", it.number, it.closed ? " · closed" : "", " \xB7 ", it.comments || 0, " comment", it.comments === 1 ? "" : "s")), it.assignee ? /*#__PURE__*/React.createElement("span", {
    className: "avatar",
    style: {
      background: it.assignee.color,
      width: 18,
      height: 18,
      fontSize: 8
    }
  }, it.assignee.initials) : null)));
}
function IssueDetail({
  repo,
  num,
  onBack
}) {
  const [issue, setIssue] = React.useState(null);
  const [comments, setComments] = React.useState([]);
  const [draft, setDraft] = React.useState("");
  const [busy, setBusy] = React.useState(false);
  const load = React.useCallback(() => {
    if (!window.OrchisAPI) return;
    window.OrchisAPI.get(`/v1/repos/${repo}/issues/${num}`).then(setIssue).catch(() => setIssue(null));
    window.OrchisAPI.get(`/v1/repos/${repo}/issues/${num}/comments`).then(setComments).catch(() => setComments([]));
  }, [repo, num]);
  React.useEffect(() => {
    load();
  }, [load]);
  const comment = async () => {
    if (!draft.trim()) return;
    setBusy(true);
    try {
      await window.OrchisAPI.post(`/v1/repos/${repo}/issues/${num}/comments`, {
        body: draft.trim()
      });
      setDraft("");
      load();
    } catch (e) {} finally {
      setBusy(false);
    }
  };
  const toggleState = async () => {
    if (!issue) return;
    setBusy(true);
    try {
      const r = await window.OrchisAPI.patch(`/v1/repos/${repo}/issues/${num}`, {
        state: issue.closed ? "open" : "closed"
      });
      setIssue(r);
    } catch (e) {} finally {
      setBusy(false);
    }
  };
  if (!issue) return /*#__PURE__*/React.createElement("div", {
    className: "subtle",
    style: {
      fontSize: 12,
      padding: 8
    }
  }, "Loading\u2026");
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 10
    }
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    style: {
      alignSelf: "flex-start"
    },
    onClick: onBack
  }, "\u2190 All issues"), /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 8,
      alignItems: "flex-start"
    }
  }, /*#__PURE__*/React.createElement(Icons.Issue, {
    size: 16,
    style: {
      color: issue.closed ? "var(--purple)" : "var(--info)",
      marginTop: 2
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 15,
      fontWeight: 500
    }
  }, issue.title), /*#__PURE__*/React.createElement("div", {
    className: "subtle",
    style: {
      fontSize: 11.5,
      marginTop: 3
    }
  }, "#", issue.number, " \xB7 ", /*#__PURE__*/React.createElement("span", {
    className: "chip",
    style: {
      height: 18,
      fontSize: 10.5,
      color: issue.closed ? "var(--purple)" : "var(--accent)"
    }
  }, issue.closed ? "closed" : "open"), " \xB7 opened by ", issue.author.name, " \xB7 ", issue.created))), issue.body ? /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      padding: 12,
      fontSize: 13,
      whiteSpace: "pre-wrap",
      lineHeight: 1.5
    }
  }, issue.body) : null, comments.map((c, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    className: "card",
    style: {
      padding: 12
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 8,
      marginBottom: 6
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "avatar",
    style: {
      background: c.author.color,
      width: 18,
      height: 18,
      fontSize: 8
    }
  }, c.author.initials), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12.5,
      fontWeight: 500
    }
  }, c.author.name), /*#__PURE__*/React.createElement("span", {
    className: "subtle",
    style: {
      fontSize: 11
    }
  }, c.when)), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 13,
      whiteSpace: "pre-wrap",
      lineHeight: 1.5
    }
  }, c.body))), window.OrchisAPI ? /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("textarea", {
    value: draft,
    onChange: e => setDraft(e.target.value),
    placeholder: "Leave a comment\u2026",
    style: {
      width: "100%",
      height: 64,
      padding: 8,
      border: "1px solid var(--line)",
      borderRadius: 6,
      fontFamily: "inherit",
      fontSize: 12.5,
      background: "var(--bg-1)",
      color: "var(--fg)",
      resize: "vertical"
    }
  }), /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn sm",
    onClick: toggleState,
    disabled: busy
  }, issue.closed ? "Reopen" : "Close issue"), /*#__PURE__*/React.createElement("span", {
    className: "spacer"
  }), /*#__PURE__*/React.createElement("button", {
    className: "btn primary sm",
    onClick: comment,
    disabled: !draft.trim() || busy
  }, "Comment"))) : null);
}
function ActionsSplit({
  repo
}) {
  const [runs, setRuns] = React.useState(null);
  React.useEffect(() => {
    if (window.OrchisAPI && repo) window.OrchisAPI.get(`/v1/repos/${repo}/checks`).then(setRuns).catch(() => setRuns([]));
  }, [repo]);
  const list = runs != null ? runs : [];
  if (runs != null && list.length === 0) {
    return /*#__PURE__*/React.createElement("div", {
      className: "card",
      style: {
        padding: 16,
        fontSize: 12.5,
        color: "var(--fg-2)",
        lineHeight: 1.55
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        fontWeight: 500,
        color: "var(--fg)",
        marginBottom: 6
      }
    }, "No check runs yet"), "Foundry doesn't execute workflows itself. Check runs appear here from the built-in ", /*#__PURE__*/React.createElement("span", {
      className: "mono"
    }, "orchis-scan"), " supply-chain check, and from any external CI you wire up via webhooks + the checks API.");
  }
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 6
    }
  }, list.map((r, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    className: "card",
    style: repoStyles.prMini
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 8,
      height: 8,
      borderRadius: 999,
      background: r.status === "ok" ? "var(--accent)" : r.status === "fail" ? "var(--danger)" : r.status === "cancelled" ? "var(--fg-3)" : "var(--warn)",
      boxShadow: r.status === "pending" || r.status === "queued" ? "0 0 0 3px color-mix(in oklab, var(--warn) 25%, transparent)" : "none"
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 13,
      fontWeight: 450
    }
  }, r.name, " ", /*#__PURE__*/React.createElement("span", {
    className: "mono subtle",
    style: {
      fontSize: 11.5
    }
  }, "\xB7 ", r.sha)), /*#__PURE__*/React.createElement("div", {
    className: "subtle",
    style: {
      fontSize: 11,
      marginTop: 2,
      overflow: "hidden",
      textOverflow: "ellipsis",
      whiteSpace: "nowrap"
    }
  }, r.detail || r.status, r.when ? " · " + r.when : "")), r.externalUrl ? /*#__PURE__*/React.createElement("a", {
    className: "btn ghost sm",
    href: r.externalUrl,
    target: "_blank",
    rel: "noreferrer"
  }, "View") : null)));
}

// Releases — list + (owner) publish + download source tarball.
function ReleasesView({
  repo,
  repoId
}) {
  const isOwner = !!(window.USERS && USERS.me && repo.org === USERS.me.handle);
  const [items, setItems] = React.useState(null);
  const [creating, setCreating] = React.useState(false);
  const [tag, setTag] = React.useState("");
  const [name, setName] = React.useState("");
  const [body, setBody] = React.useState("");
  const [prerelease, setPrerelease] = React.useState(false);
  const [busy, setBusy] = React.useState(false);
  const [err, setErr] = React.useState("");
  const reload = React.useCallback(() => {
    if (window.OrchisAPI && repoId) window.OrchisAPI.get(`/v1/repos/${repoId}/releases`).then(setItems).catch(() => setItems([]));
  }, [repoId]);
  React.useEffect(() => {
    reload();
  }, [reload]);
  const list = items != null ? items : [];
  const publish = async () => {
    if (!tag.trim()) return;
    setErr("");
    setBusy(true);
    try {
      await window.OrchisAPI.post(`/v1/repos/${repoId}/releases`, {
        tag: tag.trim(),
        name: name.trim(),
        body: body.trim(),
        prerelease
      });
      setTag("");
      setName("");
      setBody("");
      setPrerelease(false);
      setCreating(false);
      reload();
    } catch (e) {
      setErr("Could not publish — the tag may be invalid or already released.");
    } finally {
      setBusy(false);
    }
  };
  const remove = async t => {
    try {
      await window.OrchisAPI.del(`/v1/repos/${repoId}/releases/${t}`);
      reload();
    } catch (e) {}
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "20px 24px",
      maxWidth: 820
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      marginBottom: 14
    }
  }, /*#__PURE__*/React.createElement("h2", {
    style: {
      fontSize: 16,
      fontWeight: 500,
      margin: 0
    }
  }, "Releases"), /*#__PURE__*/React.createElement("span", {
    className: "spacer"
  }), isOwner && window.OrchisAPI ? /*#__PURE__*/React.createElement("button", {
    className: "btn primary sm",
    onClick: () => {
      setErr("");
      setCreating(c => !c);
    }
  }, /*#__PURE__*/React.createElement(Icons.Tag, {
    size: 12
  }), " Draft a release") : null), err ? /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "8px 12px",
      border: "1px solid var(--danger)",
      borderRadius: 6,
      color: "var(--danger)",
      fontSize: 12.5,
      marginBottom: 12
    }
  }, err) : null, creating ? /*#__PURE__*/React.createElement("div", {
    className: "card fade-in",
    style: {
      padding: 16,
      marginBottom: 16,
      display: "flex",
      flexDirection: "column",
      gap: 10
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("input", {
    className: "input",
    value: tag,
    onChange: e => setTag(e.target.value),
    placeholder: "tag e.g. v1.0.0",
    style: {
      flex: 1
    }
  }), /*#__PURE__*/React.createElement("input", {
    className: "input",
    value: name,
    onChange: e => setName(e.target.value),
    placeholder: "release name (optional)",
    style: {
      flex: 1
    }
  })), /*#__PURE__*/React.createElement("textarea", {
    value: body,
    onChange: e => setBody(e.target.value),
    placeholder: "Release notes (markdown)\u2026",
    style: {
      width: "100%",
      height: 100,
      padding: 10,
      border: "1px solid var(--line)",
      borderRadius: 6,
      fontFamily: "inherit",
      fontSize: 13,
      background: "var(--bg-1)",
      color: "var(--fg)",
      resize: "vertical"
    }
  }), /*#__PURE__*/React.createElement("label", {
    className: "row",
    style: {
      gap: 8,
      fontSize: 12.5,
      cursor: "pointer"
    }
  }, /*#__PURE__*/React.createElement("input", {
    type: "checkbox",
    checked: prerelease,
    onChange: e => setPrerelease(e.target.checked)
  }), " Mark as pre-release"), /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "subtle",
    style: {
      fontSize: 11.5
    }
  }, "Tagged from ", /*#__PURE__*/React.createElement("span", {
    className: "mono"
  }, repo.defaultBranch || "main"), " if the tag doesn't exist yet."), /*#__PURE__*/React.createElement("span", {
    className: "spacer"
  }), /*#__PURE__*/React.createElement("button", {
    className: "btn sm",
    onClick: () => setCreating(false)
  }, "Cancel"), /*#__PURE__*/React.createElement("button", {
    className: "btn primary sm",
    disabled: !tag.trim() || busy,
    onClick: publish
  }, busy ? "Publishing…" : "Publish release"))) : null, list.length === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      padding: "20px 18px",
      color: "var(--fg-3)",
      fontSize: 13
    }
  }, "No releases yet.") : list.map(rel => /*#__PURE__*/React.createElement("div", {
    key: rel.tag,
    className: "card",
    style: {
      padding: 16,
      marginBottom: 12
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 8,
      marginBottom: 6
    }
  }, /*#__PURE__*/React.createElement(Icons.Tag, {
    size: 14,
    style: {
      color: "var(--accent)"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 15,
      fontWeight: 500
    }
  }, rel.name), /*#__PURE__*/React.createElement("span", {
    className: "chip",
    style: {
      height: 18,
      fontSize: 10.5
    }
  }, rel.tag), rel.prerelease ? /*#__PURE__*/React.createElement("span", {
    className: "chip warn",
    style: {
      height: 18,
      fontSize: 10.5
    }
  }, "pre-release") : null, /*#__PURE__*/React.createElement("span", {
    className: "mono subtle",
    style: {
      fontSize: 11
    }
  }, rel.sha), /*#__PURE__*/React.createElement("span", {
    className: "spacer"
  }), /*#__PURE__*/React.createElement("span", {
    className: "subtle",
    style: {
      fontSize: 11.5
    }
  }, rel.created)), rel.body ? /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 13,
      lineHeight: 1.5,
      whiteSpace: "pre-wrap",
      color: "var(--fg-1)",
      margin: "4px 0 10px"
    }
  }, rel.body) : null, /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("a", {
    className: "btn sm",
    href: rel.tarball,
    download: true
  }, /*#__PURE__*/React.createElement(Icons.File, {
    size: 12
  }), " Source (.tar.gz)"), rel.author ? /*#__PURE__*/React.createElement("span", {
    className: "subtle",
    style: {
      fontSize: 11.5
    }
  }, "by ", rel.author.name) : null, /*#__PURE__*/React.createElement("span", {
    className: "spacer"
  }), isOwner ? /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    style: {
      color: "var(--danger)"
    },
    onClick: () => remove(rel.tag)
  }, "Delete") : null))));
}
const repoStyles = {
  shell: {
    display: "flex",
    flexDirection: "column",
    height: "100%",
    overflow: "hidden"
  },
  header: {
    display: "flex",
    alignItems: "center",
    gap: 10,
    padding: "10px 16px",
    borderBottom: "1px solid var(--line)",
    background: "var(--bg-1)",
    minHeight: 52
  },
  body: {
    flex: 1,
    display: "flex",
    minHeight: 0,
    overflow: "hidden"
  },
  tree: {
    flexShrink: 0,
    background: "var(--bg-1)",
    display: "flex",
    flexDirection: "column",
    minWidth: 180
  },
  treeHead: {
    display: "flex",
    alignItems: "center",
    justifyContent: "space-between",
    padding: "8px 10px",
    borderBottom: "1px solid var(--line)"
  },
  treeScroll: {
    flex: 1,
    overflowY: "auto",
    padding: "4px 4px 12px"
  },
  treeRow: {
    display: "flex",
    alignItems: "center",
    gap: 6,
    width: "100%",
    height: 26,
    paddingRight: 6,
    background: "transparent",
    border: "none",
    cursor: "pointer",
    borderRadius: 4,
    font: "inherit",
    color: "var(--fg-1)"
  },
  treeRowActive: {
    background: "var(--accent-soft)"
  },
  fileGlyph: {
    width: 8,
    height: 8,
    borderRadius: 2,
    flexShrink: 0
  },
  resizer: {
    width: 6,
    cursor: "col-resize",
    background: "transparent",
    borderLeft: "1px solid var(--line)"
  },
  mainArea: {
    flex: 1,
    display: "flex",
    minWidth: 0,
    overflow: "hidden"
  },
  mainResizer: {
    width: 6,
    cursor: "col-resize",
    background: "var(--bg)",
    borderLeft: "1px solid var(--line)",
    borderRight: "1px solid var(--line)"
  },
  subtabs: {
    display: "flex",
    alignItems: "center",
    gap: 2,
    padding: "0 16px",
    borderBottom: "1px solid var(--line)",
    background: "var(--bg-1)",
    height: 38
  },
  subtab: {
    height: 38,
    padding: "0 12px",
    background: "transparent",
    border: "none",
    borderBottom: "2px solid transparent",
    cursor: "pointer",
    font: "inherit",
    fontSize: 12.5,
    fontWeight: 450,
    color: "var(--fg-2)",
    display: "inline-flex",
    alignItems: "center",
    gap: 6
  },
  code: {
    display: "flex",
    flexDirection: "column",
    height: "100%"
  },
  codeBar: {
    display: "flex",
    alignItems: "center",
    gap: 12,
    padding: "6px 14px",
    fontSize: 12,
    color: "var(--fg-2)",
    borderBottom: "1px solid var(--line)",
    background: "var(--bg-1)"
  },
  pre: {
    margin: 0,
    padding: "10px 0",
    fontFamily: "var(--font-mono)",
    fontSize: 12.5,
    lineHeight: 1.55,
    background: "var(--bg)",
    overflowX: "auto"
  },
  codeLine: {
    display: "flex",
    paddingRight: 16
  },
  lineNo: {
    display: "inline-block",
    textAlign: "right",
    width: 44,
    paddingRight: 14,
    color: "var(--fg-3)",
    fontSize: 11,
    userSelect: "none",
    flexShrink: 0
  },
  readme: {
    padding: "28px 36px 60px",
    maxWidth: 820,
    fontSize: 14.5,
    lineHeight: 1.65,
    color: "var(--fg-1)"
  },
  mdH1: {
    fontSize: 28,
    fontWeight: 500,
    letterSpacing: "-0.02em",
    margin: "0 0 16px",
    paddingBottom: 12,
    borderBottom: "1px solid var(--line)",
    color: "var(--fg)"
  },
  mdH2: {
    fontSize: 18,
    fontWeight: 500,
    margin: "28px 0 12px",
    color: "var(--fg)"
  },
  mdP: {
    margin: "0 0 14px"
  },
  mdQuote: {
    borderLeft: "3px solid var(--accent-line)",
    paddingLeft: 14,
    margin: "0 0 18px",
    color: "var(--fg-2)",
    fontStyle: "italic"
  },
  mdCode: {
    background: "var(--bg-2)",
    border: "1px solid var(--line)",
    borderRadius: 8,
    padding: "12px 14px",
    fontFamily: "var(--font-mono)",
    fontSize: 12.5,
    margin: "0 0 16px",
    whiteSpace: "pre",
    overflowX: "auto"
  },
  mdUl: {
    margin: "0 0 16px 0",
    padding: "0 0 0 22px"
  },
  splitHead: {
    display: "flex",
    alignItems: "center",
    gap: 8,
    padding: "0 12px",
    height: 38,
    borderBottom: "1px solid var(--line)",
    background: "var(--bg-1)"
  },
  prMini: {
    display: "flex",
    alignItems: "center",
    gap: 10,
    padding: "10px 12px",
    cursor: "pointer",
    font: "inherit",
    color: "inherit",
    textAlign: "left"
  }
};
window.RepoView = RepoView;

// ===== src/views/pr.jsx =====
// PR review screen — clean, dense, conversational comments inline.
function PRView({
  prId,
  repo,
  setRoute
}) {
  const [pr, setPr] = React.useState(() => PRS.find(p => p.id === prId) || PRS[0]);
  const [files, setFiles] = React.useState(null);
  const [commits, setCommits] = React.useState(null);
  const [checks, setChecks] = React.useState(null);
  const [comments, setComments] = React.useState(null);
  const [tab, setTab] = React.useState("files"); // conversation | files | commits | checks
  const [reviewing, setReviewing] = React.useState(false);
  const [reviewBody, setReviewBody] = React.useState("");
  const [reviewVerdict, setReviewVerdict] = React.useState("approve");
  const [openComment, setOpenComment] = React.useState(null); // {fileIdx, lineKey}
  const [draftComments, setDraftComments] = React.useState({});
  const base = repo ? `/v1/repos/${repo}/pulls/${prId}` : null;
  const reload = React.useCallback(() => {
    if (!window.OrchisAPI || !base) return;
    window.OrchisAPI.get(base).then(setPr).catch(() => {});
    window.OrchisAPI.get(base + "/files").then(setFiles).catch(() => setFiles([]));
    window.OrchisAPI.get(base + "/commits").then(setCommits).catch(() => setCommits([]));
    window.OrchisAPI.get(base + "/checks").then(setChecks).catch(() => setChecks([]));
    window.OrchisAPI.get(base + "/comments").then(setComments).catch(() => setComments([]));
  }, [base]);
  React.useEffect(() => {
    reload();
  }, [reload]);

  // Realtime: live-refresh this PR when someone comments / reviews / merges,
  // or when a check (e.g. orchis-scan) updates. SSE auth rides the cookie.
  const [live, setLive] = React.useState(false);
  React.useEffect(() => {
    if (!window.EventSource || !repo || prId == null) return;
    const topic = `pull:${repo}#${prId}`;
    const es = new EventSource(`/v1/stream?topics=${encodeURIComponent(topic)}`);
    es.onopen = () => setLive(true);
    es.onerror = () => setLive(false);
    const onEvent = () => reload();
    ["pull.commented", "pull.reviewed", "pull.merged", "check.updated", "pull.reviewers-changed"].forEach(k => es.addEventListener(k, onEvent));
    return () => {
      es.close();
      setLive(false);
    };
  }, [repo, prId, reload]);
  const [merging, setMerging] = React.useState(false);
  const [actionErr, setActionErr] = React.useState("");
  const doMerge = async strategy => {
    setActionErr("");
    setMerging(true);
    try {
      await window.OrchisAPI.post(base + "/merge", {
        strategy: strategy || "merge"
      });
      reload();
    } catch (e) {
      setActionErr("Merge failed — likely a conflict, or you're not the repo owner.");
    } finally {
      setMerging(false);
    }
  };
  const doReview = async (verdict, body, comments) => {
    if (!window.OrchisAPI || !base) return;
    await window.OrchisAPI.post(base + "/reviews", {
      verdict,
      body,
      comments: comments || []
    }).catch(() => {});
    setPending([]);
    reload();
  };
  // Staged line comments for a batched review.
  const [pending, setPending] = React.useState([]);
  const addPending = c => {
    setPending(p => [...p, c]);
    setReviewing(true);
  };
  const postComment = async (body, extra) => {
    if (!window.OrchisAPI || !base || !body.trim()) return;
    await window.OrchisAPI.post(base + "/comments", {
      body,
      ...(extra || {})
    }).catch(() => {});
    reload();
  };
  const [reqOpen, setReqOpen] = React.useState(false);
  const [reqHandle, setReqHandle] = React.useState("");
  const [reqErr, setReqErr] = React.useState("");
  const requestReview = async () => {
    if (!base || !reqHandle.trim()) return;
    setReqErr("");
    try {
      await window.OrchisAPI.post(base + "/request-review", {
        reviewer: reqHandle.trim()
      });
      setReqHandle("");
      setReqOpen(false);
      reload();
    } catch (e) {
      setReqErr("Couldn't request — check the handle exists and you own the repo or authored the PR.");
    }
  };

  // AI assist (uses the requesting user's configured model).
  const [modelOk, setModelOk] = React.useState(false);
  React.useEffect(() => {
    if (window.OrchisAPI) {
      window.OrchisAPI.get("/v1/me/scanner").then(c => setModelOk(!!(c.hasKey || c.baseUrl))).catch(() => {});
    }
  }, []);
  const [aiBusy, setAiBusy] = React.useState("");
  const [aiResult, setAiResult] = React.useState(null); // { label, text, canApply }
  const runAI = async (label, path, canApply) => {
    if (!base) return;
    setAiBusy(path);
    setAiResult(null);
    try {
      const r = await window.OrchisAPI.post(base + "/" + path, {});
      setAiResult({
        label,
        text: r.summary || r.explanation || r.description || "",
        canApply: !!canApply
      });
    } catch (e) {
      setAiResult({
        label,
        text: "_Could not run. Make sure your model is configured in Developer settings → Scanner._",
        canApply: false
      });
    } finally {
      setAiBusy("");
    }
  };
  const applyDraft = async () => {
    if (!aiResult) return;
    await window.OrchisAPI.patch(base, {
      body: aiResult.text
    }).catch(() => {});
    setAiResult(null);
    reload();
  };
  if (!pr) return /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 40
    },
    className: "muted"
  }, "Loading pull request\u2026");
  const statusLabel = (pr.status || "open").charAt(0).toUpperCase() + (pr.status || "open").slice(1);
  return /*#__PURE__*/React.createElement("div", {
    style: prStyles.shell
  }, /*#__PURE__*/React.createElement("div", {
    style: prStyles.header
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost icon sm",
    onClick: () => setRoute({
      view: "prs"
    }),
    title: "Back"
  }, /*#__PURE__*/React.createElement(Icons.Chevron, {
    size: 12,
    style: {
      transform: "rotate(180deg)"
    }
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0,
      flex: 1
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 8,
      marginBottom: 2
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "chip accent"
  }, /*#__PURE__*/React.createElement(Icons.PR, {
    size: 11
  }), " ", statusLabel), /*#__PURE__*/React.createElement("span", {
    className: "mono subtle",
    style: {
      fontSize: 12
    }
  }, pr.repo, " #", pr.id), (pr.labels || []).map(l => /*#__PURE__*/React.createElement("span", {
    key: l.name,
    className: "chip " + l.color
  }, l.name))), /*#__PURE__*/React.createElement("h1", {
    style: prStyles.title
  }, pr.title), /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 8,
      marginTop: 6,
      color: "var(--fg-2)",
      fontSize: 12.5,
      flexWrap: "wrap"
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "avatar",
    style: {
      background: pr.author.color,
      width: 18,
      height: 18,
      fontSize: 8
    }
  }, pr.author.initials), /*#__PURE__*/React.createElement("span", null, /*#__PURE__*/React.createElement("strong", {
    style: {
      color: "var(--fg-1)",
      fontWeight: 500
    }
  }, pr.author.name), " wants to merge"), /*#__PURE__*/React.createElement("span", {
    className: "mono chip"
  }, pr.commits, " commits"), /*#__PURE__*/React.createElement("span", null, "from"), /*#__PURE__*/React.createElement("span", {
    className: "mono chip"
  }, pr.branch), /*#__PURE__*/React.createElement("span", null, "into"), /*#__PURE__*/React.createElement("span", {
    className: "mono chip"
  }, pr.base), /*#__PURE__*/React.createElement("span", {
    className: "subtle"
  }, "\xB7 updated ", pr.updated))), /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 6,
      flexShrink: 0
    }
  }, pr.status === "merged" ? /*#__PURE__*/React.createElement("span", {
    className: "chip",
    style: {
      color: "var(--purple, var(--accent))"
    }
  }, "Merged") : /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("button", {
    className: "btn sm",
    onClick: () => setReviewing(true)
  }, /*#__PURE__*/React.createElement(Icons.Check, {
    size: 12
  }), " Review"), /*#__PURE__*/React.createElement("button", {
    className: "btn primary sm",
    disabled: merging,
    onClick: () => doMerge("merge")
  }, /*#__PURE__*/React.createElement(Icons.Branch, {
    size: 12
  }), " ", merging ? "Merging…" : "Merge")))), actionErr ? /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "8px 24px",
      color: "var(--danger)",
      fontSize: 12.5,
      background: "var(--bg-1)",
      borderBottom: "1px solid var(--line)"
    }
  }, actionErr) : null, /*#__PURE__*/React.createElement("div", {
    style: prStyles.statusStrip
  }, /*#__PURE__*/React.createElement(Stat, {
    icon: /*#__PURE__*/React.createElement(Icons.Check, {
      size: 12
    }),
    label: "checks",
    value: `${pr.checks.passed} passed${pr.checks.pending ? `, ${pr.checks.pending} pending` : ''}`,
    good: pr.checks.failed === 0
  }), /*#__PURE__*/React.createElement(Stat, {
    icon: /*#__PURE__*/React.createElement(Icons.Eye, {
      size: 12
    }),
    label: "reviewers",
    value: /*#__PURE__*/React.createElement("span", {
      className: "row",
      style: {
        gap: -4
      }
    }, pr.reviewers.map((r, i) => /*#__PURE__*/React.createElement("span", {
      key: r.id,
      className: "avatar",
      style: {
        background: r.color,
        width: 18,
        height: 18,
        fontSize: 8,
        marginLeft: i ? -6 : 0,
        border: "2px solid var(--bg-1)"
      }
    }, r.initials)), /*#__PURE__*/React.createElement("span", {
      style: {
        marginLeft: 8
      }
    }, pr.reviewers.length, " requested"), pr.status !== "merged" ? reqOpen ? /*#__PURE__*/React.createElement("span", {
      className: "row",
      style: {
        gap: 4,
        marginLeft: 8
      }
    }, /*#__PURE__*/React.createElement("input", {
      value: reqHandle,
      onChange: e => setReqHandle(e.target.value),
      placeholder: "handle",
      onKeyDown: e => {
        if (e.key === "Enter") requestReview();
        if (e.key === "Escape") {
          setReqOpen(false);
          setReqErr("");
        }
      },
      autoFocus: true,
      style: {
        height: 22,
        width: 110,
        padding: "0 6px",
        border: "1px solid var(--line)",
        borderRadius: 5,
        background: "var(--bg-1)",
        color: "var(--fg)",
        font: "inherit",
        fontSize: 11.5
      }
    }), /*#__PURE__*/React.createElement("button", {
      className: "btn sm",
      onClick: requestReview,
      disabled: !reqHandle.trim()
    }, "Request"), /*#__PURE__*/React.createElement("button", {
      className: "btn ghost sm",
      onClick: () => {
        setReqOpen(false);
        setReqErr("");
      }
    }, "\u2715")) : /*#__PURE__*/React.createElement("button", {
      className: "btn ghost sm",
      style: {
        marginLeft: 8,
        height: 22
      },
      onClick: () => setReqOpen(true),
      title: "Request a review"
    }, "+ Reviewer") : null, reqErr ? /*#__PURE__*/React.createElement("span", {
      style: {
        marginLeft: 8,
        fontSize: 11,
        color: "var(--danger)"
      }
    }, reqErr) : null)
  }), /*#__PURE__*/React.createElement(Stat, {
    icon: /*#__PURE__*/React.createElement(Icons.Diff, {
      size: 12
    }),
    label: "diff",
    value: /*#__PURE__*/React.createElement("span", null, /*#__PURE__*/React.createElement("span", {
      style: {
        color: "var(--accent)"
      }
    }, "+", pr.additions), " / ", /*#__PURE__*/React.createElement("span", {
      style: {
        color: "var(--danger)"
      }
    }, "\u2212", pr.deletions), " \xB7 ", pr.files, " files")
  }), /*#__PURE__*/React.createElement(Stat, {
    icon: /*#__PURE__*/React.createElement(Icons.Branch, {
      size: 12
    }),
    label: "mergeable",
    value: /*#__PURE__*/React.createElement("span", {
      style: {
        color: "var(--accent)"
      }
    }, "clean \u2014 no conflicts"),
    good: true
  }), /*#__PURE__*/React.createElement("span", {
    className: "spacer"
  }), live ? /*#__PURE__*/React.createElement("span", {
    className: "row",
    style: {
      gap: 5,
      fontSize: 11,
      color: "var(--accent)"
    },
    title: "Live \u2014 this page updates in real time"
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 7,
      height: 7,
      borderRadius: 999,
      background: "var(--accent)",
      boxShadow: "0 0 0 3px var(--accent-soft)"
    }
  }), "Live") : null, /*#__PURE__*/React.createElement("span", {
    className: "muted",
    style: {
      fontSize: 11.5
    }
  }, pr.comments, " comments")), modelOk ? /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 6,
      padding: "8px 24px",
      borderBottom: "1px solid var(--line)",
      background: "var(--bg-1)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "subtle",
    style: {
      fontSize: 11,
      textTransform: "uppercase",
      letterSpacing: "0.05em",
      marginRight: 4
    }
  }, "AI assist"), /*#__PURE__*/React.createElement("button", {
    className: "btn sm",
    disabled: !!aiBusy,
    onClick: () => runAI("Summary", "summarize", false)
  }, aiBusy === "summarize" ? "Summarizing…" : "Summarize"), /*#__PURE__*/React.createElement("button", {
    className: "btn sm",
    disabled: !!aiBusy,
    onClick: () => runAI("Explanation", "explain", false)
  }, aiBusy === "explain" ? "Explaining…" : "Explain diff"), /*#__PURE__*/React.createElement("button", {
    className: "btn sm",
    disabled: !!aiBusy,
    onClick: () => runAI("Drafted description", "draft-description", true)
  }, aiBusy === "draft-description" ? "Drafting…" : "Draft description"), /*#__PURE__*/React.createElement("span", {
    className: "spacer"
  }), /*#__PURE__*/React.createElement("span", {
    className: "subtle",
    style: {
      fontSize: 11
    }
  }, "uses your configured model")) : null, aiResult ? /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "12px 24px 16px",
      borderBottom: "1px solid var(--line)",
      background: "var(--bg-2)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      marginBottom: 8,
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("strong", {
    style: {
      fontSize: 13
    }
  }, "AI \xB7 ", aiResult.label), /*#__PURE__*/React.createElement("span", {
    className: "spacer"
  }), aiResult.canApply ? /*#__PURE__*/React.createElement("button", {
    className: "btn sm",
    onClick: applyDraft
  }, "Apply to PR description") : null, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost icon sm",
    onClick: () => setAiResult(null),
    title: "Dismiss"
  }, /*#__PURE__*/React.createElement(Icons.Close, {
    size: 11
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 13.5,
      lineHeight: 1.6,
      maxWidth: 860
    }
  }, parseMd(aiResult.text || ""))) : null, /*#__PURE__*/React.createElement("div", {
    style: prStyles.tabs
  }, /*#__PURE__*/React.createElement(PRTab, {
    active: tab === "conversation",
    onClick: () => setTab("conversation")
  }, "Conversation ", /*#__PURE__*/React.createElement("span", {
    className: "chip",
    style: {
      height: 16,
      fontSize: 10
    }
  }, pr.comments)), /*#__PURE__*/React.createElement(PRTab, {
    active: tab === "files",
    onClick: () => setTab("files")
  }, "Files changed ", /*#__PURE__*/React.createElement("span", {
    className: "chip",
    style: {
      height: 16,
      fontSize: 10
    }
  }, pr.files)), /*#__PURE__*/React.createElement(PRTab, {
    active: tab === "commits",
    onClick: () => setTab("commits")
  }, "Commits ", /*#__PURE__*/React.createElement("span", {
    className: "chip",
    style: {
      height: 16,
      fontSize: 10
    }
  }, pr.commits)), /*#__PURE__*/React.createElement(PRTab, {
    active: tab === "checks",
    onClick: () => setTab("checks")
  }, "Checks ", /*#__PURE__*/React.createElement("span", {
    className: "chip",
    style: {
      height: 16,
      fontSize: 10
    }
  }, pr.checks.passed + pr.checks.failed + pr.checks.pending))), /*#__PURE__*/React.createElement("div", {
    style: prStyles.body
  }, tab === "files" ? /*#__PURE__*/React.createElement(FilesChanged, {
    files: files != null ? files : PR_DIFF_FILES,
    openComment: openComment,
    setOpenComment: setOpenComment,
    postComment: postComment,
    addPending: addPending,
    pending: pending,
    canComment: !!base
  }) : null, tab === "conversation" ? /*#__PURE__*/React.createElement(Conversation, {
    pr: pr,
    comments: comments,
    postComment: postComment
  }) : null, tab === "commits" ? /*#__PURE__*/React.createElement(CommitsList, {
    commits: commits,
    repo: repo,
    setRoute: setRoute
  }) : null, tab === "checks" ? /*#__PURE__*/React.createElement(ChecksList, {
    checks: checks
  }) : null), reviewing ? /*#__PURE__*/React.createElement(ReviewBar, {
    body: reviewBody,
    setBody: setReviewBody,
    verdict: reviewVerdict,
    setVerdict: setReviewVerdict,
    pendingCount: pending.length,
    onCancel: () => setReviewing(false),
    onSubmit: () => {
      doReview(reviewVerdict, reviewBody, pending);
      setReviewing(false);
      setReviewBody("");
    }
  }) : null);
}
function PRTab({
  active,
  onClick,
  children
}) {
  return /*#__PURE__*/React.createElement("button", {
    onClick: onClick,
    style: {
      display: "inline-flex",
      alignItems: "center",
      gap: 6,
      height: 40,
      padding: "0 14px",
      background: "transparent",
      border: "none",
      borderBottom: active ? "2px solid var(--accent)" : "2px solid transparent",
      color: active ? "var(--fg)" : "var(--fg-2)",
      cursor: "pointer",
      font: "inherit",
      fontSize: 13,
      fontWeight: active ? 500 : 450
    }
  }, children);
}
function Stat({
  icon,
  label,
  value,
  good
}) {
  return /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: good ? "var(--accent)" : "var(--fg-2)",
      display: "inline-flex"
    }
  }, icon), /*#__PURE__*/React.createElement("span", {
    className: "subtle",
    style: {
      fontSize: 11,
      textTransform: "uppercase",
      letterSpacing: "0.05em"
    }
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12.5,
      color: "var(--fg-1)"
    }
  }, value));
}
function FilesChanged({
  files,
  openComment,
  setOpenComment,
  postComment,
  addPending,
  pending,
  canComment
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "16px 20px 40px",
      maxWidth: 1100,
      margin: "0 auto"
    }
  }, files.map((f, fi) => /*#__PURE__*/React.createElement(FileDiff, {
    key: f.path,
    file: f,
    fi: fi,
    openComment: openComment,
    setOpenComment: setOpenComment,
    postComment: postComment,
    addPending: addPending,
    pending: pending,
    canComment: canComment
  })));
}

// anchorFor maps a diff line to the (line, side) a comment should attach to:
// added/context lines anchor to the right (new) side; deleted lines to the
// left (old) side. Returns null for lines with no usable line number.
function anchorFor(ln) {
  const oldN = parseInt(ln.num[0], 10);
  const newN = parseInt(ln.num[1], 10);
  if (ln.type === "del") return Number.isNaN(oldN) ? null : {
    line: oldN,
    side: "left"
  };
  return Number.isNaN(newN) ? null : {
    line: newN,
    side: "right"
  };
}
function FileDiff({
  file,
  fi,
  openComment,
  setOpenComment,
  postComment,
  addPending,
  pending,
  canComment
}) {
  const [collapsed, setCollapsed] = React.useState(false);
  const [busy, setBusy] = React.useState(false);
  const submitComment = async (ln, body) => {
    const a = anchorFor(ln);
    if (!a || !body.trim()) {
      setOpenComment(null);
      return;
    }
    setBusy(true);
    try {
      await postComment(body.trim(), {
        path: file.path,
        line: a.line,
        side: a.side
      });
      setOpenComment(null);
    } finally {
      setBusy(false);
    }
  };
  const stageComment = (ln, body) => {
    const a = anchorFor(ln);
    if (!a || !body.trim()) {
      setOpenComment(null);
      return;
    }
    addPending({
      path: file.path,
      line: a.line,
      side: a.side,
      body: body.trim()
    });
    setOpenComment(null);
  };
  return /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      marginBottom: 16,
      overflow: "hidden"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: prStyles.fileHead,
    onClick: () => setCollapsed(c => !c)
  }, /*#__PURE__*/React.createElement(Icons.Chevron, {
    size: 11,
    style: {
      color: "var(--fg-3)",
      transform: collapsed ? "none" : "rotate(90deg)",
      transition: "transform 80ms"
    }
  }), /*#__PURE__*/React.createElement("span", {
    className: "mono",
    style: {
      fontSize: 12.5,
      fontWeight: 500
    }
  }, file.path), /*#__PURE__*/React.createElement("span", {
    className: "chip",
    style: {
      height: 18
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--accent)"
    }
  }, "+", file.additions)), /*#__PURE__*/React.createElement("span", {
    className: "chip",
    style: {
      height: 18
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--danger)"
    }
  }, "\u2212", file.deletions)), /*#__PURE__*/React.createElement("span", {
    className: "spacer"
  }), /*#__PURE__*/React.createElement("span", {
    className: "muted",
    style: {
      fontSize: 11.5
    }
  }, file.comments.length, " comment", file.comments.length === 1 ? "" : "s"), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    onClick: e => {
      e.stopPropagation();
    },
    title: "View file"
  }, "View")), collapsed ? null : /*#__PURE__*/React.createElement("div", {
    style: {
      overflowX: "auto"
    }
  }, file.hunks.length === 0 ? /*#__PURE__*/React.createElement("div", {
    style: {
      padding: 20,
      textAlign: "center",
      color: "var(--fg-3)",
      fontSize: 12.5
    }
  }, "Load diff\u2026") : file.hunks.map((h, hi) => /*#__PURE__*/React.createElement("div", {
    key: hi
  }, /*#__PURE__*/React.createElement("div", {
    style: prStyles.hunkHead
  }, /*#__PURE__*/React.createElement("span", {
    className: "mono",
    style: {
      fontSize: 11.5,
      color: "var(--fg-2)"
    }
  }, h.header)), h.lines.map((ln, li) => {
    const lineKey = `${fi}-${hi}-${li}`;
    const a = anchorFor(ln);
    const inlineComments = (file.comments || []).filter(c => a && c.line === a.line && (c.side || "right") === a.side);
    const stagedHere = a ? (pending || []).filter(c => c.path === file.path && c.line === a.line && c.side === a.side) : [];
    return /*#__PURE__*/React.createElement(React.Fragment, {
      key: li
    }, /*#__PURE__*/React.createElement(DiffLine, {
      ln: ln,
      canComment: canComment && !!a,
      onComment: () => setOpenComment(lineKey)
    }), inlineComments.map((c, ci) => /*#__PURE__*/React.createElement(InlineComment, {
      key: ci,
      c: c
    })), stagedHere.map((c, ci) => /*#__PURE__*/React.createElement(PendingComment, {
      key: "p" + ci,
      body: c.body
    })), openComment === lineKey ? /*#__PURE__*/React.createElement(NewCommentBox, {
      busy: busy,
      onCancel: () => setOpenComment(null),
      onSubmit: body => submitComment(ln, body),
      onStage: body => stageComment(ln, body)
    }) : null);
  })))));
}

// ReadOnlyDiff renders a list of FileDiffs without the comment machinery —
// used by the compare (new PR) and commit-detail views.
function ReadOnlyDiff({
  files
}) {
  if (!files || files.length === 0) {
    return /*#__PURE__*/React.createElement("div", {
      className: "muted",
      style: {
        fontSize: 12.5,
        padding: "12px 4px"
      }
    }, "No file changes.");
  }
  return /*#__PURE__*/React.createElement(React.Fragment, null, files.map((file, fi) => /*#__PURE__*/React.createElement("div", {
    key: fi,
    className: "card",
    style: {
      marginBottom: 14,
      overflow: "hidden"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: prStyles.fileHead
  }, /*#__PURE__*/React.createElement("span", {
    className: "mono",
    style: {
      fontSize: 12.5,
      fontWeight: 500
    }
  }, file.path), /*#__PURE__*/React.createElement("span", {
    className: "chip",
    style: {
      height: 18
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--accent)"
    }
  }, "+", file.additions)), /*#__PURE__*/React.createElement("span", {
    className: "chip",
    style: {
      height: 18
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--danger)"
    }
  }, "\u2212", file.deletions))), /*#__PURE__*/React.createElement("div", {
    style: {
      overflowX: "auto"
    }
  }, (file.hunks || []).map((h, hi) => /*#__PURE__*/React.createElement("div", {
    key: hi
  }, /*#__PURE__*/React.createElement("div", {
    style: prStyles.hunkHead
  }, /*#__PURE__*/React.createElement("span", {
    className: "mono",
    style: {
      fontSize: 11.5,
      color: "var(--fg-2)"
    }
  }, h.header)), h.lines.map((ln, li) => /*#__PURE__*/React.createElement(DiffLine, {
    key: li,
    ln: ln,
    canComment: false
  }))))))));
}
window.ReadOnlyDiff = ReadOnlyDiff;
function DiffLine({
  ln,
  onComment,
  canComment
}) {
  const bg = ln.type === "add" ? "color-mix(in oklab, var(--accent) 10%, var(--bg))" : ln.type === "del" ? "color-mix(in oklab, var(--danger) 10%, var(--bg))" : "transparent";
  const marker = ln.type === "add" ? "+" : ln.type === "del" ? "−" : " ";
  const markerColor = ln.type === "add" ? "var(--accent)" : ln.type === "del" ? "var(--danger)" : "var(--fg-3)";
  return /*#__PURE__*/React.createElement("div", {
    style: {
      ...prStyles.diffLine,
      background: bg
    },
    className: "diff-line"
  }, /*#__PURE__*/React.createElement("span", {
    style: prStyles.diffNum
  }, ln.num[0]), /*#__PURE__*/React.createElement("span", {
    style: prStyles.diffNum
  }, ln.num[1]), canComment ? /*#__PURE__*/React.createElement("button", {
    style: prStyles.diffAdd,
    onClick: onComment,
    title: "Add a comment on this line"
  }, /*#__PURE__*/React.createElement(Icons.Plus, {
    size: 10
  })) : /*#__PURE__*/React.createElement("span", {
    style: {
      width: 18
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      width: 16,
      textAlign: "center",
      color: markerColor,
      fontFamily: "var(--font-mono)"
    }
  }, marker), /*#__PURE__*/React.createElement("code", {
    style: prStyles.diffCode
  }, ln.text));
}
function InlineComment({
  c
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: prStyles.inlineComment
  }, /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 8,
      marginBottom: 6
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "avatar",
    style: {
      background: c.author.color,
      width: 20,
      height: 20,
      fontSize: 9
    }
  }, c.author.initials), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 12.5,
      fontWeight: 500
    }
  }, c.author.name), /*#__PURE__*/React.createElement("span", {
    className: "subtle",
    style: {
      fontSize: 11.5
    }
  }, c.when), c.side ? /*#__PURE__*/React.createElement("span", {
    className: "mono subtle",
    style: {
      fontSize: 10.5
    }
  }, "\xB7 ", c.side === "left" ? "old" : "new", " L", c.line) : null), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 13,
      lineHeight: 1.5,
      color: "var(--fg-1)",
      whiteSpace: "pre-wrap"
    }
  }, c.body));
}
function NewCommentBox({
  onCancel,
  onSubmit,
  onStage,
  busy
}) {
  const [v, setV] = React.useState("");
  const submit = () => {
    if (v.trim()) onSubmit(v);
  };
  const stage = () => {
    if (v.trim()) onStage(v);
  };
  return /*#__PURE__*/React.createElement("div", {
    style: prStyles.inlineComment
  }, /*#__PURE__*/React.createElement("textarea", {
    value: v,
    onChange: e => setV(e.target.value),
    onKeyDown: e => {
      if ((e.metaKey || e.ctrlKey) && e.key === "Enter") {
        e.preventDefault();
        submit();
      }
    },
    placeholder: "Leave a comment\u2026 \u2318+Enter to comment now",
    style: {
      width: "100%",
      minHeight: 76,
      padding: "8px 10px",
      border: "1px solid var(--line)",
      borderRadius: 6,
      background: "var(--bg)",
      color: "var(--fg)",
      fontFamily: "inherit",
      fontSize: 13,
      resize: "vertical"
    },
    autoFocus: true
  }), /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 6,
      marginTop: 8
    }
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn sm",
    onClick: onCancel
  }, "Cancel"), /*#__PURE__*/React.createElement("span", {
    className: "spacer"
  }), onStage ? /*#__PURE__*/React.createElement("button", {
    className: "btn sm",
    onClick: stage,
    disabled: !v.trim() || busy,
    title: "Hold this comment and submit it together with a review verdict"
  }, "Add to review") : null, /*#__PURE__*/React.createElement("button", {
    className: "btn primary sm",
    onClick: submit,
    disabled: !v.trim() || busy
  }, busy ? "Posting…" : "Comment")));
}

// PendingComment — a staged (not-yet-submitted) line comment, part of an
// in-progress batched review.
function PendingComment({
  body
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      ...prStyles.inlineComment,
      borderLeft: "2px solid var(--accent)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 8,
      marginBottom: 4
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "chip",
    style: {
      height: 16,
      fontSize: 10,
      color: "var(--accent)"
    }
  }, "Pending"), /*#__PURE__*/React.createElement("span", {
    className: "subtle",
    style: {
      fontSize: 11
    }
  }, "in your review")), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 13,
      lineHeight: 1.5,
      color: "var(--fg-1)",
      whiteSpace: "pre-wrap"
    }
  }, body));
}
function Conversation({
  pr,
  comments,
  postComment
}) {
  const [draft, setDraft] = React.useState("");
  // Top-level comments only (no line/path) — line comments live in the diff.
  const topLevel = (comments || []).filter(c => c.line == null && !c.path);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 820,
      margin: "0 auto",
      padding: "20px 24px 40px"
    }
  }, /*#__PURE__*/React.createElement(ConvBlock, {
    author: pr.author,
    when: ""
  }, /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      lineHeight: 1.55
    }
  }, pr.description || /*#__PURE__*/React.createElement("span", {
    className: "muted"
  }, "No description provided."))), topLevel.map((c, i) => /*#__PURE__*/React.createElement(ConvBlock, {
    key: i,
    author: c.author,
    when: c.when
  }, /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      lineHeight: 1.55
    }
  }, c.body))), /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      marginTop: 18,
      padding: 14
    }
  }, /*#__PURE__*/React.createElement("textarea", {
    value: draft,
    onChange: e => setDraft(e.target.value),
    placeholder: "Leave a comment\u2026",
    style: {
      width: "100%",
      minHeight: 90,
      padding: 10,
      border: "1px solid var(--line)",
      borderRadius: 6,
      background: "var(--bg)",
      color: "var(--fg)",
      fontFamily: "inherit",
      fontSize: 13,
      resize: "vertical"
    }
  }), /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      marginTop: 10,
      gap: 6
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "muted",
    style: {
      fontSize: 11.5
    }
  }, "Supports markdown"), /*#__PURE__*/React.createElement("span", {
    className: "spacer"
  }), /*#__PURE__*/React.createElement("button", {
    className: "btn primary sm",
    disabled: !draft.trim(),
    onClick: () => {
      postComment && postComment(draft);
      setDraft("");
    }
  }, "Comment"))));
}
function ConvBlock({
  author,
  when,
  children
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      gap: 12,
      marginBottom: 16
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "avatar",
    style: {
      background: author.color,
      width: 28,
      height: 28,
      fontSize: 11,
      flexShrink: 0
    }
  }, author.initials), /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      flex: 1,
      padding: 12
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 8,
      marginBottom: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 13,
      fontWeight: 500
    }
  }, author.name), /*#__PURE__*/React.createElement("span", {
    className: "subtle",
    style: {
      fontSize: 11.5
    }
  }, when)), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 13.5,
      color: "var(--fg-1)"
    }
  }, children)));
}
function CommitsList({
  commits,
  repo,
  setRoute
}) {
  // Real commits: {sha, short, message, author, email, date}. Fallback to mock when null (offline).
  const mock = [{
    short: "8c1bba0",
    message: "router: log a warning when fallback is used",
    author: "Jana",
    date: ""
  }, {
    short: "0aa9ee4",
    message: "router: switch fallback sink to bounded channel",
    author: "Jana",
    date: ""
  }];
  const list = commits != null ? commits : mock;
  const open = c => {
    if (setRoute && repo && c.sha) setRoute({
      view: "commit",
      repo,
      sha: c.sha
    });
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 820,
      margin: "0 auto",
      padding: "20px 24px"
    }
  }, list.length === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      padding: "20px",
      color: "var(--fg-3)",
      fontSize: 13
    }
  }, "No commits ahead of base.") : /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      padding: 4
    }
  }, list.map((c, i) => /*#__PURE__*/React.createElement("button", {
    key: c.sha || i,
    onClick: () => open(c),
    style: {
      display: "flex",
      alignItems: "center",
      gap: 12,
      width: "100%",
      textAlign: "left",
      padding: "10px 14px",
      borderBottom: i < list.length - 1 ? "1px solid var(--line)" : "none",
      background: "transparent",
      border: "none",
      cursor: c.sha ? "pointer" : "default",
      font: "inherit",
      color: "inherit"
    }
  }, /*#__PURE__*/React.createElement(Icons.Commit, {
    size: 14,
    style: {
      color: "var(--fg-3)"
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 13,
      fontWeight: 450,
      overflow: "hidden",
      textOverflow: "ellipsis",
      whiteSpace: "nowrap"
    }
  }, (c.message || "").split("\n")[0]), /*#__PURE__*/React.createElement("div", {
    className: "subtle",
    style: {
      fontSize: 11.5,
      marginTop: 2
    }
  }, c.author, c.date ? " · " + (c.date || "").slice(0, 10) : "")), /*#__PURE__*/React.createElement("span", {
    className: "mono chip"
  }, c.short || (c.sha || "").slice(0, 7))))));
}
function ChecksList({
  checks
}) {
  // Real checks: {name, status, detail, externalUrl}. Fallback to mock when null.
  const mock = [{
    name: "ci / build",
    status: "ok",
    detail: ""
  }, {
    name: "ci / test",
    status: "ok",
    detail: ""
  }];
  const list = checks != null ? checks : mock;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 820,
      margin: "0 auto",
      padding: "20px 24px"
    }
  }, list.length === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      padding: "20px",
      color: "var(--fg-3)",
      fontSize: 13
    }
  }, "No checks have run on the latest commit yet.") : /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      padding: 4
    }
  }, list.map((c, i) => /*#__PURE__*/React.createElement("div", {
    key: c.name,
    style: {
      display: "flex",
      alignItems: "flex-start",
      gap: 12,
      padding: "10px 14px",
      borderBottom: i < list.length - 1 ? "1px solid var(--line)" : "none"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 8,
      height: 8,
      borderRadius: 999,
      marginTop: 5,
      flexShrink: 0,
      background: c.status === "ok" ? "var(--accent)" : c.status === "fail" ? "var(--danger)" : "var(--warn)",
      boxShadow: c.status === "pending" ? "0 0 0 3px color-mix(in oklab, var(--warn) 25%, transparent)" : "none"
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 13
    }
  }, c.name, " ", /*#__PURE__*/React.createElement("span", {
    className: "subtle",
    style: {
      fontSize: 11
    }
  }, "\xB7 ", c.status)), c.detail ? /*#__PURE__*/React.createElement("div", {
    className: "subtle",
    style: {
      fontSize: 11.5,
      marginTop: 3,
      whiteSpace: "pre-wrap"
    }
  }, c.detail) : null), c.externalUrl ? /*#__PURE__*/React.createElement("a", {
    className: "btn ghost sm",
    href: c.externalUrl,
    target: "_blank",
    rel: "noreferrer"
  }, "Details") : null))));
}
function ReviewBar({
  body,
  setBody,
  verdict,
  setVerdict,
  onCancel,
  onSubmit,
  pendingCount = 0
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: prStyles.reviewBar
  }, /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 8,
      marginBottom: 8
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontWeight: 500,
      fontSize: 13
    }
  }, "Submit review"), pendingCount > 0 ? /*#__PURE__*/React.createElement("span", {
    className: "chip accent",
    style: {
      height: 18,
      fontSize: 10.5
    }
  }, pendingCount, " line comment", pendingCount === 1 ? "" : "s") : null, /*#__PURE__*/React.createElement("span", {
    className: "spacer"
  }), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost icon sm",
    onClick: onCancel
  }, /*#__PURE__*/React.createElement(Icons.Close, {
    size: 11
  }))), /*#__PURE__*/React.createElement("textarea", {
    value: body,
    onChange: e => setBody(e.target.value),
    placeholder: "Summary of your review (optional)",
    style: {
      width: "100%",
      minHeight: 80,
      padding: 10,
      border: "1px solid var(--line)",
      borderRadius: 6,
      background: "var(--bg)",
      color: "var(--fg)",
      fontFamily: "inherit",
      fontSize: 13,
      resize: "vertical"
    }
  }), /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 10,
      marginTop: 10
    }
  }, /*#__PURE__*/React.createElement("label", {
    className: "row",
    style: {
      gap: 6,
      cursor: "pointer"
    }
  }, /*#__PURE__*/React.createElement("input", {
    type: "radio",
    checked: verdict === "comment",
    onChange: () => setVerdict("comment")
  }), " Comment"), /*#__PURE__*/React.createElement("label", {
    className: "row",
    style: {
      gap: 6,
      cursor: "pointer"
    }
  }, /*#__PURE__*/React.createElement("input", {
    type: "radio",
    checked: verdict === "approve",
    onChange: () => setVerdict("approve")
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--accent)"
    }
  }, "Approve")), /*#__PURE__*/React.createElement("label", {
    className: "row",
    style: {
      gap: 6,
      cursor: "pointer"
    }
  }, /*#__PURE__*/React.createElement("input", {
    type: "radio",
    checked: verdict === "changes",
    onChange: () => setVerdict("changes")
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--danger)"
    }
  }, "Request changes")), /*#__PURE__*/React.createElement("span", {
    className: "spacer"
  }), /*#__PURE__*/React.createElement("button", {
    className: "btn primary sm",
    onClick: onSubmit
  }, "Submit review", pendingCount > 0 ? " (" + pendingCount + ")" : "")));
}
const prStyles = {
  shell: {
    display: "flex",
    flexDirection: "column",
    height: "100%",
    overflow: "hidden"
  },
  header: {
    display: "flex",
    alignItems: "flex-start",
    gap: 12,
    padding: "16px 24px",
    borderBottom: "1px solid var(--line)",
    background: "var(--bg-1)"
  },
  title: {
    fontSize: 22,
    fontWeight: 500,
    letterSpacing: "-0.015em",
    margin: 0,
    lineHeight: 1.25
  },
  statusStrip: {
    display: "flex",
    alignItems: "center",
    gap: 22,
    padding: "10px 24px",
    borderBottom: "1px solid var(--line)",
    background: "var(--bg-1)",
    flexWrap: "wrap"
  },
  tabs: {
    display: "flex",
    alignItems: "center",
    gap: 2,
    padding: "0 16px",
    borderBottom: "1px solid var(--line)",
    background: "var(--bg-1)",
    height: 40
  },
  body: {
    flex: 1,
    overflowY: "auto"
  },
  fileHead: {
    display: "flex",
    alignItems: "center",
    gap: 10,
    padding: "10px 14px",
    background: "var(--bg-2)",
    borderBottom: "1px solid var(--line)",
    cursor: "pointer"
  },
  hunkHead: {
    padding: "6px 14px",
    background: "color-mix(in oklab, var(--info) 6%, var(--bg))",
    color: "var(--fg-2)",
    borderBottom: "1px solid var(--line)"
  },
  diffLine: {
    display: "flex",
    alignItems: "stretch",
    fontFamily: "var(--font-mono)",
    fontSize: 12.5,
    lineHeight: 1.6,
    position: "relative"
  },
  diffNum: {
    display: "inline-block",
    width: 50,
    paddingRight: 10,
    textAlign: "right",
    color: "var(--fg-3)",
    fontSize: 11,
    userSelect: "none",
    flexShrink: 0,
    borderRight: "1px solid var(--line)"
  },
  diffAdd: {
    width: 16,
    height: 18,
    marginTop: 1,
    border: "none",
    background: "var(--accent)",
    color: "var(--accent-fg)",
    display: "inline-flex",
    alignItems: "center",
    justifyContent: "center",
    cursor: "pointer",
    opacity: 0,
    borderRadius: 3,
    flexShrink: 0,
    marginLeft: 4,
    marginRight: 2,
    alignSelf: "center",
    transition: "opacity 80ms"
  },
  diffCode: {
    whiteSpace: "pre",
    padding: "0 12px",
    color: "var(--fg-1)",
    flex: 1
  },
  inlineComment: {
    padding: "12px 16px 14px 70px",
    background: "var(--bg-1)",
    borderTop: "1px solid var(--line)",
    borderBottom: "1px solid var(--line)"
  },
  reviewBar: {
    position: "absolute",
    right: 24,
    bottom: 24,
    width: 420,
    background: "var(--bg-1)",
    border: "1px solid var(--line-strong)",
    borderRadius: 12,
    boxShadow: "var(--shadow-lg)",
    padding: 14,
    zIndex: 50
  }
};

// Hover state for diff add button
const prStyleTag = document.createElement("style");
prStyleTag.textContent = `
.diff-line:hover button { opacity: 0.7 !important; }
.diff-line button:hover { opacity: 1 !important; }
`;
document.head.appendChild(prStyleTag);
window.PRView = PRView;

// PR list view — sidebar 'pull requests'
function PRsView({
  setRoute
}) {
  const [filter, setFilter] = React.useState("review-requested");
  const [pulls, setPulls] = React.useState(null);
  React.useEffect(() => {
    if (window.OrchisAPI) {
      window.OrchisAPI.get("/v1/pulls?filter=" + encodeURIComponent(filter)).then(setPulls).catch(() => setPulls([]));
    }
  }, [filter]);
  const list = pulls != null ? pulls : window.OrchisAPI ? [] : PRS;
  return /*#__PURE__*/React.createElement("div", {
    style: {
      overflowY: "auto",
      height: "100%"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 1100,
      margin: "0 auto",
      padding: "32px 32px 60px"
    },
    className: "fade-in"
  }, /*#__PURE__*/React.createElement("div", {
    className: "row"
  }, /*#__PURE__*/React.createElement("h1", {
    style: {
      fontSize: 26,
      fontWeight: 500,
      letterSpacing: "-0.02em",
      margin: "0 0 6px"
    }
  }, "Pull requests"), /*#__PURE__*/React.createElement("span", {
    className: "spacer"
  }), /*#__PURE__*/React.createElement("button", {
    className: "btn primary sm",
    onClick: () => setRoute({
      view: "newpr"
    })
  }, /*#__PURE__*/React.createElement(Icons.Plus, {
    size: 13
  }), " New pull request")), /*#__PURE__*/React.createElement("p", {
    className: "muted",
    style: {
      marginTop: 0,
      fontSize: 14
    }
  }, "Across all your repos."), /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 6,
      margin: "20px 0 14px",
      flexWrap: "wrap"
    }
  }, [{
    id: "review-requested",
    label: "Review requested"
  }, {
    id: "yours",
    label: "Yours"
  }, {
    id: "mentioned",
    label: "Mentioned"
  }, {
    id: "all",
    label: "All open"
  }].map(f => /*#__PURE__*/React.createElement("button", {
    key: f.id,
    onClick: () => setFilter(f.id),
    className: "btn sm",
    style: filter === f.id ? {
      background: "var(--accent-soft)",
      color: "var(--accent)",
      borderColor: "var(--accent-line)"
    } : {}
  }, f.label))), list.length === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      padding: "24px 18px",
      color: "var(--fg-3)",
      fontSize: 13.5
    }
  }, "No pull requests here yet. Push a branch and open one with ", /*#__PURE__*/React.createElement("span", {
    className: "mono"
  }, "POST /v1/repos/<you>/<repo>/pulls"), ".") : /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      overflow: "hidden"
    }
  }, list.map(p => /*#__PURE__*/React.createElement("button", {
    key: p.repo + "#" + p.id,
    onClick: () => setRoute({
      view: "pr",
      pr: p.id,
      repo: p.repo
    }),
    style: {
      display: "flex",
      alignItems: "center",
      gap: 12,
      padding: "14px 16px",
      background: "transparent",
      border: "none",
      borderBottom: "1px solid var(--line)",
      width: "100%",
      textAlign: "left",
      cursor: "pointer",
      font: "inherit",
      color: "inherit"
    }
  }, /*#__PURE__*/React.createElement(Icons.PR, {
    size: 15,
    style: {
      color: "var(--accent)",
      flexShrink: 0
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 14,
      fontWeight: 450,
      marginBottom: 3
    }
  }, p.title), /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 10,
      color: "var(--fg-2)",
      fontSize: 11.5,
      flexWrap: "wrap"
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "mono"
  }, p.repo, " #", p.id), /*#__PURE__*/React.createElement("span", null, "by ", p.author.name), /*#__PURE__*/React.createElement("span", null, "updated ", p.updated), (p.labels || []).map(l => /*#__PURE__*/React.createElement("span", {
    key: l.name,
    className: "chip " + l.color
  }, l.name)))), /*#__PURE__*/React.createElement("span", {
    className: "row",
    style: {
      gap: -4
    }
  }, (p.reviewers || []).map((r, i) => /*#__PURE__*/React.createElement("span", {
    key: r.id,
    className: "avatar",
    style: {
      background: r.color,
      width: 20,
      height: 20,
      fontSize: 9,
      marginLeft: i ? -6 : 0,
      border: "2px solid var(--bg-1)"
    }
  }, r.initials))), /*#__PURE__*/React.createElement("span", {
    className: "chip",
    style: {
      height: 20
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--accent)"
    }
  }, "+", p.additions), " ", /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--danger)"
    }
  }, "\u2212", p.deletions)))))));
}
window.PRsView = PRsView;

// ===== src/views/devsettings.jsx =====
// Developer settings — PATs, SSH keys, webhooks. Flat & fast.
function DevSettingsView({
  route,
  setRoute
}) {
  const [tab, setTab] = React.useState(route.tab || "tokens");
  const [newToken, setNewToken] = React.useState(route.newToken || false);
  const [newKey, setNewKey] = React.useState(route.newKey || false);
  const [newHook, setNewHook] = React.useState(route.newHook || false);
  const [generatedToken, setGeneratedToken] = React.useState(null);
  React.useEffect(() => {
    if (route.tab) setTab(route.tab);
    if (route.newToken) setNewToken(true);
    if (route.newKey) setNewKey(true);
    if (route.newHook) setNewHook(true);
  }, [route]);
  const tabs = [{
    id: "tokens",
    label: "Tokens",
    icon: /*#__PURE__*/React.createElement(Icons.Key, null)
  }, {
    id: "ssh",
    label: "SSH keys",
    icon: /*#__PURE__*/React.createElement(Icons.Key, null)
  }, {
    id: "scanner",
    label: "Scanner",
    icon: /*#__PURE__*/React.createElement(Icons.Bolt, null)
  }, {
    id: "webhooks",
    label: "Webhooks",
    icon: /*#__PURE__*/React.createElement(Icons.Webhook, null)
  }, {
    id: "apps",
    label: "OAuth apps",
    icon: /*#__PURE__*/React.createElement(Icons.Bolt, null)
  }, {
    id: "preferences",
    label: "Preferences",
    icon: /*#__PURE__*/React.createElement(Icons.Settings, null)
  }];
  return /*#__PURE__*/React.createElement("div", {
    style: {
      overflowY: "auto",
      height: "100%"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: dsStyles.page,
    className: "fade-in"
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: 24
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 11.5,
      color: "var(--fg-3)",
      textTransform: "uppercase",
      letterSpacing: "0.06em",
      marginBottom: 6
    }
  }, "Developer settings"), /*#__PURE__*/React.createElement("h1", {
    style: {
      fontSize: 26,
      fontWeight: 500,
      letterSpacing: "-0.02em",
      margin: 0
    }
  }, "Your dev surface"), /*#__PURE__*/React.createElement("p", {
    className: "muted",
    style: {
      margin: "6px 0 0",
      fontSize: 14,
      maxWidth: 540
    }
  }, "Tokens, SSH keys, and webhooks \u2014 flat. No more digging four levels deep to rotate a PAT.")), /*#__PURE__*/React.createElement("div", {
    style: dsStyles.layout
  }, /*#__PURE__*/React.createElement("nav", {
    style: dsStyles.sideTabs
  }, tabs.map(t => /*#__PURE__*/React.createElement("button", {
    key: t.id,
    onClick: () => setTab(t.id),
    style: {
      ...dsStyles.sideTab,
      ...(tab === t.id ? dsStyles.sideTabActive : {})
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 16,
      color: tab === t.id ? "var(--accent)" : "var(--fg-2)"
    }
  }, t.icon), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      textAlign: "left"
    }
  }, t.label), t.count != null ? /*#__PURE__*/React.createElement("span", {
    className: "chip",
    style: {
      height: 18,
      fontSize: 10.5
    }
  }, t.count) : null))), /*#__PURE__*/React.createElement("div", {
    style: dsStyles.main
  }, tab === "tokens" ? /*#__PURE__*/React.createElement(TokensPanel, {
    newToken: newToken,
    setNewToken: setNewToken,
    generated: generatedToken,
    setGenerated: setGeneratedToken
  }) : null, tab === "ssh" ? /*#__PURE__*/React.createElement(SSHPanel, {
    newKey: newKey,
    setNewKey: setNewKey
  }) : null, tab === "scanner" ? /*#__PURE__*/React.createElement(ScannerPanel, null) : null, tab === "webhooks" ? /*#__PURE__*/React.createElement(WebhooksPanel, {
    newHook: newHook,
    setNewHook: setNewHook
  }) : null, tab === "apps" ? /*#__PURE__*/React.createElement(AppsPanel, null) : null, tab === "preferences" ? /*#__PURE__*/React.createElement(PreferencesPanel, null) : null))));
}
function TokensPanel({
  newToken,
  setNewToken,
  generated,
  setGenerated
}) {
  if (generated) {
    return /*#__PURE__*/React.createElement("div", {
      className: "fade-in"
    }, /*#__PURE__*/React.createElement("div", {
      className: "card",
      style: {
        padding: 20,
        borderColor: "var(--accent-line)",
        background: "var(--accent-soft)"
      }
    }, /*#__PURE__*/React.createElement("div", {
      className: "row",
      style: {
        gap: 8,
        marginBottom: 10
      }
    }, /*#__PURE__*/React.createElement(Icons.Check, {
      size: 14,
      style: {
        color: "var(--accent)"
      }
    }), /*#__PURE__*/React.createElement("span", {
      style: {
        fontWeight: 500,
        color: "var(--accent)"
      }
    }, "Token created \u2014 copy it now.")), /*#__PURE__*/React.createElement("p", {
      className: "muted",
      style: {
        margin: "0 0 12px",
        fontSize: 13
      }
    }, "You won't be able to see this token again. It's only shown here."), /*#__PURE__*/React.createElement("div", {
      style: {
        display: "flex",
        alignItems: "center",
        gap: 10,
        padding: "12px 14px",
        borderRadius: 8,
        background: "var(--bg)",
        border: "1px solid var(--line-strong)",
        fontFamily: "var(--font-mono)",
        fontSize: 13
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        flex: 1,
        overflow: "hidden",
        textOverflow: "ellipsis",
        whiteSpace: "nowrap"
      }
    }, generated.value), /*#__PURE__*/React.createElement("button", {
      className: "btn sm",
      onClick: () => navigator.clipboard?.writeText(generated.value).catch(() => {})
    }, /*#__PURE__*/React.createElement(Icons.Copy, {
      size: 12
    }), " Copy")), /*#__PURE__*/React.createElement("button", {
      className: "btn primary sm",
      style: {
        marginTop: 14
      },
      onClick: () => setGenerated(null)
    }, "I've saved it")));
  }

  // Live token list. Reloads when we return from the "generated" view (i.e.
  // after creating/rotating a token). Falls back to the mock when offline.
  const [tokens, setTokens] = React.useState(null);
  const reload = React.useCallback(() => {
    if (window.OrchisAPI) {
      window.OrchisAPI.get("/v1/me/tokens").then(setTokens).catch(() => setTokens([]));
    }
  }, []);
  React.useEffect(() => {
    if (!generated) reload();
  }, [generated, reload]);
  const list = tokens != null ? tokens : window.OrchisAPI ? [] : TOKENS;
  const revoke = async id => {
    try {
      await window.OrchisAPI.del("/v1/me/tokens/" + id);
      reload();
    } catch (e) {}
  };
  const rotate = async id => {
    try {
      const created = await window.OrchisAPI.post("/v1/me/tokens/" + id + "/rotate", {});
      setGenerated({
        name: created.name,
        value: created.token
      });
    } catch (e) {}
  };
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    style: dsStyles.head
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("h2", {
    style: dsStyles.h2
  }, "Personal access tokens"), /*#__PURE__*/React.createElement("p", {
    className: "muted",
    style: dsStyles.subtitle
  }, "Use these for git over HTTPS, CI pipelines, and API access.")), /*#__PURE__*/React.createElement("button", {
    className: "btn primary",
    onClick: () => setNewToken(true)
  }, /*#__PURE__*/React.createElement(Icons.Plus, {
    size: 14
  }), " New token")), newToken ? /*#__PURE__*/React.createElement(NewTokenForm, {
    onCancel: () => setNewToken(false),
    onCreate: t => {
      setNewToken(false);
      setGenerated(t);
    }
  }) : null, list.length === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      marginTop: 14,
      padding: "20px 18px",
      color: "var(--fg-3)",
      fontSize: 13
    }
  }, "No tokens yet. Create one to push over HTTPS or hit the API.") : /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      marginTop: 14
    }
  }, list.map((t, i) => /*#__PURE__*/React.createElement("div", {
    key: t.id,
    style: {
      display: "flex",
      alignItems: "center",
      gap: 16,
      padding: "14px 18px",
      borderBottom: i < list.length - 1 ? "1px solid var(--line)" : "none",
      opacity: t.expired ? 0.55 : 1
    }
  }, /*#__PURE__*/React.createElement(Icons.Key, {
    size: 16,
    style: {
      color: t.expiringSoon ? "var(--warn)" : t.expired ? "var(--fg-3)" : "var(--fg-2)"
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 8,
      marginBottom: 4
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 14,
      fontWeight: 500
    }
  }, t.name), t.expiringSoon ? /*#__PURE__*/React.createElement("span", {
    className: "chip warn"
  }, "expires ", t.expires) : null, t.expired ? /*#__PURE__*/React.createElement("span", {
    className: "chip"
  }, "expired") : null), /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 6,
      color: "var(--fg-2)",
      fontSize: 11.5,
      flexWrap: "wrap"
    }
  }, (t.scopes || []).map(s => /*#__PURE__*/React.createElement("span", {
    key: s,
    className: "mono chip"
  }, s))), /*#__PURE__*/React.createElement("div", {
    className: "subtle",
    style: {
      fontSize: 11.5,
      marginTop: 6
    }
  }, "Created ", t.created || "—", " \xB7 Last used ", t.lastUsed || "never", t.expires ? ` · Expires ${t.expires}` : "")), /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 6
    }
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn sm",
    onClick: () => rotate(t.id)
  }, "Rotate"), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    style: {
      color: "var(--danger)"
    },
    onClick: () => revoke(t.id)
  }, "Revoke"))))), /*#__PURE__*/React.createElement("p", {
    className: "subtle",
    style: {
      marginTop: 14,
      fontSize: 12
    }
  }, /*#__PURE__*/React.createElement(Icons.Bolt, {
    size: 11,
    style: {
      verticalAlign: "-1px",
      marginRight: 4
    }
  }), "Tip: ", /*#__PURE__*/React.createElement("span", {
    className: "kbd"
  }, "\u2318K"), " \u2192 \"create token\" \u2014 anywhere in Orchis."));
}
function NewTokenForm({
  onCancel,
  onCreate
}) {
  const [name, setName] = React.useState("");
  const [expires, setExpires] = React.useState("90");
  const [scopes, setScopes] = React.useState({
    "repo:read": true
  });
  const [busy, setBusy] = React.useState(false);
  const [err, setErr] = React.useState("");
  const submit = async () => {
    setErr("");
    setBusy(true);
    const selected = Object.keys(scopes).filter(k => scopes[k]);
    const expires_in_days = expires === "never" ? 0 : parseInt(expires, 10);
    try {
      if (window.OrchisAPI) {
        const created = await window.OrchisAPI.post("/v1/me/tokens", {
          name: name.trim(),
          scopes: selected,
          expires_in_days
        });
        onCreate({
          name: created.name,
          value: created.token
        });
      } else {
        onCreate({
          name,
          value: "orc_pat_demo_offline_token_value"
        });
      }
    } catch (e) {
      setErr("Could not create token. Try again.");
      setBusy(false);
    }
  };
  const SCOPE_GROUPS = [{
    label: "Repositories",
    scopes: [{
      id: "repo:read",
      desc: "Read code, issues, PRs"
    }, {
      id: "repo:write",
      desc: "Push, comment, merge"
    }, {
      id: "repo:admin",
      desc: "Manage settings, webhooks"
    }]
  }, {
    label: "Actions & packages",
    scopes: [{
      id: "actions:read",
      desc: "Read CI runs and logs"
    }, {
      id: "packages:write",
      desc: "Publish packages"
    }]
  }, {
    label: "Account",
    scopes: [{
      id: "user:read",
      desc: "Profile, email, orgs"
    }]
  }];
  return /*#__PURE__*/React.createElement("div", {
    className: "card fade-in",
    style: {
      padding: 18,
      marginTop: 14,
      borderColor: "var(--accent-line)"
    }
  }, /*#__PURE__*/React.createElement("h3", {
    style: {
      fontSize: 15,
      fontWeight: 500,
      margin: "0 0 14px"
    }
  }, "New personal access token"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "1fr 220px",
      gap: 14,
      marginBottom: 16
    }
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Name",
    hint: "What is this token for?"
  }, /*#__PURE__*/React.createElement("input", {
    className: "input",
    value: name,
    onChange: e => setName(e.target.value),
    placeholder: "e.g. macbook \u2014 atlas dev",
    autoFocus: true
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Expires"
  }, /*#__PURE__*/React.createElement("select", {
    className: "input",
    value: expires,
    onChange: e => setExpires(e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: "30"
  }, "30 days"), /*#__PURE__*/React.createElement("option", {
    value: "60"
  }, "60 days"), /*#__PURE__*/React.createElement("option", {
    value: "90"
  }, "90 days"), /*#__PURE__*/React.createElement("option", {
    value: "365"
  }, "1 year"), /*#__PURE__*/React.createElement("option", {
    value: "never"
  }, "No expiration (not recommended)")))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginBottom: 14
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "section-title",
    style: {
      marginBottom: 8
    }
  }, "Scopes"), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 14
    }
  }, SCOPE_GROUPS.map(g => /*#__PURE__*/React.createElement("div", {
    key: g.label
  }, /*#__PURE__*/React.createElement("div", {
    className: "muted",
    style: {
      fontSize: 11.5,
      marginBottom: 6
    }
  }, g.label), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "1fr 1fr",
      gap: 6
    }
  }, g.scopes.map(s => /*#__PURE__*/React.createElement("label", {
    key: s.id,
    className: "row",
    style: {
      gap: 10,
      padding: "8px 10px",
      border: "1px solid var(--line)",
      borderRadius: 6,
      cursor: "pointer",
      background: scopes[s.id] ? "var(--accent-soft)" : "var(--bg-1)",
      borderColor: scopes[s.id] ? "var(--accent-line)" : "var(--line)"
    }
  }, /*#__PURE__*/React.createElement("input", {
    type: "checkbox",
    checked: !!scopes[s.id],
    onChange: () => setScopes(p => ({
      ...p,
      [s.id]: !p[s.id]
    }))
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "mono",
    style: {
      fontSize: 12.5
    }
  }, s.id), /*#__PURE__*/React.createElement("div", {
    className: "subtle",
    style: {
      fontSize: 11
    }
  }, s.desc))))))))), err ? /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "8px 12px",
      border: "1px solid var(--danger)",
      borderRadius: 6,
      color: "var(--danger)",
      fontSize: 12.5,
      marginBottom: 12
    }
  }, err) : null, /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 8
    }
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn",
    onClick: onCancel
  }, "Cancel"), /*#__PURE__*/React.createElement("span", {
    className: "spacer"
  }), /*#__PURE__*/React.createElement("button", {
    className: "btn primary",
    disabled: !name.trim() || busy,
    onClick: submit
  }, busy ? "Generating…" : "Generate token")));
}
function SSHPanel({
  newKey,
  setNewKey
}) {
  const [name, setName] = React.useState("");
  const [body, setBody] = React.useState("");
  const [busy, setBusy] = React.useState(false);
  const [err, setErr] = React.useState("");
  const [keys, setKeys] = React.useState(null);
  const reload = React.useCallback(() => {
    if (window.OrchisAPI) {
      window.OrchisAPI.get("/v1/me/ssh-keys").then(setKeys).catch(() => setKeys([]));
    }
  }, []);
  React.useEffect(() => {
    reload();
  }, [reload]);
  const list = keys != null ? keys : window.OrchisAPI ? [] : SSH_KEYS;
  const add = async () => {
    setErr("");
    setBusy(true);
    try {
      await window.OrchisAPI.post("/v1/me/ssh-keys", {
        name: name.trim(),
        key: body.trim()
      });
      setName("");
      setBody("");
      setNewKey(false);
      reload();
    } catch (e) {
      setErr("Could not add key — check it's a valid public key and isn't already registered.");
    } finally {
      setBusy(false);
    }
  };
  const remove = async id => {
    try {
      await window.OrchisAPI.del("/v1/me/ssh-keys/" + id);
      reload();
    } catch (e) {}
  };
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    style: dsStyles.head
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("h2", {
    style: dsStyles.h2
  }, "SSH keys"), /*#__PURE__*/React.createElement("p", {
    className: "muted",
    style: dsStyles.subtitle
  }, "For pushing and pulling over SSH \u2014 one paste, you're set. Clone with ", /*#__PURE__*/React.createElement("span", {
    className: "mono"
  }, "ssh://git@foundry.orchis.ai:2222/<you>/<repo>.git"))), /*#__PURE__*/React.createElement("button", {
    className: "btn primary",
    onClick: () => setNewKey(true)
  }, /*#__PURE__*/React.createElement(Icons.Plus, {
    size: 14
  }), " Add SSH key")), newKey ? /*#__PURE__*/React.createElement("div", {
    className: "card fade-in",
    style: {
      padding: 18,
      marginTop: 14
    }
  }, /*#__PURE__*/React.createElement("h3", {
    style: {
      fontSize: 15,
      fontWeight: 500,
      margin: "0 0 12px"
    }
  }, "Add SSH key"), err ? /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "8px 12px",
      border: "1px solid var(--danger)",
      borderRadius: 6,
      color: "var(--danger)",
      fontSize: 12.5,
      marginBottom: 12
    }
  }, err) : null, /*#__PURE__*/React.createElement(Field, {
    label: "Title",
    hint: "What machine is this?"
  }, /*#__PURE__*/React.createElement("input", {
    className: "input",
    value: name,
    onChange: e => setName(e.target.value),
    placeholder: "e.g. macbook-air",
    autoFocus: true
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      height: 12
    }
  }), /*#__PURE__*/React.createElement(Field, {
    label: "Key",
    hint: "Paste your public key (begins with ssh-ed25519 or ssh-rsa)"
  }, /*#__PURE__*/React.createElement("textarea", {
    value: body,
    onChange: e => setBody(e.target.value),
    placeholder: "ssh-ed25519 AAAAC3Nz\u2026",
    style: {
      width: "100%",
      height: 120,
      padding: 10,
      border: "1px solid var(--line)",
      borderRadius: 6,
      fontFamily: "var(--font-mono)",
      fontSize: 12.5,
      background: "var(--bg-1)",
      color: "var(--fg)",
      resize: "vertical"
    }
  })), /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 8,
      marginTop: 14
    }
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn",
    onClick: () => {
      setNewKey(false);
      setErr("");
    }
  }, "Cancel"), /*#__PURE__*/React.createElement("span", {
    className: "spacer"
  }), /*#__PURE__*/React.createElement("button", {
    className: "btn primary",
    onClick: add,
    disabled: !name || !body || busy
  }, busy ? "Adding…" : "Add key"))) : null, list.length === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      marginTop: 14,
      padding: "20px 18px",
      color: "var(--fg-3)",
      fontSize: 13
    }
  }, "No SSH keys yet. Add one to push over SSH.") : /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      marginTop: 14
    }
  }, list.map((k, i) => /*#__PURE__*/React.createElement("div", {
    key: k.id,
    style: {
      display: "flex",
      alignItems: "center",
      gap: 16,
      padding: "14px 18px",
      borderBottom: i < list.length - 1 ? "1px solid var(--line)" : "none"
    }
  }, /*#__PURE__*/React.createElement(Icons.Key, {
    size: 16,
    style: {
      color: "var(--fg-2)"
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 14,
      fontWeight: 500,
      marginBottom: 4
    }
  }, k.name), /*#__PURE__*/React.createElement("div", {
    className: "mono subtle",
    style: {
      fontSize: 11.5,
      overflow: "hidden",
      textOverflow: "ellipsis"
    }
  }, k.fingerprint), /*#__PURE__*/React.createElement("div", {
    className: "subtle",
    style: {
      fontSize: 11.5,
      marginTop: 4
    }
  }, "Added ", k.created || k.added || "—", " \xB7 Last used ", k.lastUsed || "never")), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    style: {
      color: "var(--danger)"
    },
    onClick: () => remove(k.id)
  }, "Remove")))));
}
function WebhooksPanel({
  newHook,
  setNewHook
}) {
  const EVENTS = ["push", "pull_request", "issue", "release", "deploy", "comment"];
  const ownedRepos = REPOS.filter(r => r.org === USERS.me.handle);
  const [hooks, setHooks] = React.useState(null);
  const [url, setUrl] = React.useState("");
  const [repo, setRepo] = React.useState(ownedRepos[0] ? ownedRepos[0].id : "");
  const [events, setEvents] = React.useState(["push"]);
  const [secret, setSecret] = React.useState("");
  const [busy, setBusy] = React.useState(false);
  const [err, setErr] = React.useState("");
  const [tested, setTested] = React.useState("");
  const reload = React.useCallback(() => {
    if (window.OrchisAPI) window.OrchisAPI.get("/v1/me/webhooks").then(setHooks).catch(() => setHooks([]));
  }, []);
  React.useEffect(() => {
    reload();
  }, [reload]);
  React.useEffect(() => {
    if (!repo && ownedRepos[0]) setRepo(ownedRepos[0].id);
  }, [ownedRepos.length]);
  const list = hooks != null ? hooks : window.OrchisAPI ? [] : WEBHOOKS;
  const toggleEvent = e => setEvents(evs => evs.includes(e) ? evs.filter(x => x !== e) : [...evs, e]);
  const add = async () => {
    setErr("");
    if (!repo) {
      setErr("Create a repo first — webhooks attach to a repo.");
      return;
    }
    if (events.length === 0) {
      setErr("Pick at least one event.");
      return;
    }
    setBusy(true);
    try {
      await window.OrchisAPI.post("/v1/repos/" + repo + "/webhooks", {
        url: url.trim(),
        events,
        secret: secret.trim()
      });
      setUrl("");
      setSecret("");
      setEvents(["push"]);
      setNewHook(false);
      reload();
    } catch (e) {
      setErr("Could not create webhook — check the URL is http(s) and you own the repo.");
    } finally {
      setBusy(false);
    }
  };
  const test = async w => {
    setTested("");
    try {
      await window.OrchisAPI.post("/v1/repos/" + w.repo + "/webhooks/" + w.id + "/test");
      setTested(w.id);
      setTimeout(() => setTested(""), 2500);
    } catch (e) {}
  };
  const remove = async w => {
    try {
      await window.OrchisAPI.del("/v1/repos/" + w.repo + "/webhooks/" + w.id);
      reload();
    } catch (e) {}
  };
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    style: dsStyles.head
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("h2", {
    style: dsStyles.h2
  }, "Webhooks"), /*#__PURE__*/React.createElement("p", {
    className: "muted",
    style: dsStyles.subtitle
  }, "Get a signed POST when something happens. Across all your repos in one place. Each delivery carries ", /*#__PURE__*/React.createElement("span", {
    className: "mono"
  }, "X-Orchis-Event"), ", ", /*#__PURE__*/React.createElement("span", {
    className: "mono"
  }, "X-Orchis-Delivery"), ", and (if a secret is set) an ", /*#__PURE__*/React.createElement("span", {
    className: "mono"
  }, "X-Orchis-Signature"), " HMAC.")), /*#__PURE__*/React.createElement("button", {
    className: "btn primary",
    onClick: () => {
      setErr("");
      setNewHook(true);
    }
  }, /*#__PURE__*/React.createElement(Icons.Plus, {
    size: 14
  }), " New webhook")), newHook ? /*#__PURE__*/React.createElement("div", {
    className: "card fade-in",
    style: {
      padding: 18,
      marginTop: 14
    }
  }, /*#__PURE__*/React.createElement("h3", {
    style: {
      fontSize: 15,
      fontWeight: 500,
      margin: "0 0 12px"
    }
  }, "New webhook"), err ? /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "8px 12px",
      border: "1px solid var(--danger)",
      borderRadius: 6,
      color: "var(--danger)",
      fontSize: 12.5,
      marginBottom: 12
    }
  }, err) : null, /*#__PURE__*/React.createElement(Field, {
    label: "Payload URL"
  }, /*#__PURE__*/React.createElement("input", {
    className: "input",
    value: url,
    onChange: e => setUrl(e.target.value),
    placeholder: "https://hooks.example.com/orchis",
    autoFocus: true
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      height: 12
    }
  }), /*#__PURE__*/React.createElement(Field, {
    label: "Repo"
  }, ownedRepos.length === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "subtle",
    style: {
      fontSize: 12.5
    }
  }, "You don't own any repos yet. Create one first.") : /*#__PURE__*/React.createElement("select", {
    className: "input",
    value: repo,
    onChange: e => setRepo(e.target.value)
  }, ownedRepos.map(r => /*#__PURE__*/React.createElement("option", {
    key: r.id,
    value: r.id
  }, r.id)))), /*#__PURE__*/React.createElement("div", {
    style: {
      height: 12
    }
  }), /*#__PURE__*/React.createElement(Field, {
    label: "Secret",
    hint: "Optional \u2014 used to HMAC-sign each payload"
  }, /*#__PURE__*/React.createElement("input", {
    className: "input",
    value: secret,
    onChange: e => setSecret(e.target.value),
    placeholder: "(optional signing secret)"
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      height: 12
    }
  }), /*#__PURE__*/React.createElement(Field, {
    label: "Events"
  }, /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 6,
      flexWrap: "wrap"
    }
  }, EVENTS.map(e => /*#__PURE__*/React.createElement("label", {
    key: e,
    className: "chip",
    style: {
      cursor: "pointer",
      padding: "0 10px",
      height: 24
    }
  }, /*#__PURE__*/React.createElement("input", {
    type: "checkbox",
    checked: events.includes(e),
    onChange: () => toggleEvent(e),
    style: {
      marginRight: 6
    }
  }), e)))), /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 8,
      marginTop: 14
    }
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn",
    onClick: () => {
      setNewHook(false);
      setErr("");
    }
  }, "Cancel"), /*#__PURE__*/React.createElement("span", {
    className: "spacer"
  }), /*#__PURE__*/React.createElement("button", {
    className: "btn primary",
    onClick: add,
    disabled: !url || !repo || busy
  }, busy ? "Adding…" : "Add webhook"))) : null, list.length === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      marginTop: 14,
      padding: "20px 18px",
      color: "var(--fg-3)",
      fontSize: 13
    }
  }, "No webhooks yet. Add one to get a POST on push, PR, and more.") : /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      marginTop: 14
    }
  }, list.map((w, i) => /*#__PURE__*/React.createElement("div", {
    key: w.id,
    style: {
      display: "flex",
      alignItems: "center",
      gap: 16,
      padding: "14px 18px",
      borderBottom: i < list.length - 1 ? "1px solid var(--line)" : "none"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      width: 8,
      height: 8,
      borderRadius: 999,
      background: w.status === "ok" ? "var(--accent)" : "var(--warn)"
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "mono",
    style: {
      fontSize: 13,
      marginBottom: 4,
      overflow: "hidden",
      textOverflow: "ellipsis",
      whiteSpace: "nowrap"
    }
  }, w.url), /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 6,
      flexWrap: "wrap"
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "mono subtle",
    style: {
      fontSize: 11.5
    }
  }, w.repo), w.events.map(e => /*#__PURE__*/React.createElement("span", {
    key: e,
    className: "chip",
    style: {
      height: 18,
      fontSize: 10.5
    }
  }, e))), /*#__PURE__*/React.createElement("div", {
    className: "subtle",
    style: {
      fontSize: 11.5,
      marginTop: 4
    }
  }, "Last delivery ", w.lastDelivery)), /*#__PURE__*/React.createElement("button", {
    className: "btn sm",
    onClick: () => test(w)
  }, tested === w.id ? "Sent ✓" : "Test"), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    style: {
      color: "var(--danger)"
    },
    onClick: () => remove(w)
  }, "Delete")))));
}

// Scanner — per-user, BYO endpoint. Drives the orchis-scan PR check.
function ScannerPanel() {
  const [cfg, setCfg] = React.useState(null);
  const [saved, setSaved] = React.useState(false);
  const [err, setErr] = React.useState("");
  React.useEffect(() => {
    if (window.OrchisAPI) {
      window.OrchisAPI.get("/v1/me/scanner").then(c => setCfg({
        ...c,
        apiKey: ""
      })).catch(() => setCfg({
        enabled: false,
        provider: "anthropic",
        baseUrl: "",
        model: "",
        hasKey: false,
        apiKey: ""
      }));
    } else {
      setCfg({
        enabled: false,
        provider: "anthropic",
        baseUrl: "",
        model: "",
        hasKey: false,
        apiKey: ""
      });
    }
  }, []);
  if (!cfg) return /*#__PURE__*/React.createElement("div", {
    className: "muted",
    style: {
      padding: 20
    }
  }, "Loading\u2026");
  const set = (k, v) => {
    setCfg(c => ({
      ...c,
      [k]: v
    }));
    setSaved(false);
  };
  const save = async () => {
    setErr("");
    setSaved(false);
    try {
      const body = {
        enabled: cfg.enabled,
        provider: cfg.provider,
        baseUrl: cfg.baseUrl,
        model: cfg.model
      };
      if (cfg.apiKey) body.apiKey = cfg.apiKey;
      const res = await window.OrchisAPI.put("/v1/me/scanner", body);
      setCfg({
        ...res,
        apiKey: ""
      });
      setSaved(true);
    } catch (e) {
      setErr("Could not save. Check the fields and try again.");
    }
  };
  const isAnthropic = cfg.provider === "anthropic";
  return /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    style: dsStyles.head
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("h2", {
    style: dsStyles.h2
  }, "Your model (scanner + AI assist)"), /*#__PURE__*/React.createElement("p", {
    className: "muted",
    style: dsStyles.subtitle
  }, "One model config, used two ways: it powers the ", /*#__PURE__*/React.createElement("span", {
    className: "mono"
  }, "orchis-scan"), " supply-chain check on PRs in repos you own, and the in-PR ", /*#__PURE__*/React.createElement("strong", null, "AI assist"), " (Summarize / Explain diff / Draft description). Use Claude, or point at any OpenAI-compatible endpoint you host (Ollama, LM Studio, vLLM, llama.cpp). The \"enable scanning\" toggle below only controls the automatic scan; AI assist works whenever a model is set."))), err ? /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "8px 12px",
      border: "1px solid var(--danger)",
      borderRadius: 6,
      color: "var(--danger)",
      fontSize: 12.5,
      margin: "14px 0"
    }
  }, err) : null, saved ? /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "8px 12px",
      border: "1px solid var(--accent-line)",
      background: "var(--accent-soft)",
      borderRadius: 6,
      color: "var(--accent)",
      fontSize: 12.5,
      margin: "14px 0"
    }
  }, "Saved.") : null, /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      padding: 18,
      marginTop: 14,
      display: "flex",
      flexDirection: "column",
      gap: 16
    }
  }, /*#__PURE__*/React.createElement("label", {
    className: "row",
    style: {
      gap: 10,
      cursor: "pointer"
    }
  }, /*#__PURE__*/React.createElement("input", {
    type: "checkbox",
    checked: cfg.enabled,
    onChange: e => set("enabled", e.target.checked)
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      fontSize: 14,
      fontWeight: 500
    }
  }, "Enable scanning on my repos' PRs")), /*#__PURE__*/React.createElement(Field, {
    label: "Provider"
  }, /*#__PURE__*/React.createElement("select", {
    className: "input",
    value: cfg.provider,
    onChange: e => set("provider", e.target.value)
  }, /*#__PURE__*/React.createElement("option", {
    value: "anthropic"
  }, "Anthropic (Claude Sonnet / Opus)"), /*#__PURE__*/React.createElement("option", {
    value: "openai_compatible"
  }, "OpenAI-compatible endpoint (Ollama / LM Studio / vLLM / OpenAI / \u2026)"))), !isAnthropic ? /*#__PURE__*/React.createElement(Field, {
    label: "Base URL",
    hint: "OpenAI-compatible base, e.g. http://your-host:11434/v1 (Ollama) or https://api.openai.com/v1"
  }, /*#__PURE__*/React.createElement("input", {
    className: "input",
    value: cfg.baseUrl,
    onChange: e => set("baseUrl", e.target.value),
    placeholder: "http://localhost:11434/v1"
  })) : null, /*#__PURE__*/React.createElement(Field, {
    label: "Model",
    hint: isAnthropic ? "e.g. claude-sonnet-4-5 or claude-opus-4-1 (blank = sonnet)" : "e.g. qwen2.5-coder:14b, llama3.1, gpt-4o-mini"
  }, /*#__PURE__*/React.createElement("input", {
    className: "input",
    value: cfg.model,
    onChange: e => set("model", e.target.value),
    placeholder: isAnthropic ? "claude-sonnet-4-5" : "qwen2.5-coder"
  })), /*#__PURE__*/React.createElement(Field, {
    label: "API key",
    hint: cfg.hasKey ? "A key is saved. Leave blank to keep it; type to replace." : isAnthropic ? "Your Anthropic API key (sk-ant-…)" : "Optional — many local servers need no key"
  }, /*#__PURE__*/React.createElement("input", {
    className: "input",
    type: "password",
    value: cfg.apiKey,
    onChange: e => set("apiKey", e.target.value),
    placeholder: cfg.hasKey ? "•••••••• (saved)" : "",
    autoComplete: "off"
  })), /*#__PURE__*/React.createElement("div", {
    className: "row"
  }, /*#__PURE__*/React.createElement("span", {
    className: "subtle",
    style: {
      fontSize: 11.5
    }
  }, "Your key is stored server-side and never shown back to the browser."), /*#__PURE__*/React.createElement("span", {
    className: "spacer"
  }), /*#__PURE__*/React.createElement("button", {
    className: "btn primary",
    onClick: save
  }, "Save scanner"))), /*#__PURE__*/React.createElement("p", {
    className: "subtle",
    style: {
      marginTop: 14,
      fontSize: 12
    }
  }, /*#__PURE__*/React.createElement(Icons.Bolt, {
    size: 11,
    style: {
      verticalAlign: "-1px",
      marginRight: 4
    }
  }), "On each PR open or push, the scanner reviews the diff and posts a result. It never blocks merges by default \u2014 it's a visible check you can act on."));
}
function AppsPanel() {
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("h2", {
    style: dsStyles.h2
  }, "OAuth apps"), /*#__PURE__*/React.createElement("p", {
    className: "muted",
    style: dsStyles.subtitle
  }, "Apps you've authorized to act on your behalf."), /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      marginTop: 14
    }
  }, [{
    name: "kelp-deploy-bot",
    scopes: ["repo:read", "actions:read"],
    by: "kelp",
    last: "today"
  }, {
    name: "atlas-dashboard",
    scopes: ["repo:read"],
    by: "open-strata",
    last: "2 weeks ago"
  }].map((a, i) => /*#__PURE__*/React.createElement("div", {
    key: a.name,
    style: {
      display: "flex",
      alignItems: "center",
      gap: 16,
      padding: "14px 18px",
      borderBottom: i === 0 ? "1px solid var(--line)" : "none"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width: 32,
      height: 32,
      borderRadius: 8,
      background: "var(--bg-2)",
      display: "flex",
      alignItems: "center",
      justifyContent: "center"
    }
  }, /*#__PURE__*/React.createElement(Icons.Bolt, {
    size: 14
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 14,
      fontWeight: 500
    }
  }, a.name), /*#__PURE__*/React.createElement("div", {
    className: "subtle",
    style: {
      fontSize: 11.5
    }
  }, "by ", a.by, " \xB7 last used ", a.last), /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 6,
      marginTop: 6
    }
  }, a.scopes.map(s => /*#__PURE__*/React.createElement("span", {
    key: s,
    className: "mono chip",
    style: {
      fontSize: 10.5
    }
  }, s)))), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    style: {
      color: "var(--danger)"
    }
  }, "Revoke")))));
}
function PreferencesPanel() {
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("h2", {
    style: dsStyles.h2
  }, "Preferences"), /*#__PURE__*/React.createElement("p", {
    className: "muted",
    style: dsStyles.subtitle
  }, "Tune Orchis to your workflow."), /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      marginTop: 14,
      padding: 4
    }
  }, /*#__PURE__*/React.createElement(PrefRow, {
    label: "Email me on review requests",
    defaultOn: true
  }), /*#__PURE__*/React.createElement(PrefRow, {
    label: "Email me when a PAT is about to expire",
    defaultOn: true
  }), /*#__PURE__*/React.createElement(PrefRow, {
    label: "Show 'Cmd+K' hint on home",
    defaultOn: true
  }), /*#__PURE__*/React.createElement(PrefRow, {
    label: "Show keyboard shortcuts in tooltips",
    defaultOn: true
  }), /*#__PURE__*/React.createElement(PrefRow, {
    label: "Auto-merge when checks pass (per-PR opt-in)"
  })));
}
function PrefRow({
  label,
  defaultOn
}) {
  const [on, setOn] = React.useState(!!defaultOn);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      padding: "10px 14px",
      borderBottom: "1px solid var(--line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      fontSize: 13
    }
  }, label), /*#__PURE__*/React.createElement("button", {
    onClick: () => setOn(o => !o),
    style: {
      width: 36,
      height: 20,
      borderRadius: 999,
      border: "1px solid " + (on ? "var(--accent-line)" : "var(--line-strong)"),
      background: on ? "var(--accent)" : "var(--bg-2)",
      cursor: "pointer",
      padding: 0,
      position: "relative"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      top: 1,
      left: on ? 17 : 1,
      width: 16,
      height: 16,
      borderRadius: 999,
      background: on ? "var(--accent-fg)" : "var(--fg-2)",
      transition: "left 120ms"
    }
  })));
}
function Field({
  label,
  hint,
  children
}) {
  return /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 8,
      marginBottom: 6
    }
  }, /*#__PURE__*/React.createElement("label", {
    style: {
      fontSize: 12.5,
      fontWeight: 500
    }
  }, label), hint ? /*#__PURE__*/React.createElement("span", {
    className: "subtle",
    style: {
      fontSize: 11.5
    }
  }, hint) : null), children);
}
const dsStyles = {
  page: {
    maxWidth: 1080,
    margin: "0 auto",
    padding: "36px 36px 80px"
  },
  layout: {
    display: "grid",
    gridTemplateColumns: "220px 1fr",
    gap: 32
  },
  sideTabs: {
    display: "flex",
    flexDirection: "column",
    gap: 2
  },
  sideTab: {
    display: "flex",
    alignItems: "center",
    gap: 10,
    height: 34,
    padding: "0 10px",
    background: "transparent",
    border: "1px solid transparent",
    borderRadius: 6,
    cursor: "pointer",
    color: "var(--fg-1)",
    fontSize: 13,
    font: "inherit",
    fontWeight: 450
  },
  sideTabActive: {
    background: "var(--bg-2)",
    color: "var(--fg)",
    borderColor: "var(--line)"
  },
  main: {
    minWidth: 0
  },
  head: {
    display: "flex",
    alignItems: "flex-start",
    justifyContent: "space-between",
    gap: 12,
    flexWrap: "wrap"
  },
  h2: {
    fontSize: 18,
    fontWeight: 500,
    margin: "0 0 4px",
    letterSpacing: "-0.01em"
  },
  subtitle: {
    margin: 0,
    fontSize: 13
  }
};
window.DevSettingsView = DevSettingsView;

// ===== src/views/search.jsx =====
// Code search view — full-text search across repo contents (git grep backend).
function SearchView({
  route,
  setRoute
}) {
  const [q, setQ] = React.useState(route && route.q || "");
  const [lang, setLang] = React.useState("");
  const [repo, setRepo] = React.useState(route && route.repo || "");
  const [hits, setHits] = React.useState(null);
  const [loading, setLoading] = React.useState(false);
  const inputRef = React.useRef(null);
  React.useEffect(() => {
    setTimeout(() => inputRef.current && inputRef.current.focus(), 20);
  }, []);
  React.useEffect(() => {
    if (!window.OrchisAPI || q.trim().length < 2) {
      setHits(q.trim() ? [] : null);
      return;
    }
    let cancelled = false;
    setLoading(true);
    const t = setTimeout(() => {
      const params = new URLSearchParams({
        q: q.trim()
      });
      if (lang) params.set("lang", lang);
      if (repo) params.set("repo", repo);
      window.OrchisAPI.get("/v1/search/code?" + params.toString()).then(res => {
        if (!cancelled) {
          setHits(res || []);
          setLoading(false);
        }
      }).catch(() => {
        if (!cancelled) {
          setHits([]);
          setLoading(false);
        }
      });
    }, 200);
    return () => {
      cancelled = true;
      clearTimeout(t);
    };
  }, [q, lang, repo]);

  // Group hits by repo for display.
  const groups = React.useMemo(() => {
    const m = new Map();
    (hits || []).forEach(h => {
      if (!m.has(h.repo)) m.set(h.repo, []);
      m.get(h.repo).push(h);
    });
    return [...m.entries()].map(([name, items]) => ({
      name,
      items
    }));
  }, [hits]);
  const langs = ["", "go", "js", "ts", "py", "rs", "rb", "java", "c", "cpp", "sh", "md", "json", "yaml", "html", "css"];
  return /*#__PURE__*/React.createElement("div", {
    style: searchStyles.scroll
  }, /*#__PURE__*/React.createElement("div", {
    style: searchStyles.page,
    className: "fade-in"
  }, /*#__PURE__*/React.createElement("h1", {
    style: {
      fontSize: 22,
      fontWeight: 500,
      letterSpacing: "-0.015em",
      margin: "0 0 4px"
    }
  }, "Code search"), /*#__PURE__*/React.createElement("p", {
    className: "muted",
    style: {
      margin: "0 0 16px",
      fontSize: 13
    }
  }, "Literal full-text search across your repositories."), /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 8,
      marginBottom: 14,
      flexWrap: "wrap"
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 8,
      flex: 1,
      minWidth: 240,
      border: "1px solid var(--line)",
      borderRadius: 8,
      padding: "0 10px",
      background: "var(--bg-1)"
    }
  }, /*#__PURE__*/React.createElement(Icons.Search, {
    size: 15,
    style: {
      color: "var(--fg-2)"
    }
  }), /*#__PURE__*/React.createElement("input", {
    ref: inputRef,
    value: q,
    onChange: e => setQ(e.target.value),
    placeholder: "Search code\u2026 (min 2 chars)",
    style: {
      flex: 1,
      border: "none",
      outline: "none",
      background: "transparent",
      color: "var(--fg)",
      font: "inherit",
      fontSize: 14,
      height: 40
    }
  })), /*#__PURE__*/React.createElement("select", {
    className: "input",
    value: lang,
    onChange: e => setLang(e.target.value),
    style: {
      height: 40
    }
  }, langs.map(l => /*#__PURE__*/React.createElement("option", {
    key: l,
    value: l
  }, l === "" ? "Any language" : l))), repo ? /*#__PURE__*/React.createElement("button", {
    className: "btn sm",
    onClick: () => setRepo(""),
    title: "Clear repo filter"
  }, repo, " \u2715") : null), q.trim().length >= 2 && !loading && groups.length === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      padding: "20px 18px",
      color: "var(--fg-3)",
      fontSize: 13
    }
  }, "No matches.") : null, loading ? /*#__PURE__*/React.createElement("div", {
    className: "muted",
    style: {
      fontSize: 12.5,
      padding: "4px 2px"
    }
  }, "Searching\u2026") : null, groups.map(g => /*#__PURE__*/React.createElement("div", {
    key: g.name,
    style: {
      marginBottom: 18
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 6,
      marginBottom: 6
    }
  }, /*#__PURE__*/React.createElement(Icons.Repo, {
    size: 13,
    style: {
      color: "var(--fg-2)"
    }
  }), /*#__PURE__*/React.createElement("span", {
    className: "mono",
    style: {
      fontSize: 12.5,
      fontWeight: 500
    }
  }, g.name), /*#__PURE__*/React.createElement("span", {
    className: "subtle",
    style: {
      fontSize: 11
    }
  }, g.items.length, " hit", g.items.length === 1 ? "" : "s")), /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      overflow: "hidden"
    }
  }, g.items.map((h, i) => /*#__PURE__*/React.createElement("button", {
    key: i,
    onClick: () => setRoute({
      view: "repo",
      repo: h.repo,
      file: h.path
    }),
    style: {
      ...searchStyles.hit,
      borderBottom: i < g.items.length - 1 ? "1px solid var(--line)" : "none"
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 8,
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "mono subtle",
    style: {
      fontSize: 11.5,
      overflow: "hidden",
      textOverflow: "ellipsis",
      whiteSpace: "nowrap"
    }
  }, h.path), /*#__PURE__*/React.createElement("span", {
    className: "mono subtle",
    style: {
      fontSize: 11,
      flexShrink: 0
    }
  }, ":", h.line)), /*#__PURE__*/React.createElement("code", {
    style: searchStyles.snippet
  }, highlight(h.text, q.trim())))))))));
}

// highlight wraps case-insensitive literal matches of q in the snippet.
function highlight(text, q) {
  if (!q) return text;
  const lower = text.toLowerCase();
  const ql = q.toLowerCase();
  const out = [];
  let i = 0,
    k = 0;
  while (i < text.length) {
    const idx = lower.indexOf(ql, i);
    if (idx < 0) {
      out.push(text.slice(i));
      break;
    }
    if (idx > i) out.push(text.slice(i, idx));
    out.push(/*#__PURE__*/React.createElement("mark", {
      key: k++,
      style: {
        background: "var(--accent-soft)",
        color: "var(--accent)",
        borderRadius: 3,
        padding: "0 1px"
      }
    }, text.slice(idx, idx + q.length)));
    i = idx + q.length;
  }
  return out;
}
const searchStyles = {
  scroll: {
    height: "100%",
    overflowY: "auto"
  },
  page: {
    maxWidth: 900,
    margin: "0 auto",
    padding: "28px 24px 60px"
  },
  hit: {
    display: "flex",
    flexDirection: "column",
    gap: 4,
    width: "100%",
    textAlign: "left",
    padding: "9px 12px",
    background: "transparent",
    border: "none",
    cursor: "pointer",
    font: "inherit",
    color: "inherit"
  },
  snippet: {
    fontFamily: "var(--font-mono)",
    fontSize: 12,
    color: "var(--fg-1)",
    whiteSpace: "pre",
    overflow: "hidden",
    textOverflow: "ellipsis"
  }
};
window.SearchView = SearchView;

// ===== src/views/newpr.jsx =====
// Open-a-PR — compare two branches and create a pull request.
function NewPRView({
  route,
  setRoute
}) {
  const [repo, setRepo] = React.useState(route && route.repo || "");
  const [repos, setRepos] = React.useState([]);
  const [branches, setBranches] = React.useState([]);
  const [base, setBase] = React.useState("");
  const [head, setHead] = React.useState(route && route.head || "");
  const [cmp, setCmp] = React.useState(null);
  const [title, setTitle] = React.useState("");
  const [body, setBody] = React.useState("");
  const [reviewer, setReviewer] = React.useState("");
  const [busy, setBusy] = React.useState(false);
  const [err, setErr] = React.useState("");
  const titleTouched = React.useRef(false);

  // If no repo given, offer a picker (owned repos).
  React.useEffect(() => {
    if (!repo && window.OrchisAPI) window.OrchisAPI.get("/v1/repos").then(rs => setRepos(rs || [])).catch(() => {});
  }, [repo]);

  // Load branches for the chosen repo; seed base = default branch.
  React.useEffect(() => {
    if (!repo || !window.OrchisAPI) return;
    window.OrchisAPI.get(`/v1/repos/${repo}/branches`).then(bs => {
      setBranches(bs || []);
      const def = (bs || []).find(b => b.isDefault);
      if (def && !base) setBase(def.name);
    }).catch(() => {});
  }, [repo]);

  // Compare whenever base+head are both set and differ.
  React.useEffect(() => {
    if (!repo || !base || !head || base === head || !window.OrchisAPI) {
      setCmp(null);
      return;
    }
    let cancelled = false;
    window.OrchisAPI.get(`/v1/repos/${repo}/compare?base=${encodeURIComponent(base)}&head=${encodeURIComponent(head)}`).then(res => {
      if (cancelled) return;
      setCmp(res);
      if (!titleTouched.current && res.commits && res.commits.length) {
        setTitle(res.commits[0].message.split("\n")[0]);
      }
    }).catch(() => {
      if (!cancelled) setCmp(null);
    });
    return () => {
      cancelled = true;
    };
  }, [repo, base, head]);
  const create = async () => {
    if (!repo || !head || !title.trim()) return;
    setErr("");
    setBusy(true);
    try {
      const pr = await window.OrchisAPI.post(`/v1/repos/${repo}/pulls`, {
        title: title.trim(),
        head,
        base,
        body
      });
      if (reviewer.trim()) {
        await window.OrchisAPI.post(`/v1/repos/${repo}/pulls/${pr.id}/request-review`, {
          reviewer: reviewer.trim()
        }).catch(() => {});
      }
      setRoute({
        view: "pr",
        pr: pr.id,
        repo
      });
    } catch (e) {
      setErr("Could not open the PR — the head branch may have no new commits, or you lack write access.");
      setBusy(false);
    }
  };
  const branchOpts = branches.map(b => b.name);
  const canCompare = repo && base && head && base !== head;
  return /*#__PURE__*/React.createElement("div", {
    style: npStyles.scroll
  }, /*#__PURE__*/React.createElement("div", {
    style: npStyles.page,
    className: "fade-in"
  }, /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      marginBottom: 16
    }
  }, /*#__PURE__*/React.createElement(Icons.PR, {
    size: 18,
    style: {
      color: "var(--accent)"
    }
  }), /*#__PURE__*/React.createElement("h1", {
    style: {
      fontSize: 20,
      fontWeight: 500,
      margin: "0 0 0 8px"
    }
  }, "Open a pull request")), err ? /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "8px 12px",
      border: "1px solid var(--danger)",
      borderRadius: 6,
      color: "var(--danger)",
      fontSize: 12.5,
      marginBottom: 14
    }
  }, err) : null, /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      padding: 14,
      marginBottom: 16,
      display: "flex",
      flexDirection: "column",
      gap: 10
    }
  }, !route || !route.repo ? /*#__PURE__*/React.createElement(Field, {
    label: "Repository"
  }, /*#__PURE__*/React.createElement("select", {
    className: "input",
    value: repo,
    onChange: e => {
      setRepo(e.target.value);
      setBase("");
      setHead("");
    }
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "Choose a repo\u2026"), repos.map(r => /*#__PURE__*/React.createElement("option", {
    key: r.id,
    value: r.id
  }, r.id)))) : null, /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 10,
      flexWrap: "wrap"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 180
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "section-title",
    style: {
      marginBottom: 4
    }
  }, "Base (merge into)"), /*#__PURE__*/React.createElement("select", {
    className: "input",
    value: base,
    onChange: e => setBase(e.target.value),
    disabled: !repo
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "base\u2026"), branchOpts.map(b => /*#__PURE__*/React.createElement("option", {
    key: b,
    value: b
  }, b)))), /*#__PURE__*/React.createElement("div", {
    style: {
      alignSelf: "flex-end",
      padding: "0 4px 8px",
      color: "var(--fg-3)"
    }
  }, "\u2190"), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 180
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "section-title",
    style: {
      marginBottom: 4
    }
  }, "Compare (head)"), /*#__PURE__*/React.createElement("select", {
    className: "input",
    value: head,
    onChange: e => setHead(e.target.value),
    disabled: !repo
  }, /*#__PURE__*/React.createElement("option", {
    value: ""
  }, "head\u2026"), branchOpts.map(b => /*#__PURE__*/React.createElement("option", {
    key: b,
    value: b
  }, b))))), canCompare && cmp ? /*#__PURE__*/React.createElement("div", {
    className: "subtle",
    style: {
      fontSize: 12
    }
  }, cmp.aheadBy, " commit", cmp.aheadBy === 1 ? "" : "s", " \xB7 ", /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--accent)"
    }
  }, "+", cmp.additions), " ", /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--danger)"
    }
  }, "\u2212", cmp.deletions), " \xB7 ", cmp.filesCount, " file", cmp.filesCount === 1 ? "" : "s") : canCompare ? /*#__PURE__*/React.createElement("div", {
    className: "subtle",
    style: {
      fontSize: 12
    }
  }, "Comparing\u2026") : null), canCompare && cmp && cmp.aheadBy === 0 ? /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      padding: "16px 18px",
      color: "var(--fg-3)",
      fontSize: 13,
      marginBottom: 16
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "mono"
  }, head), " is not ahead of ", /*#__PURE__*/React.createElement("span", {
    className: "mono"
  }, base), " \u2014 nothing to merge.") : null, canCompare && cmp && cmp.aheadBy > 0 ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      padding: 14,
      marginBottom: 16,
      display: "flex",
      flexDirection: "column",
      gap: 10
    }
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Title"
  }, /*#__PURE__*/React.createElement("input", {
    className: "input",
    value: title,
    onChange: e => {
      titleTouched.current = true;
      setTitle(e.target.value);
    },
    placeholder: "Pull request title"
  })), /*#__PURE__*/React.createElement(Field, {
    label: "Description"
  }, /*#__PURE__*/React.createElement("textarea", {
    value: body,
    onChange: e => setBody(e.target.value),
    placeholder: "What does this change and why?",
    style: {
      width: "100%",
      height: 110,
      padding: 10,
      border: "1px solid var(--line)",
      borderRadius: 6,
      fontFamily: "inherit",
      fontSize: 13,
      background: "var(--bg-1)",
      color: "var(--fg)",
      resize: "vertical"
    }
  })), /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 10
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1
    }
  }, /*#__PURE__*/React.createElement(Field, {
    label: "Request a reviewer (optional)"
  }, /*#__PURE__*/React.createElement("input", {
    className: "input",
    value: reviewer,
    onChange: e => setReviewer(e.target.value),
    placeholder: "handle"
  }))), /*#__PURE__*/React.createElement("button", {
    className: "btn primary",
    style: {
      alignSelf: "flex-end",
      height: 36
    },
    disabled: busy || !title.trim(),
    onClick: create
  }, busy ? "Opening…" : "Create pull request"))), /*#__PURE__*/React.createElement("div", {
    className: "section-title",
    style: {
      margin: "0 0 8px"
    }
  }, cmp.commits.length, " commit", cmp.commits.length === 1 ? "" : "s"), /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      marginBottom: 16,
      overflow: "hidden"
    }
  }, cmp.commits.map((c, i) => /*#__PURE__*/React.createElement("div", {
    key: c.sha,
    className: "row",
    style: {
      gap: 10,
      padding: "9px 14px",
      borderBottom: i < cmp.commits.length - 1 ? "1px solid var(--line)" : "none"
    }
  }, /*#__PURE__*/React.createElement(Icons.Commit, {
    size: 13,
    style: {
      color: "var(--fg-2)"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: 1,
      fontSize: 13,
      overflow: "hidden",
      textOverflow: "ellipsis",
      whiteSpace: "nowrap"
    }
  }, c.message.split("\n")[0]), /*#__PURE__*/React.createElement("span", {
    className: "mono subtle",
    style: {
      fontSize: 11
    }
  }, c.short)))), /*#__PURE__*/React.createElement("div", {
    className: "section-title",
    style: {
      margin: "0 0 8px"
    }
  }, "Changes"), /*#__PURE__*/React.createElement(ReadOnlyDiff, {
    files: cmp.files
  })) : null));
}
const npStyles = {
  scroll: {
    height: "100%",
    overflowY: "auto"
  },
  page: {
    maxWidth: 940,
    margin: "0 auto",
    padding: "28px 24px 60px"
  }
};
window.NewPRView = NewPRView;

// ===== src/views/commit.jsx =====
// Commit detail — message, author, and the commit's diff.
function CommitView({
  route,
  setRoute
}) {
  const repo = route.repo;
  const sha = route.sha;
  const [data, setData] = React.useState(null);
  const [err, setErr] = React.useState(false);
  React.useEffect(() => {
    if (!window.OrchisAPI || !repo || !sha) return;
    setData(null);
    setErr(false);
    window.OrchisAPI.get(`/v1/repos/${repo}/commits/${sha}`).then(setData).catch(() => setErr(true));
  }, [repo, sha]);
  return /*#__PURE__*/React.createElement("div", {
    style: cvStyles.scroll
  }, /*#__PURE__*/React.createElement("div", {
    style: cvStyles.page,
    className: "fade-in"
  }, /*#__PURE__*/React.createElement("button", {
    className: "btn ghost sm",
    style: {
      marginBottom: 12
    },
    onClick: () => setRoute({
      view: "repo",
      repo
    })
  }, "\u2190 ", repo), err ? /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      padding: 18,
      color: "var(--fg-3)",
      fontSize: 13
    }
  }, "Commit not found.") : null, !data && !err ? /*#__PURE__*/React.createElement("div", {
    className: "muted",
    style: {
      fontSize: 12.5
    }
  }, "Loading\u2026") : null, data ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("div", {
    className: "card",
    style: {
      padding: 16,
      marginBottom: 16
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 16,
      fontWeight: 500,
      whiteSpace: "pre-wrap",
      marginBottom: 8
    }
  }, data.message), /*#__PURE__*/React.createElement("div", {
    className: "row",
    style: {
      gap: 10,
      fontSize: 12,
      color: "var(--fg-2)"
    }
  }, /*#__PURE__*/React.createElement(Icons.Commit, {
    size: 13
  }), /*#__PURE__*/React.createElement("span", {
    className: "mono"
  }, data.short), /*#__PURE__*/React.createElement("span", null, "\xB7"), /*#__PURE__*/React.createElement("span", null, data.author), /*#__PURE__*/React.createElement("span", {
    className: "subtle"
  }, data.email), /*#__PURE__*/React.createElement("span", {
    className: "spacer"
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--accent)"
    }
  }, "+", data.additions), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--danger)"
    }
  }, "\u2212", data.deletions))), /*#__PURE__*/React.createElement(ReadOnlyDiff, {
    files: data.files
  })) : null));
}
const cvStyles = {
  scroll: {
    height: "100%",
    overflowY: "auto"
  },
  page: {
    maxWidth: 940,
    margin: "0 auto",
    padding: "24px 24px 60px"
  }
};
window.CommitView = CommitView;

// ===== src/app.jsx =====
// Main app — shell, routing, command palette, tweaks.

const TWEAK_DEFAULTS = /*EDITMODE-BEGIN*/{
  "accent": "spring",
  "density": "default",
  "font": "geist",
  "showSplitTip": true
} /*EDITMODE-END*/;
const ACCENTS = {
  spring: {
    name: "Spring",
    accent: "oklch(62% 0.14 150)",
    soft: "oklch(94% 0.04 150)",
    line: "oklch(80% 0.08 150)",
    darkAccent: "oklch(72% 0.16 150)",
    darkSoft: "oklch(28% 0.06 150)",
    darkLine: "oklch(45% 0.1 150)"
  },
  cobalt: {
    name: "Cobalt",
    accent: "oklch(60% 0.16 250)",
    soft: "oklch(94% 0.04 250)",
    line: "oklch(80% 0.08 250)",
    darkAccent: "oklch(70% 0.18 250)",
    darkSoft: "oklch(28% 0.07 250)",
    darkLine: "oklch(45% 0.12 250)"
  },
  ember: {
    name: "Ember",
    accent: "oklch(62% 0.16 40)",
    soft: "oklch(94% 0.04 40)",
    line: "oklch(80% 0.09 40)",
    darkAccent: "oklch(72% 0.17 40)",
    darkSoft: "oklch(30% 0.07 40)",
    darkLine: "oklch(45% 0.12 40)"
  },
  violet: {
    name: "Violet",
    accent: "oklch(58% 0.17 300)",
    soft: "oklch(94% 0.04 300)",
    line: "oklch(78% 0.08 300)",
    darkAccent: "oklch(70% 0.18 300)",
    darkSoft: "oklch(28% 0.07 300)",
    darkLine: "oklch(45% 0.12 300)"
  }
};
const FONTS = {
  geist: {
    sans: '"Geist", ui-sans-serif, -apple-system, sans-serif',
    mono: '"Geist Mono", ui-monospace, monospace',
    label: "Geist"
  },
  serifMix: {
    sans: '"Fraunces", "Geist", serif',
    mono: '"Geist Mono", monospace',
    label: "Editorial"
  },
  mono: {
    sans: '"Geist Mono", monospace',
    mono: '"Geist Mono", monospace',
    label: "All mono"
  }
};
function App() {
  const [theme, setThemeState] = React.useState(() => {
    return localStorage.getItem("orchis-theme") || (window.matchMedia("(prefers-color-scheme: dark)").matches ? "dark" : "light");
  });
  const [t, setTweak] = useTweaks(TWEAK_DEFAULTS);
  const [route, setRoute] = React.useState({
    view: "home"
  });
  const [paletteOpen, setPaletteOpen] = React.useState(false);
  const [splitOpen, setSplitOpen] = React.useState(true);
  const [splitContent, setSplitContent] = React.useState({
    type: "prs",
    repo: "kelp/atlas"
  });
  const setTheme = v => {
    setThemeState(v);
    localStorage.setItem("orchis-theme", v);
  };

  // Apply theme + density + accent to document
  React.useEffect(() => {
    document.documentElement.setAttribute("data-theme", theme);
  }, [theme]);
  React.useEffect(() => {
    document.documentElement.setAttribute("data-density", t.density);
  }, [t.density]);
  React.useEffect(() => {
    const a = ACCENTS[t.accent] || ACCENTS.spring;
    const root = document.documentElement;
    if (theme === "dark") {
      root.style.setProperty("--accent", a.darkAccent);
      root.style.setProperty("--accent-soft", a.darkSoft);
      root.style.setProperty("--accent-line", a.darkLine);
    } else {
      root.style.setProperty("--accent", a.accent);
      root.style.setProperty("--accent-soft", a.soft);
      root.style.setProperty("--accent-line", a.line);
    }
  }, [t.accent, theme]);
  React.useEffect(() => {
    const f = FONTS[t.font] || FONTS.geist;
    document.documentElement.style.setProperty("--font-sans", f.sans);
    document.documentElement.style.setProperty("--font-mono", f.mono);
  }, [t.font]);

  // Cmd+K palette
  React.useEffect(() => {
    const handler = e => {
      if ((e.metaKey || e.ctrlKey) && (e.key === "k" || e.key === "K")) {
        e.preventDefault();
        setPaletteOpen(o => !o);
      }
      if ((e.metaKey || e.ctrlKey) && e.key === "\\") {
        e.preventDefault();
        if (route.view === "repo") setSplitOpen(s => !s);
      }
    };
    window.addEventListener("keydown", handler);
    return () => window.removeEventListener("keydown", handler);
  }, [route.view]);
  const navigate = next => {
    setRoute(next);
    if (next.newToken || next.newKey || next.newHook) {
      // already encoded
    }
  };
  const openSplit = content => {
    setSplitContent(content);
    setSplitOpen(true);
  };

  // Render current view
  let view;
  if (route.view === "home") view = /*#__PURE__*/React.createElement(DashboardView, {
    setRoute: navigate,
    openPalette: () => setPaletteOpen(true),
    route: route
  });else if (route.view === "prs") view = /*#__PURE__*/React.createElement(PRsView, {
    setRoute: navigate
  });else if (route.view === "pr") view = /*#__PURE__*/React.createElement(PRView, {
    prId: route.pr,
    repo: route.repo,
    setRoute: navigate
  });else if (route.view === "search") view = /*#__PURE__*/React.createElement(SearchView, {
    route: route,
    setRoute: navigate
  });else if (route.view === "newpr") view = /*#__PURE__*/React.createElement(NewPRView, {
    route: route,
    setRoute: navigate
  });else if (route.view === "commit") view = /*#__PURE__*/React.createElement(CommitView, {
    route: route,
    setRoute: navigate
  });else if (route.view === "settings") view = /*#__PURE__*/React.createElement(DevSettingsView, {
    route: route,
    setRoute: navigate
  });else if (route.view === "repo") view = /*#__PURE__*/React.createElement(RepoView, {
    repoId: route.repo,
    file: route.file,
    setRoute: navigate,
    openSplit: openSplit,
    splitOpen: splitOpen,
    splitContent: splitContent,
    closeSplit: () => setSplitOpen(false)
  });else view = /*#__PURE__*/React.createElement(DashboardView, {
    setRoute: navigate,
    openPalette: () => setPaletteOpen(true),
    route: route
  });
  return /*#__PURE__*/React.createElement("div", {
    style: appStyles.shell
  }, /*#__PURE__*/React.createElement(Sidebar, {
    route: route,
    setRoute: navigate,
    openPalette: () => setPaletteOpen(true),
    theme: theme,
    setTheme: setTheme
  }), /*#__PURE__*/React.createElement("main", {
    style: appStyles.main
  }, view), /*#__PURE__*/React.createElement(Palette, {
    open: paletteOpen,
    onClose: () => setPaletteOpen(false),
    onNavigate: navigate
  }), t.showSplitTip && route.view === "repo" ? /*#__PURE__*/React.createElement("div", {
    style: appStyles.tip,
    className: "fade-in"
  }, /*#__PURE__*/React.createElement(Icons.Bolt, {
    size: 12,
    style: {
      color: "var(--accent)"
    }
  }), /*#__PURE__*/React.createElement("span", null, "Try ", /*#__PURE__*/React.createElement("span", {
    className: "kbd"
  }, "\u2318\\"), " to toggle split. ", /*#__PURE__*/React.createElement("span", {
    className: "kbd"
  }, "\u2318K"), " reaches anything."), /*#__PURE__*/React.createElement("button", {
    className: "btn ghost icon sm",
    onClick: () => setTweak({
      showSplitTip: false
    })
  }, /*#__PURE__*/React.createElement(Icons.Close, {
    size: 11
  }))) : null, /*#__PURE__*/React.createElement(TweaksPanel, {
    title: "Tweaks"
  }, /*#__PURE__*/React.createElement(TweakSection, {
    label: "Theme"
  }, /*#__PURE__*/React.createElement(TweakRadio, {
    label: "Mode",
    value: theme,
    onChange: setTheme,
    options: [{
      label: "Light",
      value: "light"
    }, {
      label: "Dark",
      value: "dark"
    }]
  }), /*#__PURE__*/React.createElement(TweakSelect, {
    label: "Accent",
    value: t.accent,
    onChange: v => setTweak({
      accent: v
    }),
    options: Object.entries(ACCENTS).map(([k, v]) => ({
      label: v.name,
      value: k
    }))
  })), /*#__PURE__*/React.createElement(TweakSection, {
    label: "Layout"
  }, /*#__PURE__*/React.createElement(TweakRadio, {
    label: "Density",
    value: t.density,
    onChange: v => setTweak({
      density: v
    }),
    options: [{
      label: "Compact",
      value: "compact"
    }, {
      label: "Default",
      value: "default"
    }, {
      label: "Cozy",
      value: "cozy"
    }]
  }), /*#__PURE__*/React.createElement(TweakSelect, {
    label: "Font",
    value: t.font,
    onChange: v => setTweak({
      font: v
    }),
    options: Object.entries(FONTS).map(([k, v]) => ({
      label: v.label,
      value: k
    }))
  })), /*#__PURE__*/React.createElement(TweakSection, {
    label: "Behavior"
  }, /*#__PURE__*/React.createElement(TweakToggle, {
    label: "Keyboard tip",
    value: t.showSplitTip,
    onChange: v => setTweak({
      showSplitTip: v
    })
  })), /*#__PURE__*/React.createElement(TweakSection, {
    label: "Try"
  }, /*#__PURE__*/React.createElement(TweakButton, {
    label: "Open \u2318K palette",
    onClick: () => setPaletteOpen(true)
  }), /*#__PURE__*/React.createElement(TweakButton, {
    label: "Create a token (3-step)",
    onClick: () => navigate({
      view: "settings",
      tab: "tokens",
      newToken: true
    })
  }))));
}

// (Tweak controls all come from tweaks-panel.jsx)

const appStyles = {
  shell: {
    display: "flex",
    height: "100%",
    overflow: "hidden",
    background: "var(--bg)"
  },
  main: {
    flex: 1,
    position: "relative",
    minWidth: 0,
    overflow: "hidden"
  },
  tip: {
    position: "absolute",
    bottom: 16,
    left: "50%",
    transform: "translateX(-50%)",
    display: "flex",
    alignItems: "center",
    gap: 10,
    padding: "8px 12px",
    background: "var(--bg-1)",
    border: "1px solid var(--line-strong)",
    borderRadius: 999,
    boxShadow: "var(--shadow-md)",
    fontSize: 12,
    color: "var(--fg-1)",
    zIndex: 100
  }
};

// --- Auth gate -------------------------------------------------------------
// Wraps the app: checks /v1/me, shows a sign-in screen on 401, hydrates the
// real user into USERS.me on success. If the fetch fails entirely (no backend,
// e.g. opening index.html from disk), falls back to the mock so the prototype
// still works standalone.
function SignIn() {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: "fixed",
      inset: 0,
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      background: "var(--bg)",
      color: "var(--fg)",
      fontFamily: "var(--font-sans)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      width: 360,
      maxWidth: "90vw",
      padding: 32,
      border: "1px solid var(--line)",
      borderRadius: 14,
      background: "var(--bg-1)",
      textAlign: "center"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 22,
      fontWeight: 600,
      letterSpacing: "-0.02em",
      marginBottom: 6
    }
  }, "Orchis Foundry"), /*#__PURE__*/React.createElement("div", {
    style: {
      fontSize: 13.5,
      color: "var(--fg-2)",
      marginBottom: 24
    }
  }, "A calm, self-hosted code platform. Sign in to continue."), /*#__PURE__*/React.createElement("a", {
    href: "/v1/auth/oidc/github",
    style: {
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      gap: 10,
      padding: "11px 16px",
      borderRadius: 10,
      textDecoration: "none",
      background: "var(--fg)",
      color: "var(--bg)",
      fontSize: 14,
      fontWeight: 500
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: "18",
    height: "18",
    viewBox: "0 0 24 24",
    fill: "currentColor",
    "aria-hidden": "true"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M12 2a10 10 0 0 0-3.16 19.49c.5.09.68-.22.68-.48v-1.7c-2.78.6-3.37-1.34-3.37-1.34-.45-1.16-1.11-1.46-1.11-1.46-.91-.62.07-.6.07-.6 1 .07 1.53 1.03 1.53 1.03.89 1.53 2.34 1.09 2.91.83.09-.65.35-1.09.63-1.34-2.22-.25-4.55-1.11-4.55-4.94 0-1.09.39-1.99 1.03-2.69-.1-.25-.45-1.27.1-2.65 0 0 .84-.27 2.75 1.02a9.6 9.6 0 0 1 5 0c1.91-1.29 2.75-1.02 2.75-1.02.55 1.38.2 2.4.1 2.65.64.7 1.03 1.6 1.03 2.69 0 3.84-2.34 4.69-4.57 4.93.36.31.68.92.68 1.85v2.74c0 .27.18.58.69.48A10 10 0 0 0 12 2Z"
  })), "Sign in with GitHub")));
}
function Root() {
  const [state, setState] = React.useState("loading"); // loading | authed | signin
  React.useEffect(() => {
    fetch("/v1/me", {
      credentials: "same-origin"
    }).then(r => {
      if (r.status === 401) {
        setState("signin");
        return null;
      }
      if (!r.ok) throw new Error("me failed");
      return r.json();
    }).then(async me => {
      if (me) {
        // Hydrate the mock user object in place so the (untouched) views see
        // the real signed-in identity.
        Object.assign(USERS.me, {
          id: me.id || "me",
          name: me.name || me.handle,
          handle: me.handle,
          initials: me.initials || "?",
          color: me.color || USERS.me.color,
          avatarUrl: me.avatarUrl || "",
          isAdmin: !!me.isAdmin
        });
        // Replace the mock repo list with the user's real repos.
        try {
          await window.loadRepos();
        } catch (e) {/* keep mock on failure */}
        setState("authed");
      }
    }).catch(() => {
      // No backend reachable (standalone prototype) — keep the mock.
      setState("authed");
    });
  }, []);
  if (state === "loading") {
    return /*#__PURE__*/React.createElement("div", {
      style: {
        position: "fixed",
        inset: 0,
        background: "var(--bg)"
      }
    });
  }
  if (state === "signin") return /*#__PURE__*/React.createElement(SignIn, null);
  return /*#__PURE__*/React.createElement(App, null);
}
ReactDOM.createRoot(document.getElementById("root")).render(/*#__PURE__*/React.createElement(Root, null));

// Mobile responsive — collapse sidebar on small screens
const respCss = document.createElement("style");
respCss.textContent = `
@media (max-width: 760px) {
  aside { width: 56px !important; }
  aside [style*="font-size: 14px"], aside [style*="font-size:14px"] { display: none; }
  aside button span:not(:first-child) { display: none; }
  aside button { justify-content: center !important; }
  aside > div:nth-of-type(2) { display: none; }
  aside nav button { padding: 0 !important; width: 36px; height: 36px; }
  aside [class*="section-title"] { display: none; }
}
`;
document.head.appendChild(respCss);